# MV2-070 implementation evidence

## C01c2b4a saved-state recovery — 11 September 2026

Implemented on isolated `codex/HappyDucky02/mv2-070-draft-recovery` from `8bcf636`
after the explicit refinement. A selected saved draft has an identifier-only
fragment/link; reopening it reads current settings/history through the existing
private API. Next history state is preserved. No configuration, request key or
permission is stored in the URL/browser storage. Failed reads, new-draft creation,
successful deletion and explicit list reset clear the appropriate locator/state.
Five-language guidance distinguishes server-saved recovery from memory-only edits
and uncertain new-draft retry keys.

Page hiding synchronously clears private component state and aborts outstanding
requests. Restoring a cached document reloads session and data, so another tab's
principal/access changes are rechecked. Non-departing links no longer prompt for
discard; accepted ordinary links navigate once. Workspace/sign-out approval only
bypasses a second unload prompt after the existing committed-navigation event.

Production build/type checks and frontend gates passed, including two locator/history
tests. **10 API/backlog checks passed.** The production-browser suite passed
**31 full-document axe checkpoints**, five-language links/reload/current revision,
revoked/default-off/missing locator clearing, actual Back/Forward document traversal,
cached-document privacy/session revalidation, modified/new-tab/download/anchor
clicks and committed-only unload behavior. Recovery performs no writes. Existing
CRUD/delivery journeys also passed. The test harness now waits for a genuinely
new document, preventing stale previous-page assertions during navigation.
Browser APIs remain synthetic; incomplete axe and independent native-language/
screen-reader review remain open.

The previous `5e318b0` automatic release was still testing during implementation;
public readiness remained `fc62f43`. This candidate's activation is unverified.
This accepts saved-state recovery only. Same-document traversal cancellation while
editing, recovery of unsaved fields/request keys after a crash and complete editing
navigation acceptance remain C01c2b4b. Source/Start/live user gates remain open;
MV2-070 stays IN PROGRESS. See the [behavior and limitations](../POLLEN_DRAFT_READER.md#c01c2b4a-saved-state-recovery).

## C01c2b3 delivery preferences — 11 September 2026

Implemented in isolated `codex/HappyDucky02/mv2-070-delivery-settings` from
`5e318b0`, after the explicit backlog refinement. New and existing supported
drafts can edit email mode, explicit digest time, optional distinct quiet hours
and timezone through the same preview/create/revision-CAS paths. New drafts
remain email-off by default. Mode changes clear digest time; disabling quiet
hours clears the interval. Overnight intervals and midnight clocks are retained.
Five-language explanations distinguish saved preferences from live consent,
scheduling and email delivery. Contextual help documents these controls.

Production build/type checks and existing frontend gates passed. **35 API,
configuration and backlog tests passed**, including a new real API journey
covering invalid schedules without revisions, exact persisted preferences,
immutable historical schedules and unchanged job/outbox counts. Targeted Ruff
passed. The expanded production-browser suite passed **26 full-document axe
checkpoints** plus five-language missing/equal/overnight clock validation, all
mode transitions, explicit clearing, timezone labels, preview invalidation,
exact create/edit payloads, numeric preservation and uncertain-write history
recovery. Existing read/create/edit/delete journeys passed. All browser APIs are
synthetic; the mobile schedule screenshot was visually inspected. Incomplete
axe findings remain for human review; native-language/screen-reader sign-off
and real workspace/user acceptance are still pending.

Prior `fc62f43` was actually activated at `2026-09-11T16:35:06Z` after all gates,
including 2261 API passes and 12 skips; public readiness confirms that SHA.
The controller tests `5e318b0` independently. This delivery-settings candidate
is not yet verified active. No source approval, collector, Start, consent record,
outbox producer or scheduler is added. Runtime timezone/DST scheduling and
consent/permission checks remain C04; broader configurations, navigation/crash
recovery and source/live acceptance remain open. MV2-070 stays IN PROGRESS.

## C01c2b2 revision-checked editing — 11 September 2026

Implemented on isolated `codex/HappyDucky02/mv2-070-draft-edit` from `0e28584`
after recording the explicit refinement. The [numeric editor](../POLLEN_DRAFT_READER.md#c01c2b2-revision-checked-editing)
preserves supported v1 metadata and saved delivery preferences, requires a new
server preview, and appends against the reviewed expected revision. Unsupported
settings stay read-only. Conflict recovery requires discard/reload/review; a lost
response uses current state and exact next immutable history without an extra
write. Historical proof is distinguished from current settings and request ownership.

Production build/type checks and existing frontend gates passed, including
**15 configuration-compatibility checks**. **20 API/repository/backlog tests passed.**
The expanded production-browser suite passed **21 full-document axe checkpoints**
and five-language editing, exact Decimal/metadata/preference preservation, CSRF,
double clicks, original-revision conflict, lost-response/later-revision history
recovery, unchanged explicit retry, conflicting history, discard/reload, revocation
and unsupported-category read-only behavior. Existing reader/create/delete journeys
also passed. APIs are intercepted synthetic fixtures; incomplete axe results remain
for human review, with no native-language or screen-reader sign-off.
The final suite also verifies departure protection after an uncertain save of
unchanged settings. The narrow editor screenshot was visually inspected.

The preceding `fc62f43` automatic release remains in progress with no recorded
error; its full suite was not restarted or duplicated. Public readiness still
reports `40e7283bc4b7d61b56c6fc2b212c7f35cec3b315` at `2026-09-11T16:25:41Z`.
This editor's activation is unverified. Delivery-preference editing, broader unsupported settings,
navigation/crash recovery, real workspace and source/live acceptance remain open.
MV2-070 stays IN PROGRESS.

## C01c2b1 revision-checked deletion — 11 September 2026

Implemented on isolated `codex/HappyDucky02/mv2-070-draft-delete` from `fc62f43`,
after the explicit backlog refinement. The [private reader](../POLLEN_DRAFT_READER.md#c01c2b1-confirmed-deletion)
offers manager-only draft deletion with exact station/revision confirmation and
history-removal warning. Synchronous duplicate protection and disabled competing
reads preserve the attempted snapshot. Conflicts require explicit reload/review;
uncertain and missing outcomes remain distinct from confirmed success. Success
clears private detail/history and restarts pagination. No Start or live lifecycle
operation is introduced, and no source or rollout setting changes.

Production build, type checking and existing frontend gates passed. The expanded
browser suite passed **16 full-document axe checkpoints** and five-language
deletion confirmation/cancel/success, duplicate-click, CSRF, conflict/fresh-revision,
uncertain-then-missing, revocation and active/viewer-denial cases. Earlier reader
and creator journeys still passed. **10 API/DB/backlog checks passed**, including
the existing evaluation/configuration-history deletion cascade. Browser APIs and
records were synthetic; independent native/screen-reader and real-user acceptance
remain pending. Actual candidate activation is still unverified.

The preceding `40e7283` release completed all gates at `2026-09-11T15:08:19Z`
after 2261 API tests passed (12 skipped); public readiness confirmed that SHA.
The next automatic `fc62f43` run continues independently. Editing, delivery
preferences, navigation/crash recovery and live source/product acceptance remain
open; MV2-070 stays IN PROGRESS.

## C01c2a private draft creation — 11 September 2026

Implemented on isolated `codex/HappyDucky02/mv2-070-draft-create` from `56dd5b1`,
after recording the refinement and reading the installed Next server/client guide.
The [creator](../POLLEN_DRAFT_READER.md#c01c2a-new-private-drafts) adds multiple
allergens, explicit observation/forecast periods, threshold/reset and rapid rules,
station and timezone. A server configuration-only preview precedes explicit Save.
Editing invalidates preview; double-submit protection and a frozen exact request
key/payload preserve idempotent retry after an uncertain committed save. Email is
off, categories are not enabled, and no Start request or source activation is made.

Final production build/type checking and existing frontend gates passed.
**34 API/contract/backlog tests passed.** The expanded real-production-browser
suite passed **11 full-document axe checkpoints** (six reader plus five creator)
and five-language creation journeys, keyboard form entry, narrow layout, exact
Decimal payloads, observation/forecast rules, invalid preview, preview invalidation,
CSRF, double clicks, committed-save/lost-response retry, saved-record navigation,
discard decisions and revoked access. All APIs were synthetic and intercepted;
the mobile form screenshot was visually inspected. Incomplete axe findings remain
retained for manual review; this is not a native-language or screen-reader sign-off.

At `2026-09-11T14:53:57Z`, the automatic `40e7283` release was still running with
no recorded error. Public release remains the previously verified `1144a47`.
Candidate activation, existing-draft edit/delete, delivery-preference editing,
browser Back/Forward/crash recovery and live source/product acceptance remain open.
MV2-070 stays IN PROGRESS; no running full suite was restarted or duplicated.

## C01c1 private draft reader — 11 September 2026

Implemented the [draft reader](../POLLEN_DRAFT_READER.md) in isolated
`codex/HappyDucky02/mv2-070-draft-ui` from `40e7283`, after recording the refinement.
Read the installed Next 16.3.3 server/client guide before frontend implementation.
The `/pollen-watch` route reads private list/detail/history with bounded cursors,
exact saved Decimal strings, distinct access/error states and disabled Start.
The complete principal keys component state; abort and response checks suppress
obsolete reads. No persistent/shared private cache or mutation is introduced.
Five-language copy and the required contextual guide are included.

The production build passed the existing i18n/shell/resource/report/help checks
and Next compilation/type checking. Initial browser checks passed five full-document
axe checkpoints with no reported violations, keyboard activation, 390px/desktop
layouts, five locales, pagination, late responses, access/revocation and error
states. Native-language/screen-reader, live workspace and full product acceptance
remain pending. Final verification passed **six full-document axe checkpoints**,
including expanded history after repairing its heading order. Keyboard selection
focuses the settings heading, with a tested mobile-header scroll offset. The mobile
screenshot was visually inspected. Final Next compilation/type checking, i18n/help
checks and **nine API/backlog regressions** passed. No source admission, live
coverage, Start or rollout is claimed. Candidate activation remains unverified.

The preceding `1144a47` release completed all gates at `2026-09-11T13:42:41Z`
after 2225 API tests passed (12 skipped), and public readiness confirmed that SHA.
The independent `40e7283` release is still being tested; no run was restarted.

## C02c1 private checkpoint/evidence ledger — 11 September 2026

Implemented the [internal rehearsal ledger](../EVALUATION_LEDGER.md) on isolated
`codex/HappyDucky02/mv2-070-numeric-ledger`, based on Monitoring `5a07cd4`.
The explicit refinement was recorded before implementation. Additive generic
tables retain exact binding, versioned request, evaluation decision and checkpoint.
Subject/revision admission, owner/organization scope, current membership, a subject
write lock, sequence CAS and unique request/material identities preserve private
state and immutable evidence in one caller-owned transaction. Draft deletion
cascades through all added records. No route, collector, live admission or outbox
is introduced; stored requests and returned entries are labelled draft rehearsal.

The initial SQLite regression run passed 45 cases. The expanded PostgreSQL 17 run
passed 28 cases (17 ledger and 11 existing draft repository cases), including
simultaneous initial/existing checkpoint writers, idempotency, failure injection,
rollback, historical input, isolation/revocation and additive upgrade. Its disposable
container used one CPU, 512 MB, tmpfs and a random loopback port and was removed.
After adding retained request time/input, final verification passed **59 SQLite
and component/API/contract/backlog cases** and all **17 ledger cases again on
isolated PostgreSQL 17**. Targeted Ruff and diff checks passed. Both temporary
PostgreSQL containers were removed. At `2026-09-11T13:16:45Z`, the public site
remained ready on `030ab29`; the independent automatic `1144a47` API suite was
still running. This candidate's actual activation remains unverified.
MV2-070 remains IN PROGRESS; this does not complete C02 or live rollout.

## C02b2 numeric coordination — 11 September 2026

Implemented the [numeric coordinator](../POLLEN_NUMERIC_COORDINATOR.md) on the
existing isolated task branch based on `1144a47`. The refinement was recorded
before implementation. Combined configurations derive separately validated
threshold/rapid components from the same private rule revision. Contiguous rapid
matches do not repeat transitions; eligible false/true changes reset/reopen the
rapid condition. Initial/recovery/policy/correction baselines are silent.
Independent rule states prevent one reset from falsely resolving the whole monitor.
Simultaneous transitions return one material ID with all reasons and component
evidence. JSON restart, exact replay and checkpoint retry preserve deduplication.

The final regression run passed **129 cases** (19 coordinator cases, 109 existing
rapid/threshold/configuration checks and the mandatory actual-backlog consistency
check), including the shared source-order refactor and combined forecast rules
with future valid times. Targeted Ruff and diff checks passed.
No database/outbox, live source, rollout, authorization or delivery is introduced;
full history recomputation, categories, atomic ledger and UI remain open.

**Actual prior release:** `030ab29` completed every automatic gate at
`2026-09-11T12:21:06Z`: 2180 API tests passed, 12 skipped, build/backup/start/public
health/publication succeeded. Independent public readiness confirmed that SHA.
It includes C01a/C01b1 and C02a, with source-backed Start still blocked. This is
release evidence for implemented components, not completion of MV2-070.

## C02b1 exact-window rapid comparison — 11 September 2026

Implemented the [rapid comparison component](../POLLEN_RAPID_COMPONENT.md) on the
isolated MV2-070 branch based on Monitoring `030ab29`. It binds both source records
to one private configuration and exact source/method/station/allergen/period;
forecasts also require the same issue/member/grid/layer/cell. Only the configured
1–24-hour interval is accepted. Decimal subtraction uses its own sufficient
precision; missing/ineligible data never becomes a numeric zero or a match.
Approved historical endpoints may be older than their former live freshness
deadline; current data must still be fresh. Both inputs, evaluation time and
unavailable reason are retained. Comparison IDs are deterministic and change
with either source correction, but are explicitly not notification IDs.

The first run passed **109 focused cases**: 45 new rapid comparisons, 39 threshold
cases and 25 configuration cases. Tests cover exact/inexact window endpoints,
inclusive/negative/zero deltas, precision beyond the ambient Decimal context,
policy/quality/rights/clock gating, historical baseline handling, serialization,
corrections and private/forecast identity. Targeted Ruff passed. Final verification
passed all **110 cases**, including the actual-backlog consistency check, after
adding retained evaluation time and finite-delta validation. Diff checks passed.

No endpoint, database migration, source approval, rollout or delivery is added.
Overlapping rapid episodes, combined rules and correction/recovery coordination
remain C02b2; categories/historical recomputation and atomic ledger/outbox remain
open. MV2-070/C02 stay IN PROGRESS. The active automatic `030ab29` API run was not
restarted. Prior `bd4fcc5` failed only the expected 49 old-date digest cases; its
full log reported 2087 passed and 12 skipped, with no new failure category.

## C02a threshold component — 11 September 2026

Implemented the [versioned pure threshold component](../POLLEN_THRESHOLD_COMPONENT.md)
on the existing isolated task branch after fast-forwarding Monitoring `cbca4b0`.
Immutable source/sample/forecast identity, private configuration binding and
previous/current evidence support restartable numeric state. Inclusive Decimal
trigger/reset and hysteresis distinguish baseline, material transitions,
unavailable/recovery, latest corrections and older-history handoff. Conflicting
same-revision bytes and mixed source/private bindings fail. Refetching old bytes
cannot extend freshness under the same policy. Initial high state and recovery
establish a baseline without an invented crossing notification.

**64 focused tests passed:** 39 new component cases and 25 existing configuration
cases. These cover boundaries, missing/high initial state, stale/revoked data,
expired refetch, recovery, source revisions, JSON restart, deterministic transition
identity, precision beyond default Decimal context, mixed private/source identity,
forecast separation and unsupported rule rejection. Targeted Ruff passed; the
existing unrelated Starlette deprecation remains.
The final run including the mandatory actual-backlog consistency check passed
all **65 cases**; diff checks passed after the older-receipt replay guard.

No route invokes this component, and no database/outbox, source approval,
authorization grant, rollout or consent is added. Source-policy admission/period
alignment remain adapter obligations. Rapid/category and historical recomputation
are C02b; transactional ledger/outbox is C02c. C02 and MV2-070 remain IN PROGRESS.

## Earlier checkpoints

Status: IN PROGRESS. Host: HappyDucky02. Implementer: Codex, Backend scope.
Task: `codex/HappyDucky02/mv2-070-personal-subjects`, based on `9034523`.
The C01a refinement was recorded in the active backlog before implementation.

## C01a — internal draft persistence, 2026-09-11

Added generic `monitoring_subjects` and `monitoring_subject_revisions` tables,
registered in the existing organization-scoping policy. Migration
`f0627ab1d43e` follows `ef5169a0c32d` and only creates these tables/indexes;
legacy tables, legal CHECKs and APIs are unchanged. A composite foreign key
prevents a revision from pointing into another organization.

`helvetic_lens.monitoring_subjects` provides create, get, bounded list, revise,
bounded history and delete operations for private drafts. It validates the
MV2-069 configuration, retains Decimal values canonically, stores a payload hash,
and keeps old revisions when settings change. Returned configuration dictionaries
are copies. It has no public route, collector, Start or delivery side effect.

The caller supplies a server-authenticated user ID and owns the transaction.
Every repository call rechecks active membership in the session's organization;
each subject query/mutation additionally filters the owner. Existing administrator
membership permits draft writes; viewers may read their own records but cannot
write. Unknown roles, revoked membership and inactive users fail closed. Shared
organization administration does not grant access to another person's draft.

Create keys are unique per organization + owner. A retry with identical normalized
settings returns the same subject, including its current revision after later edits;
different settings with that original key produce a conflict. The database unique
constraint/savepoint also handles concurrent duplicate requests. Deleting a draft
deletes its key and private revision history; post-deletion replay/tombstone policy
for active monitors remains C01b/C06 work.

Edit/delete require the expected revision. A single conditional write checks owner,
organization, draft state and current revision. Edit appends its new revision in
the same transaction; stale writers cannot overwrite/delete a newer revision.
SQLite's legacy savepoint mode is handled explicitly so the caller's rollback
also rolls back a create/edit instead of accidentally retaining a released savepoint.
Active subjects cannot be changed through these draft operations.

## Verification

- **10 repository tests passed on SQLite**, across the initial nine-case run and
  the additional predecessor-schema upgrade case.
- **The same 10 cases passed on PostgreSQL 17**, using disposable containers,
  memory-backed data, a loopback-only random port and a unique schema per test.
  Containers were removed in `finally`; no serving database/volume was used.
- **3 existing MV2-001 tests passed**, including populated-copy characterization,
  preserved evidence hashes, legacy legal CHECK rejection and rollout isolation.
- Targeted Ruff and diff whitespace checks passed. An existing Starlette/httpx
  deprecation warning remains; it is unrelated to this change.

Coverage includes concurrent duplicate creation, key/payload conflicts, revision
history after a new session/repeated migration, stale edits and deletes, same-tenant
other-owner denial, cross-tenant reads/history/FK denial, role changes after a cached
read, inactive users, viewer write denial, invalid/private configuration fields,
atomic rollback, private-history deletion and refusal to mutate active subjects.
The migration case upgrades from the previous schema with populated organizations,
users and memberships and verifies all of their stored rows remain identical.

To reproduce, run `pytest tests/test_monitoring_subjects.py` from `services/api`
with an isolated temporary directory. For PostgreSQL, explicitly set
`MV2_SUBJECT_TEST_DATABASE_URL` to a disposable `postgresql+psycopg` URL on
`127.0.0.1` with database name `mv2_c01_test`; the harness refuses other hosts or
database names. Do not point it at any serving instance.

## Remaining boundaries

C01a is implemented/tested; automatic deployment of its exact commit is pending.
Revision immutability is enforced through this repository's append-only operations,
not a blanket claim that arbitrary privileged SQL cannot change rows. C01a did not
add a personal HTTP or worker surface; the C01b1 continuation below adds the gated
HTTP surface and pagination. At the C01a checkpoint, pagination beyond bounded lists,
authenticated API/CSRF, explicit Start, source/rollout gate wiring, lifecycle/UI,
delivery consent, deletion/export of active monitor data and full C01/C07 acceptance
remain necessary. MV2-069 source/category/decoder/prototype gates are still open.
Passing these persistence tests does not complete MV2-070 or Pollen Watch.

## C01a-R1 — release metadata failure follow-up

The c2ab0d6 automatic run failed before activation: 2,091 tests passed, 12 skipped,
and the existing actual-backlog test failed on index/detail status mismatch. Its
MV2-069 index still said PLANNED. Subsequent 001/070 detail changes also required
index synchronization. The accepted public release remained 8d5e94b; no failing
candidate was promoted. At discovery the scheduler was already testing 9034523.

Synchronized the three index rows with the recorded detail evidence and added a
workflow instruction to run the existing exact-backlog check before publishing any
status change. No parser, test assertion, task denominator or deployment gate was
relaxed. This follow-up is separate from the tested C01a implementation 0c69b95.
Verification: all **45 existing monitoring-progress tests passed**, including the
actual repository backlog, strict mismatches, malformed snapshots and deferred
exclusions; diff whitespace checks passed. Full automatic release remains pending.

## C01b1 — authenticated draft API, 2026-09-11

Added the [private draft API](../PERSONAL_DRAFT_API.md) through the existing app
middleware. Server-side configuration defaults off; only an exact shadow grant
for a Monitoring workspace permits the routes. The gate never treats anonymous
development access or a caller's body field as a personal identity/source approval.
The serving environment was not changed to enable these endpoints.

Implemented create/read/edit/delete, configuration-only preview, paged lists and
history. The Start endpoint checks ownership/revision and explicitly rejects the
unaccepted source/live workflow gate; it neither activates a subject nor creates
a job. Existing viewer and CSRF policies remain in force. Responses do not cache
private settings, and the shared per-user rate bucket cannot be bypassed by varying
subject paths. The broader application endpoints retain their existing policy.

Verification: **8 HTTP tests passed** (seven flow/security tests plus the additional
shared-rate test); **11 repository regressions passed on SQLite**, including a new
equal-timestamp/history pagination case; **4 existing authentication tests passed**;
the mandatory actual-backlog consistency check passed. The new pagination case
also passed against disposable PostgreSQL 17, with cleanup confirmed by the runner.
Targeted Ruff passed. Only the existing Starlette deprecation warning remains.

HTTP coverage includes separate-workspace and same-workspace other-owner reads,
history, cursor, edit/delete/Start denial; role downgrade; default/wrong-workspace/
wrong-instance/unsupported-mode rollout rejection; CSRF; secret-value omission;
idempotent create and payload conflict; stale revision; paging; private deletion;
no monitor/job from preview and no activation/job from Start. No live source,
email, browser/device, successful Start or deployment acceptance is claimed.
