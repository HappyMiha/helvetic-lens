# MV2-001 execution evidence — 2026-09-11

Host: HappyDucky02. Task: `codex/HappyDucky02/mv2-001-extension-contracts`.
Integration target: `codex/HappyDucky02/monitoring-v2`, based on `cb47c01`.
Contract v1; no new database migration, source adapter, UI route or live feature flag.
Implementation: `8d5e94b786885609cf4413f17a640ac6a8097792`. Status: DONE.
The release acceptance below completes this foundation's verification boundary.

## Results

- `python scripts/monitoring_compatibility_inventory.py --revision cb47c01 --output docs/monitoring-v2/evidence/mv2-001-inventory.json`: passed. 174 API route declarations, 30 web pages, 75 model declarations; no removed baseline routes or altered legacy models; all 35 unfinished legacy items mapped; exact annotated MVP tag unchanged.
- `python -m pytest services/api/tests/test_monitoring_contracts.py services/api/tests/test_regulatory_corpus.py services/api/tests/test_basel_stadt.py -q`: 20 passed, 1 failed initially. The failure was exact-response comparison of a moving pagination capture timestamp in the test; the production response was not changed.
- After fixing that test's clock, `python -m pytest services/api/tests/test_monitoring_contracts.py -q`: **3 passed in 2.35s**. Together with the unchanged 18 corpus/Basel tests, all 21 selected tests passed. One existing Starlette/httpx deprecation warning remains.
- `python -m ruff check services/api/helvetic_lens/monitoring_contracts.py services/api/tests/test_monitoring_contracts.py scripts/monitoring_compatibility_inventory.py`: passed.
- `git diff --check`: passed before publication.

Native Windows used the existing integration checkout's Python environment with
PYTHONPATH explicitly pointed at this task's `services/api`. Database copies and
test artifacts are isolated under this task's parent work directory. No deployed
data, production credentials, live source requests or email delivery were used.

## SHA-256 fixtures and inventory

| File | SHA-256 |
|---|---|
| test_monitoring_contracts.py | `85a305b61e29da6a95a5a6f126a239e39fee0aca4d0eb66f454a824fda4c6b16` |
| test_regulatory_corpus.py | `16716d545fa5170e6e1469cc3bd04c8b11da05537c2eef01b83bdbb41e6c56b2` |
| test_basel_stadt.py | `fe7cb8fd00864af915e106b5ae5d5c23a0868383fd94cb4c59b336b06819e1c0` |
| mv2-001-inventory.json | `824bf2631f63fb24de85e0695c9409105e786b6b7772708e3c9afaf8aab5e0ca` |

## Acceptance review and limits

AC1: pinned baseline/tag and complete 35-item mapping verified from Git.
AC2: legal CHECK rejection exercised on a populated SQLite copy; additive schema,
bridge identity/admission and fallback are specified in the ADR. No schema rewrite.
AC3: immutable state facts, versioned calculations, optional AI and actor decisions
have distinct contract/provenance fields aligned with ARCHITECTURE.md.

Codex reviewed the current model constraints, organization scoping, job dispatch,
compatibility routes and boundary tests. This is not independent product review.
The current API has no rollout consumers yet; MV2-070 must wire and verify them.
The test copy is synthetic SQLite, not a production PostgreSQL snapshot or the
full MV2-056 migration/recovery rehearsal. Source feasibility, live pollen delivery,
native-language review and real-user readiness remain later gates.

At task start the public site independently reported `ready`, instance `monitoring-v2`,
release `git-cb47c01c0ef4c3632e0e65388d4092801ca42c2d`; the automatic deployment
journal reported all gates succeeded for that same SHA. That validates the prior
operating channel, not deployment of this change.

## Automatic release acceptance — verified 2026-09-11

The deployment journal records `status=succeeded`, target and activated SHA
`8d5e94b786885609cf4413f17a640ac6a8097792`, finished at **07:04:38 UTC**.
Every recorded step succeeded: checkout, configuration validation, API lint,
full API tests, images, model-runtime capture, writer quiescence, pre-deploy
backup, startup, public health and release publication. No gate was bypassed.
The history summary records success but not a test count; none is inferred here.

At the 07:29 UTC continuation, an independent HTTPS request to
`https://monitoring.helveticlens.ch/api/ready` returned `status=ready` and
`release=git-8d5e94b786885609cf4413f17a640ac6a8097792`. The next automatic run
was already testing c2ab0d6; it did not invalidate the accepted release.
The annotated MVP tag object was checked again and remains unchanged.

AC1–3 and the stated characterization/review protocol are satisfied; MV2-001
is DONE. This closes the foundation task only. The production migration rehearsal,
source acceptance, rollout consumers and actual Pollen Watch remain later work.
