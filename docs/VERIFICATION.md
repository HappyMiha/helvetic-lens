# Verification record

## Organization AI policy and personal-language variants — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl089-organization-policy`, based on `ca69b87` and
  including the separately published release-build fix `df353da`. Added persisted
  organization enable/quota settings, strict/revisioned API writes, tenant/admin/
  CSRF checks, cancellation of obsolete automatic work, and publication fences.
  Migration `d9fb034a6dc7` adds only the policy table; no tenant is enabled by it.
- Corrected the initial proposed organization-language behavior after user review:
  feed/API defaults follow the user, not an admin's organization locale. New
  matching coalesces at most five active member-language admissions; variants
  share the organization's existing quota, never a quota per user. The internal
  legacy locale remains only a fallback when there are no active members. The
  form has no language override. Different languages no longer supersede one
  another's queued/running assessments; exact successful variants remain reusable.
- **84 combined tests passed, 2 PostgreSQL-only tests skipped in 158.23 seconds**
  across policy, assessment storage, automation, jobs and readers. **16 final
  policy tests passed, 1 PostgreSQL-only test skipped in 24.11 seconds**, including
  two authenticated users in the same organization with distinct preferences,
  browser fallback, explicit locale, active/foreign-member filtering, duplicate
  language coalescing, independent variant completion/reuse, saved quotas, API
  permissions, cancellation during generation, history retention and migration.
  The authenticated route test stubs the reader only; other tests use the real
  store/reader/gateway with synthetic model outputs and approval evidence.
- PostgreSQL **17.11** in disposable loopback/tmpfs containers: five initial suites
  passed (persistence, concurrent editors, cancellation during generation, lower
  allowance, migration downgrade/upgrade). After the user-language correction,
  four suites passed (active member languages, independent saved variants,
  cancellation, concurrent editors). These are nine executions of seven distinct
  scenarios, not nine independent scenarios. Only each verified QA container's
  synthetic schema was reset. Both owned containers were stopped/removed; the
  running application/database and production were untouched.
- Final real Next production UI with intercepted synthetic APIs: **20 admin/viewer
  settings journeys** and **10 saved-brief journeys**, covering all five locales
  at 390/1440 pixels, with **40 full-document axe checkpoints**. Save errors and
  conflicts preserve edits; explicit reload restores saved limits; viewers cannot
  edit; brief requests carry user locale and navigation does not invoke inference.
  Mobile screenshots were visually inspected. Other axe incomplete findings and
  native-language/human usability review remain unverified, not certified passed.
- Root **`npm run build` passed** including localization/value audit, 31 shell,
  22 resource and 198 report tests, TypeScript and production compilation. Ruff
  and diff checks pass. The earlier 1,740-test Linux baseline did not contain this
  task. No real-model quality, multilingual inference performance, HappySnowman
  deployment, production migration or live notification delivery is claimed.
- Still open: generating missing older variants after a preference/member change,
  complete automatic refresh, global admission fairness/capacity, downstream
  digest/history/delivery reuse, large dossiers, independent approved-model/language
  and target-hardware/user evaluation. Missing language variants remain explicitly
  unavailable; reading never silently generates or substitutes another language.


## Deployment web-build localization regression — 8 September 2026 (HappyDucky02)

- The reported `97e923f` production attempt failed before activation in the web
  Docker build: the root localization gate rejected unused `prompts.characters`.
  The brief prompt introduced a variable 4,000-character counter but left the
  existing 12,000-character catalogue entry unused. The form now uses the existing
  localized entry for ordinary prompts and the bounded entry for shared briefs.
  The localization gate remains enabled and unchanged.
- Root `npm run build` passes, including localization/value audit, shell/resource/
  report regressions and production Next build. A separate archive of `ca69b87`
  plus **only** `prompt-settings-page.tsx` was built through the real web Dockerfile
  successfully as `helvetic-lens-web-qa:prompts-i18n`. Thus the Docker result does
  not depend on the unfinished organization-policy changes in the working tree.
- This verifies the reported `build_images` blocker locally on Linux Docker. It
  does not record a HappySnowman activation, rerun its API tests, or modify its
  running release/data. The failed historical deployment record remains accurate.

## Persisted model configuration during shared briefs — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl089-model-freshness`, based on `97e923f`.
  Previously the runtime was re-observed but the request retained its old Settings
  object after an administrator saved a different provider/model configuration.
  Service-created runners now compare against fresh, explicitly scoped persisted
  settings before runtime contact, after observation/measurement and at publication.
  Reader checks surround its bounded metadata observation and return `not_current`
  without an answer when that service snapshot became stale.
- **125 combined regressions pass, 1 PostgreSQL-only test skipped**, in 221.07
  seconds, covering configuration, execution, prompts, readers, durable jobs and
  automatic admission. New tests edit the actual database without changing the
  runner's old Settings object. They cover model/provider/profile/output limit/
  sampling edits during runtime, token counting and generation; stale clients,
  queued work, credential/transport-only reuse, reset to environment, privileged
  scope, reused-session refresh and a newer worker's attempt token.
- **43 final configuration/reader tests pass in 69.24 seconds** after the reader
  status clarification; Ruff and diff checks pass.
- Supersession touches only the exact running assessment/token. Existing successful
  history remains intact. No generation is triggered by saves or reads; stale configuration
  before admission prevents runtime contact, and no automatic fallback is added.
  Public configuration hashes exclude credentials and transport-only knobs.
- This is real SQLite/service/ModelClient code with synthetic local runtime and
  approved-model fixtures, not actual model quality or a HappySnowman deployment.
  No schema or frontend changes. PostgreSQL concurrency/target-host acceptance is
  not claimed by this change. The earlier full Linux suite remains a separate
  immutable baseline run and does not include either new prompt/configuration task.

## Shared brief prompt controls — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl089-brief-prompts`, based on `5bf2227`.
  Organization/platform prompt settings now persist an optional 4,000-character
  shared-brief focus. The exact composed request is measured, generated and
  fingerprinted; saved changes fence queued work and in-flight publication.
  Readers withdraw stale results without deleting history or generating work.
- **96 regressions pass, 1 PostgreSQL-only scenario skipped** in 186.99 seconds
  across prompt, execution, reader, job and relation-prompt freshness suites.
  After adding the same organization lock to reset, **7 final prompt scenarios
  pass in 10.61 seconds**. They cover effective platform/organization precedence,
  older clients, empty/oversized API inputs, exact measured/wire prompts, reuse,
  reset/history preservation and changes during runtime/count/generation/queue.
  Test doubles provide model runtime/answers; this is not real-model quality.
- The first targeted run exposed an actual reset/resave duplicate revision
  failure. Revisions now advance past persisted history under the organization
  lock; previous revisions remain intact. No schema migration is required.
- Final production build, TypeScript, Ruff and diff checks pass. The real built
  UI passes **10 mobile/desktop journeys across all five locales**, plus platform
  save and viewer denial, with synthetic intercepted APIs. Save/error/retry/empty
  override preserve the page, draft and correct 4,000-character display limit;
  no AI endpoint is invoked. Ten full-document axe checkpoints report no
  violations; incomplete checks remain in the audit JSON, not certified away.
- Browser QA found and fixed an unlabeled duplicate complementary landmark;
  manual screenshot inspection found and fixed the mismatched character counter.
  Final mobile/desktop screenshots were inspected. Native-language validation,
  real users, deployed end-to-end and target hardware remain unverified.
- HL-089 remains open for policy/quota/locale UI, automatic refresh/catch-up,
  delivery/history reuse, official facts, large dossiers and independent model
  approval. No production restart, database modification or notification.
  The ongoing full Linux API memory-fix run uses the earlier immutable `5bf2227`
  code and does **not** validate these new prompt edits.

## Database retention behind the API gate — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/api-migration-memory`, based on `3af161a`.
  The complete frozen Linux suite on `5edfaa5` collected 1,731 tests and advanced
  to 41%, then hit its 6 GiB RAM allocation and 2,360,827,904 bytes of swap.
  A 120-second stack dump showed SQLite schema migration preparation, not AI or
  an external provider. No OOM kill occurred; repeated memory-limit pressure was
  recorded. The owned QA run was interrupted and then stopped (exit 143); it is
  **incomplete**, not green. No application container or production data was stopped.
- A local GC reference-chain probe traced retained metadata through the engine's
  compiled cache, Database, a listener closure, and SQLAlchemy SessionEvents'
  global weak-key registry. Listener values captured Database, which pointed back
  to their sessionmaker class key. Capturing only the tenant ContextVar removes
  that retention cycle without changing organization selection. Migration
  completion/failure also drops the temporary connection and compiled schema
  cache; it does not change schema revisions or skip migrations.
- **3 lifecycle tests pass on Windows (1.14 seconds) and Linux (3.92 seconds)**:
  disposed Database/engine/session class weak references are released; the real
  migration chain and repeated startup complete; failed migration schema and
  connection objects are released while the engine remains usable. On the
  immutable old source, two of these tests fail and one passes, confirming the
  regression is detected rather than merely restating the implementation.
- **15 combined lifecycle/authentication/organization-access/isolation tests pass**
  (18.77 seconds). Five real PostgreSQL 17 suites also pass: `scope`,
  `current-scope`, `migration`, `current-refresh`, `jobs-concurrency`.
  PostgreSQL was a separately labelled localhost container with ephemeral data;
  only its fixed-ID synthetic database was recreated between cases, then the
  container was removed. No production database, real inference or delivery.
- A repeated-create/close probe decreased GC-tracked objects after 25 apps from
  2,010,306 to 710,581. At 75 fixed-code apps the count was 922,746. These are
  diagnostic object counts, not a claim of zero growth or a production capacity
  benchmark: framework caches and other allocations still exist.
- The full repeated Linux run has **1,747 collected tests**, from an immutable
  `3af161a` source archive plus exactly the changed Database module and new test.
  It retains the full suite and frozen dependencies, UID 1000, read-only source,
  no Docker socket/secrets, and separate temporary data. Its 6 GiB limit has swap
  disabled to avoid host thrashing. **Completed: 1,740 passed, 7 skipped, one
  existing Starlette/httpx deprecation warning, in 3,020.55 seconds (50m20s).**
  The seven skips are PostgreSQL-specific concurrency cases (brief dispatch,
  onboarding milestones, personal onboarding, relation reprocessing, offline
  relation coalescing, and two topic-review cases); they are not represented as
  passing in this SQLite full run. No tests were removed from collection.
  The Docker command exited 0 and the QA container removed itself after completion.
  Its sampled usage near 86%
  was 3.953 GiB / 6 GiB; this is an observation, not a measured peak. The Windows
  bind-mounted source, 2-CPU allocation and local Docker filesystem make elapsed
  time unsuitable as a HappySnowman performance prediction.
  Git blob hashes of the archived changed Database module and lifecycle tests
  match `5bf2227`; all other code comes from `3af161a`. Newer prompt/model-settings
  tasks have separate targeted evidence, not coverage by this immutable run.
  HappySnowman installation, target-host gate and production recovery remain
  unverified. Report artifact: `test-results/full-linux-api-memory-final.txt`.
- Ruff and diff checks pass. The existing Starlette/httpx deprecation warning is
  visible and not suppressed; it is separate from the retained database graph.

## Reviewed deployment-manager recovery — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/deploy-manager-recovery`, based on `5edfaa5`.
  Update-only installation reads a full reviewed commit from the trusted fetched
  main history; it does not pull/reset a production checkout or execute the
  candidate. The real deployment lock guards syntax validation, backup and
  atomic replacement. Active deployments fail closed with exit 75.
- **13 Linux tests passed in 1.28 seconds**, using actual shell, Git, Python
  compilation, flock, file permissions and rename. Coverage includes exact
  committed bytes despite broken working-tree edits, idempotence, contention
  with Python's lock, wrong origin/unmerged revision/invalid syntax/symlink
  rejection, invalid arguments, preserved unrelated cron entries, and recovery
  after a simulated failed atomic rename. Only cron and the intentional rename
  failure use fixture adapters; no user's scheduler is touched.
- The isolated container ran as UID 1000 with a read-only filesystem/source
  snapshot and ephemeral temporary directories. Two explicit source/test files
  were mounted, without Docker socket, production data or credentials. An
  ephemeral pinned pytest installation required package-download network access.
  The test container was removed after completion.
- Initial QA exposed Windows mixed line endings in the copied shell script.
  The passing Linux snapshot uses LF; an explicit Git LF attribute and normalized
  working file prevent that checkout problem. Ruff and diff checks pass.
- The separate full Linux API run uses immutable commit `5edfaa5`; at this
  checkpoint it is still running and is not claimed as a full-suite pass or
  verification of this later installer change. HappySnowman access/installation,
  production restart and target-host release success remain unverified.

## Deployment API-test timeout diagnostics — 8 September 2026 (HappyDucky02)

- User supplied a failed `api_tests` phase on release `be17fcc`: Docker CLI timed
  out after the hard-coded 1,800 seconds. Inspection found `_run` discarded
  `TimeoutExpired.output`, the QA container was unnamed, and no explicit cleanup
  followed CLI timeout. This proves diagnostic/cleanup gaps, not which test on
  HappySnowman was slow or hung.
- Branch `codex/HappyDucky02/deploy-api-test-timeout`, based on `c59d523`. The gate
  now keeps the complete test suite and frozen dependencies, with a two-hour
  default bounded host override, verbose IDs, durations and a 120-second diagnostic
  stack dump. Partial stdout/stderr survives timeout in redacted host logs/error
  excerpts. Unique per-gate containers get targeted cleanup; cleanup failure does
  not hide the original failure or mark an unverified cleanup successful.
- **27 final Linux tests passed in 1.43 seconds**, covering the release manager and its
  persisted history. One test executes a real child process that prints a test ID
  and synthetic secrets then sleeps: its one-second timeout preserves the ID and
  removes the secrets. Other tests cover cleanup command targeting on success,
  failure, timeout and interruption; missing containers, failed cleanup, operator
  budget validation and unchanged lint timeout. External Docker/deployment commands
  in pipeline tests are replaced; this does not claim a real deployment passed.
- **21 API deployment/history/configuration regressions passed** in 2.55 seconds.
  The actual pipeline test also asserts that the complete API suite, verbose IDs
  and diagnostic stack option remain in the gate without test selection/skips.
- The isolated Linux container had no network, Docker socket, production data or
  credentials. Only explicit read-only source/test copies were mounted; it ran as
  UID 1000 with temporary `/tmp` and was removed. Ruff and diff checks pass.
- The current Windows-compatible collection contains **1,704 tests** (0.95 seconds
  to collect), excluding the two Linux-only manager files tested separately. This
  is a suite-size observation, not a full-suite pass or a runtime measurement of
  the failing `be17fcc` release. The user authorized production recovery, but
  HappySnowman access/actual manager installation and target-host gate completion
  are not yet verified. No production restart or cleanup has been claimed.

## Matching-triggered brief admission — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-matching-brief-admission`, based on `653b5cf`.
  Opt-in live/history matching now commits a complete bounded event-ID batch and
  admission outbox with its matching checkpoint. Matching makes no model calls.
- **107 combined tests passed** in 180.27 seconds across automation, measured
  execution, durable brief jobs, topic live/history and ordinary jobs. The one
  SQLite skip is the existing PostgreSQL row-lock test, run separately below.
  Initial new-fixture failure was a missing required detected date, corrected
  without weakening a production constraint. Final rerun includes all assertions.
- Actual service tests cover matching → outbox → measured admission → generation
  → current feed reader; replayed batches reuse the one generation. Coverage also
  includes rollback, complete history traversal, disabled/revoked approval/policy,
  foreign scope, corrupt payload, allowance deferral, per-event limitations,
  four duplicate deliveries, cancellation during measurement, same/new worker
  lease replacement, crash recovery and partial-batch cancel/retry. Failed saved
  output remains failed and does not cause automatic new generation.
- **Seven PostgreSQL 17.11 scenarios passed**: roundtrip, backfill rollback and
  traversal, later events after a limitation, crash recovery, same-owner lease
  replacement, duplicate delivery and dispatcher skip-locked ordering. Only a
  dedicated loopback port 55524/tmpfs QA database was reset; its container was
  removed after verification. Ruff and whitespace checks passed.
- Model/runtime/approval are synthetic; dispatch uses callbacks, not a live
  Redis/Celery broker. This is not real model approval, performance/fairness
  certification or production activation. No inference endpoint, production
  database, user notification or Docker working volume was changed. Automatic
  policy remains off by default and HL-089 remains in progress.

## Exact-current saved brief reader — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-current-brief-reader`, based on `bbca13c`.
  Added organization-authorized, exact-current saved-brief reads and a collapsed
  full-width feed panel. No history means no runtime probe; saved results use one
  bounded local metadata observation and never tokenize, generate or enqueue.
- **70 combined API regressions passed** in 101.02 seconds. The final expanded
  reader suite has **20 passing tests** in 30.27 seconds: source/profile/runtime/
  approval/locale/cloud changes, malformed provenance and citations, revoked and
  foreign event access, pending/failed states, HTTP validation and exact native/
  legacy evidence routing. Existing generation remains one call after rereading;
  historical records and job counts are preserved.
- **Four PostgreSQL 17.11 scenarios passed** using the actual service: current,
  stale profile, corrupt citation and revoked access. Only an isolated loopback
  port 55524, tmpfs database was used; its verified QA container was removed.
  Runtime metadata, approval and generation fixtures were synthetic throughout.
- Typecheck and production build passed. `npm run check:interest-brief:browser`
  passed ten real production-UI journeys at 390/1440 pixels across all five
  locales. Opening is on demand; refresh preserves page identity and hides old
  text on loading/failure/staleness. Exact evidence links and every non-current
  state are checked. Mobile/desktop screenshots were visually inspected.
- **20 full-document axe checkpoints passed** all reported violation severities
  and the unresolved prohibited-ARIA gate. Other incomplete checks remain in
  `test-results/accessibility/interest-brief.json`; this is not accessibility or
  native-speaker certification. All API traffic was intercepted. The fixture
  explicitly permits the existing Marvin context/conversation bootstrap POSTs
  with synthetic failures; the brief reader itself issues no writes/inference.
- Ruff/diff checks passed. No production rollout, real notifications, model
  approval or real inference occurred. Automatic enrichment, broader historical/
  delivery reuse, large-dossier planning and independent usefulness/language/
  capacity evidence remain open; HL-089 is not marked complete.

## Durable shared brief execution — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-durable-brief-jobs`, based on fresh `be17fcc`.
  Measured local admission now persists assessment/job/outbox together; successful
  inputs reuse, simultaneous admissions coalesce and quotas roll back rejected
  reservations. Jobs retain only IDs/fingerprint/locale, not copied source text.
- The real worker service routes brief jobs through a guarded runner. Tests cover
  cancellation before and during inference, four duplicate deliveries, same/different
  worker lease replacement, changed sources, invalid payload binding, privileged
  cross-tenant rejection, crash recovery, exact successful reuse and bounded retries.
  Final/cancelled/exhausted jobs close their exact unfinished assessment too.
- Dispatcher now uses job-before-outbox locking, skips locked jobs, preserves
  running leases and respects retry delay even with an older pending message.
  Existing generic job tests pass alongside the new regressions.
- **162 combined tests passed** in 192.60 seconds. After final terminal-state
  synchronization, **34 targeted tests passed** in 65.62 seconds. One PostgreSQL-only
  test is explicitly skipped in SQLite and verified on PostgreSQL, not waived.
- **Nine final PostgreSQL 17.11 suites passed**: service execution/outbox roundtrip,
  crash recovery, dispatch/backoff, lease replacement, supersession, concurrent
  admission, four separate connections, locked-dispatch ordering and exhausted
  recovery. Containers used only port 55523 on loopback and tmpfs test data, and
  were removed. No existing DB was migrated or reset. Ruff/diff checks passed.
- Actual database and ModelClient code use synthetic approvals/runtime/replies;
  sender callbacks test dispatch without sending to a live Redis/Celery worker.
  No model approval, real inference, notification or production rollout was made.
  This backend slice makes no new browser, language, usability or capacity claim.
- HL-089 remains open: automatic matching policy, exact-current feed/digest readers,
  useful model evaluation, official facts, large dossiers, administrator prompt/
  quota policy and measured fairness/capacity are not completed by this job adapter.

## Native comparison editor — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-native-baseline-ui`, based on `48c4daf`.
  Added organization-authorized candidate/read/save routes and the registry-linked
  `/native-comparison/{event_id}` page. Explicit selection is shared within the
  organization, guarded by revision/CSRF, and read-only for viewers. No model call,
  automatic predecessor choice or legal chronology inference is introduced.
- Complete saved comparisons are audited before displaying paged material/all
  passages. Source access or fingerprint changes withdraw old excerpts. Large
  comparisons remain browsable; the AI planner retains its separate 64-unit bound.
- 92 combined API regressions passed in 105.24 seconds; 13 final targeted cases
  passed in 16.89 seconds after extending language-candidate isolation coverage.
  Four PostgreSQL 17.11 suites passed HTTP roundtrip, large comparison, candidate
  isolation and revoked access. Their labelled loopback/tmpfs container was removed;
  working databases and production were untouched.
- Typecheck, production Next.js build, Ruff and diff checks passed. Ten Chromium
  journeys at 390/1440 px across DE/FR/IT/RM/EN exercised selection, save without
  reload, exact evidence links, pagination, conflict/reload/clear and viewer access.
  Additional stale/empty/read-error recovery checks passed. Twenty full-document
  axe checkpoints reported no violations; incomplete color-contrast observations
  remain recorded, so this is not accessibility certification or native review.
- Reviewed the final mobile screenshot. Browser/API inputs are synthetic fixtures,
  not real-user research, model evaluation or verified legal provenance. No
  production deployment/migration or notification was performed.
- User-requested deployment history remains separately shipped in `68cd244` on
  `origin/main`. Its 11 API regressions were rerun successfully; this does not
  establish that the new release manager has been installed on HappySnowman.

## Explicit native comparison baselines — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-native-comparison`, based on freshly fetched
  `5d95405`. Added the `d8eaf2395cb6` migration, full saved native comparison
  records and separate organization/event baseline selections. The internal
  boundary checks admission, both version grants, exact work/known-language
  expression, source fingerprints and original identity. It never chooses by
  import time. Revision CAS prevents overwritten edits; cleared selections keep
  a tombstone and old comparisons; unchanged saves preserve the reusable AI key.
- The existing admission/planning/local execution path consumes the selected
  complete diff, sends all material/uncertain evidence and context, stores its
  exact comparison/side bindings and revalidates before publication. A 400-unit
  pair with one change supplies two source units. Oversized material sets are
  explicitly refused for AI, not sampled; the full comparison is still saved.
- **200 combined regressions passed in 204.46 seconds** across native comparison,
  current admission, brief contract/storage, measured local execution and legacy
  material planning. **31 final native tests passed in 39.56 seconds**, including
  the subsequently added caller-rollback regression and final SQLite transaction
  fix. An earlier broad run found a 10-ms deadline test racing request admission;
  tests now separately prove outer cancellation and an already-expired request
  budget. Production budgets were not weakened. Only the existing Starlette/httpx
  deprecation warning remains.
- **Eight PostgreSQL 17.11 scenarios passed on final code:** `native-complete`,
  `native-selection`, `native-concurrency`, `native-migration`, `native-scope`,
  `native-gateway`, `native-fence`, `native-rollback`. Each used a fresh empty
  `hl089_regression` DB through `scripts/check_interest_assessment_postgres.py` in
  a labelled, loopback-only port-55521, tmpfs QA container. Both scratch containers
  from this task were removed afterward; working containers were untouched.
- The rollback test first exposed sqlite3's legacy SAVEPOINT/RELEASE implicit
  commit. The implementation now begins a real outer write transaction before
  its savepoint, so caller rollback removes both selection and comparison. The
  complete diff is computed before acquiring that write transaction. Concurrent
  selection and rollback behavior were verified on both database engines.
- Ruff and `git diff --check` pass. The existing standalone PostgreSQL script
  retains only its pre-existing `E402` bootstrap exception during Ruff checking.
  No secrets, working database changes, external model calls, messages or
  production deployment. Synthetic gateway responses/review artifacts prove
  execution and fencing, not model/legal quality, translation or GPU latency.
- This remains an internal integration: baseline editor/actor-authorized routes,
  verified connector predecessor policy, official facts, large-set aggregation,
  durable scheduling and reader/delivery integration remain open under HL-089.
  No automatic native baseline, user-visible feature availability or full backlog
  completion is claimed.

## Complete saved material comparisons for event briefs — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-material-dossier`, based on fresh `1284486`.
  Current-input admission can now use a complete persisted legacy comparison
  instead of all unchanged source passages. It checks every saved side/position,
  exact passage records, IDs, counts, algorithm and material flags; includes every
  material/uncertain change plus exact enclosing hierarchy context; and preserves
  before/after labels, full audit counts and diff identity in the v3 contract.
  The real measured local runner consumes this dossier without another AI stage.
- **190 combined regressions passed in 155.27 seconds** across
  `test_interest_material.py`, `test_interest_admission.py`,
  `test_interest_assessment.py`, `test_interest_assessment_store.py`,
  `test_interest_execution.py` and `test_diffing.py`. After adding the stale-mode
  selection regression, **34 final material scenarios passed in 32.63 seconds**.
  These exercise inserted-article renumbering, real line wraps, numbers and
  removals/additions, exact ancestor context, 400-passage versions, full coverage
  checks, malformed/stale input, no sampling on overflow, baseline ambiguity,
  Version/Law ownership, known-language conflicts, assignment mismatch, side and
  reference validation, stale completion, and actual ModelClient request budgeting.
- **Five final PostgreSQL 17.11 scenarios passed** through the disposable runner:
  `material-reuse`, `material-fence`, `material-baseline`, `material-gateway`,
  `material-existing-mode`. Each used a freshly recreated empty DB in this task's
  labelled tmpfs container, bound to a random localhost port. No working database
  or production data was used. The QA container was stopped after verification.
- In the 400-passage gateway scenario, one modified passage became two exact
  before/after evidence units in a single generation; all 800 source passages
  remained covered by the stored comparison audit. This is a structural/payload
  result using a synthetic HTTP model reply and approval, not real token counts,
  semantic quality, real model latency or a target-GPU benchmark.
- Review found imported legacy baselines need not have a native corpus mirror.
  Admission now applies existing Version/Law ownership and same-law binding to
  those records without writing mirrors during reads. Any existing native mirror
  must agree with the source expression. Another review found an obsolete mode
  could hide a complete comparison for the same pair; admission now tries the
  saved records for that exact pair, with a regression on SQLite and PostgreSQL.
  It never chooses among different baselines by timestamp or rewrites a stale diff.
- Ruff and whitespace checks pass. The PostgreSQL runner retains its established
  import bootstrap (E402 excluded for that standalone script). The existing
  Starlette/httpx deprecation warning remains. Logs are ignored under
  `test-results/interest-material-*`. No UI/build/browser, original-file byte
  verification, production migration/deployment, real inference or delivery claims.
- HL-089 remains **IN PROGRESS**. Native-only persisted comparison/baseline
  binding, full large material/target-law aggregation, official source fact binding,
  durable queue/fairness/recovery, exact-current readers/delivery integration and
  independent quality/hardware reviews remain open. Existing deterministic v6
  classification is not proof that a moved/renumbered provision has no legal effect.

## Measured local organization-brief execution — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-local-execution`, based on fresh `da9203f`.
  The internal local runner authorizes current event access, binds an independently
  approved `interest_brief` task/locale runtime, measures the complete wire request
  before reservation, and uses one shared two-generation/120-second async budget.
  It revalidates actual runtime, approval and saved inputs before fenced publication.
  Exact completed/running inputs do not regenerate; cancellation/failure closes
  the owned attempt. Successful storage records allowlisted token/runtime proof.
- **261 combined regressions passed in 201.69 seconds** across
  `test_interest_execution.py`, `test_interest_admission.py`,
  `test_interest_assessment.py`, `test_interest_assessment_store.py`,
  `test_ai_capabilities.py`, `test_analysis_runtime_binding.py`,
  `test_prompt_token_measurements.py` and `test_capability_execution.py`.
  After adding concurrency and identical-restart reuse coverage, the final
  **33 local-execution scenarios passed in 47.65 seconds**. They use actual
  ModelClient HTTP validation with MockTransport, real DB transactions and
  explicitly synthetic runtime/review records; no real model was approved.
- New scenarios cover exact admission/generation wire equality; physical and
  reviewed context overflow; malformed measurements; task/locale/identity refusal;
  no cloud fallback; one JSON/citation repair; a shared transport retry ceiling;
  input changes before reservation and during inference; revoked approval;
  changed model/configuration; cancellation/timeout; no DB connection held across
  HTTP; bounded execution metadata; concurrent-call coalescing and context isolation;
  and reuse after a restart of the same immutable runtime identity.
- **Five PostgreSQL 17.11 scenarios passed:** `execution-reuse`,
  `execution-transactions`, `execution-fence`, `execution-repair`,
  `execution-cancel`. Each used a fresh empty database in the labelled loopback-only
  tmpfs QA container. No working or production database was changed. The temporary
  container was stopped after verification. Logs are ignored under
  `test-results/interest-execution-*`.
- One early negative fixture changed the nested artifact identity without its
  matching runtime descriptor; it correctly failed as an invalid runtime rather
  than an unapproved new runtime. The fixture now supplies a consistent changed
  descriptor to exercise the intended approval test. No acceptance guard was loosened.
- Ruff and whitespace checks pass. The standalone PostgreSQL runner retains its
  explicit repository import bootstrap (Ruff E402 excluded for that script only).
  Pytest reports the existing Starlette/httpx deprecation warning.
- HL-089 remains **IN PROGRESS**: no public route, automatic queue/matching trigger,
  reader/delivery integration, complete material-change planner, official-fact
  binder, fair per-organization admission, hard-crash lease recovery or real-model
  quality/hardware approval is claimed. Synchronous DB work is not covered by a
  measured end-to-end latency SLA. No production deployment or notifications.

## Current organization inputs for shared AI briefs — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-current-dossier`, based on fresh `68cd244`.
  Added complete current topic/law/direct-watch assembly independently of UI
  pagination and worker `prepare_current`/`finish_current` methods. Native/legacy
  source ownership, work/language identity, current matching signals, profile,
  model/prompt and exact evidence fingerprints are rechecked before reservation
  and completion. The character envelope and cloud approval are checked before
  queued storage. The v2 result preserves server-bound event type and input
  limitations. [Contract and remaining integration](INTEREST_ASSESSMENTS.md).
- **126 final API regressions passed in 111.16 seconds:**
  `test_interest_admission.py`, `test_interest_assessment.py`,
  `test_interest_assessment_store.py`, `test_interest_feed.py` and
  `test_organization_isolation.py`. The **50 new scenarios** cover five locales,
  seven interests with twelve full passages, 65-interest refusal without sampling,
  traversal past 105 stale candidates, exact native/legacy evidence, missing or
  malformed sources, cross-work/language/tenant rejection, expired/rejected/muted
  matches, profile and runtime changes, stale completion, prompt changes, a new
  interest during inference, budget/cloud refusal before persistence, empty
  profile importance, and corrected metadata withdrawing law relevance. Model
  output is a deterministic double, not semantic or language-quality evidence.
- **Eight PostgreSQL 17.11 scenarios passed** through the extended disposable
  runner: `current-reuse`, `current-law-watch`, `current-scope`, `current-sparse`,
  `current-overflow`, `current-refresh`, `current-fence` and `current-rescore`.
  Each used a fresh empty database in the explicitly labelled tmpfs QA container,
  bound to a random loopback port. The runner refuses non-test or populated
  databases. The container was stopped and its removal verified after testing.
- Review found that a current rule label alone could retain stale law retrieval
  signals. Admission now recomputes the established deterministic score/reasons
  from current source/target facts without rewriting candidate history; a new
  regression and PostgreSQL scenario prove withdrawal. An initial fixture assumed
  the topic revision object was under `current_revision`; the test now reads its
  actual `plan.id` contract. No production validation or test gate was disabled.
- Ruff and whitespace checks pass. Logs are ignored under
  `test-results/interest-admission-*`, with the final PostgreSQL run in
  `interest-admission-postgres-verified.log`. No UI changes, browser/build claims,
  schema migration, working Docker rebuild, actual inference, message delivery or
  production deployment occurred.
- HL-089 remains **IN PROGRESS**. This internal path includes all saved passages
  only when they fit; it is not a material-diff planner, tokenizer measurement,
  verified original-file byte read, globally frozen database snapshot or a
  100-user capacity result. Official status/date binding, large-document planning,
  actual runtime approval/token budgeting, durable scheduling and guarded retry,
  exact-current reader/delivery integration and independent evaluation remain
  required. Nothing starts automatically from matching or page reads yet.

## Complete deployment attempt history — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-102-deployment-history`, based on fresh `1e80667`.
  The host release manager checkpoints every attempt into a persistent SQLite
  journal independently of application PostgreSQL. The platform-admin page now
  has outcome filters, cursor pages and expandable exact-run details: requested
  versus verified activated revisions, saved commit summaries, timing, phases,
  sanitized errors and rollback outcomes. The latest report and commit list start
  collapsed. [Contract and retention limits](DEPLOYMENT_HISTORY.md).
- **27 API/access/configuration tests passed in 11.27 seconds** across
  `test_deployment_history.py`, `test_deployments.py`,
  `test_organization_access.py` and `test_production_deployment.py`. These cover
  equal-time cursor traversal, new-record boundaries, filter/archive identity,
  exact details, legacy fallback, quoted-secret redaction, malformed records and
  anonymous/organization-admin/platform-admin access. No inference is involved.
- **14 Linux release-manager tests passed in 1.26 seconds**, using the actual
  writer and pipeline with external commands replaced. They verify 81 records
  survive a three-entry legacy window, immutable terminal records, checkpoints,
  success, test/health failures, failed rollback, interrupted runs and rejected
  ancestry. The disposable container used a small read-only QA snapshot and no
  network, credentials or working deployment data; it was removed after testing.
  Windows cannot import the manager's Linux `fcntl` dependency, so no shim or
  platform check bypass was used to claim these tests passed on Windows.
- **Final production Next.js build passed**, including localization, existing
  shell/resource/report checks and TypeScript compilation. **Ten final browser
  journeys passed** (five locales at 390/1440 pixels), including the collapsed
  latest report, exact date/time, expandable commits, saved notes, failure-detail
  retry, filters/pages, real pointer access, minimum 44px buttons and non-admin
  boundaries. All API traffic was intercepted; no deployment write was made.
  The full-document axe run reports **20 checkpoints, zero violations and 20
  incomplete results** retained for manual review. It is not accessibility or
  native-language certification; the full application suite was not rerun.
  The final mobile screenshot was visually inspected for readable stacked detail.
- Initial browser checks exposed scroll-region semantics and small controls,
  corrected before the final build. A synthetic assistant endpoint returning an
  invalid successful response was replaced with an explicit offline response;
  existing assistant context requests remain intercepted, not hidden as real
  inference. Ruff and whitespace checks pass. Ignored evidence is under
  `test-results/deployment-history-*` and `test-results/accessibility/`.
- No production rollout, working Docker rebuild, database migration or production
  log recovery occurred. Historical records already discarded by the old rolling
  limit cannot be restored; manual deployments outside the release manager are
  not automatically captured. Notes are labelled saved commit summaries, with
  the existing 50-commit limit disclosed and exact comparison links when known.
  The manager update needs the existing authorized installation/release process.

## Shared interest brief contract and storage — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-089-interest-assessments`, based on fresh `a4aac7f`.
  Added the typed internal event dossier/brief engine, scoped assessment and
  normalized reference tables, unique-input reservation, fenced completion and
  bounded explicit retries. The engine permits low importance/no action, rejects
  incomplete interest coverage or invalid citations, takes dates/status from saved
  facts and has a shared two-call/120-second limit with one JSON repair. No source
  passages, private history or raw provider errors are persisted in the manifest.
  [Implementation boundary and remaining HL-089 work](INTEREST_ASSESSMENTS.md).
- **104 combined tests passed in 90.67 seconds:** `test_interest_assessment.py`,
  `test_interest_assessment_store.py`, `test_organization_isolation.py`,
  `test_relation_analysis.py`, `test_interest_feed.py`, `test_digest_topics.py`.
  The 52 new tests include all-five-locale instructions and four event kinds,
  seven-interest completeness, low/unknown-profile importance, malformed/extra
  fields, missing/duplicate interests, cross-interest/nonexistent citations,
  forged profile/date/status, one repair only, no calls on overflow/unapproved
  cloud, timeout/cancellation, caller mutation, immutable successes, stale-worker
  rejection, private tenant policy and six-way transaction races. Test doubles
  are not evidence of real-model reasoning or native-language correctness.
- **Seven PostgreSQL 17.11 scenarios passed** via
  `scripts/check_interest_assessment_postgres.py`: reuse, superseded, retry,
  scope, concurrency, validation and migration. The runner refuses populated or
  non-local/non-test databases. The populated migration roundtrip preserves
  existing corpus/topic records; new table columns, unique input and reference
  indexes match models. Scratch startup applies head `d7d9f1284ba5`.
- Ruff and `git diff --check` pass. Per-suite ignored logs are under
  `test-results/interest-assessment-*`. The database was a labelled, loopback-only,
  tmpfs QA container, not a working service. No production deployment/database
  changes, real inference, email or UI changes. Complete current-input assembly,
  queue/admission/runtime wiring and reader presentation remain open; this is not
  a claim that HL-089 is available to users. User-requested deployment history
  HL-102 is now the explicit next task after publishing this foundation.

## Saved topics and direct watches in digests — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-079-topic-digest`, based on fresh `9a57d0c`. Preview, queued preparation and final delivery now use the current-interest reader with existing source/period/personal-state filters. Saved topic confidence is not promoted into impact severity. Summaries group one event across law/topic/direct-watch interests, disclose five-interest limits and link to exact current event/review evidence. Five-language text/HTML/web presentation and the full-list destination now include Today. Old projection checkpoints restart, while historical summaries remain compatible. [Contract and remaining HL-079 work](DIGESTS.md#current-topic-and-direct-watch-interests-hl-079-8-september-2026).
- **194 final API tests passed in 234.28 seconds** across the new topic digest scenarios, existing digest delivery/preview/resume/quiet-hour/period/schedule/coverage tests, interest feed, and relation configuration/prompt/version freshness. The **20 focused new scenarios also passed separately in 29.15 seconds**: topic-only and mixed law/topic delivery, direct active/paused watches, five-language escaped rendering, capped topic fan-out, unknown versus high impact filtering, independent personal state, sparse 50+6 keyset traversal, privileged cross-organization denial, legacy checkpoint restart, and revalidation of paused/archived/revised/changed/expired/rejected/muted/revoked/dismissed evidence before sending. An exact match-time/cutoff test separately proves that equality waits for the next snapshot. All mail is intercepted or development-only and model calls remain absent for topic-only delivery.
- **Six PostgreSQL 17.11 scenarios passed** (`digest-topics`, `digest-topics-mixed`, `digest-topics-pages`, `digest-topics-filters`, `digest-topics-evidence`, `digest-topics-revoked`) in fresh per-case scratch databases. The task-only container used temporary storage and a random loopback port, then was stopped/removed by its verified ID and task label. No working database, credentials, saved application document or production service was used. These are correctness checks, not target-host or 100-user capacity evidence.
- **Final production build passed:** 1,915 main catalogue keys and 1,622 literal calls, existing shell/resource/report checks, TypeScript and Next compilation. Four unused legacy digest strings per locale were replaced by typed interest-specific copy. **Ten production-browser journeys passed** (DE/FR/IT/RM/EN × 390/1440px): actual saved topic terms, review/event URLs, 44px links, sparse/back/retry behavior, preserved unsaved preferences, local time/quiet-hour controls and no send/inference request. The new full-document axe checks report **10 checkpoints, zero violations and 10 incomplete rule results** retained for manual review. The full application accessibility chain was not rerun. The final 390px topic-card screenshot was visually inspected for wrapping/readability; independent language, mobile-device and assistive-technology validation remains open.
- Initial verification exposed outdated test assumptions: the mail double lacked its `message_id` argument; a retry probe instrumented the old reader; a synthetic unanalysed lead correctly had no confirmed relation citation; a reused direct-watch fixture ended with its watch paused. Tests now assert those actual contracts. An immediate saved-match fixture intermittently fell outside the strict preview cutoff; historical fixture timestamps are now explicitly before the read, with equality tested independently, rather than loosening admission or evidence checks. No production access rule, validation rule or accessibility severity was disabled.
- Ruff and whitespace checks pass. No new schema migration, real mail, provider inference, deployment or application-data rewrite. HL-079 remains **IN PROGRESS** for topic/law/pack/type preference selection, organization policy, independent notification-noise evidence and any immediate option. Per-event validation/fan-out costs and intended-host capacity remain HL-099 work; a bounded event-key page is not claimed to bound all database work.

## Header notification reading centre — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-078-notification-centre`, based on fresh `2c06970`. The global header opens an unread interest-feed dialog, five candidates per request, reusing persisted event grouping and personal read/dismiss state. Links open exact event context without inference. It preserves visible records on page/save errors, supports retry and sparse continuation, and resets local state with user/organization/locale. Polling is configured at 60 seconds through the existing visibility/online-aware resource store, with no mounted feed subscription while closed. A one-second cache freshness window avoids an immediate duplicate read after imperative page navigation. [Contract and remaining HL-078 work](NOTIFICATIONS.md).
- **33 existing API regressions passed in 41.86 seconds** (`test_interest_feed.py`, `test_feed_evidence.py`, `test_auth.py`): one event for multiple interests, topic-only and direct-watch eligibility, shared persisted read state, stale/rejected evidence, sparse pages, scoped cursors, private/foreign denial and exact saved evidence. This frontend task changes no backend schema or authorization. No new PostgreSQL or live cross-device claim is made.
- **Final production build passed**, including the existing localization, shell/resource/report contracts and TypeScript checks. **20 new production-browser journeys passed** across all five locales, 390/1440px and viewer/organization-admin roles: unopened centre makes no feed read, initial/next-page failure and retained-record retry, exact event URLs, read once, failed then explicit dismissal, state retained after reopening, empty intermediate batch with later continuation, latest/back controls, 44px targets, wrapping, keyboard containment, Escape/return focus and focus after successful state changes. All application APIs were intercepted. No actual provider call, email, organization switch or application data write occurred in the browser harness.
- `npm run check:notifications:browser` reports **80 full-document axe checkpoints, zero violations, 120 unresolved incomplete rule results** retained in `test-results/accessibility/notifications.json`; those incomplete results require manual review, not certification. Added this suite to the mandatory `check:accessibility` chain, but did not rerun or claim the entire chain in this task. **30 existing shell browser journeys also passed on the final build**, covering localized role-filtered mobile navigation, focus/skip-link behavior and overlay cleanup. The five new copy records are typed but have not had independent native-speaker review.
- The first axe pass found the old overview workflow hint at 3.79:1 contrast. It now uses the shared foreground color; no axe rule was excluded. A later test briefly matched the previous document between same-language user navigations; the harness now waits for a new-document marker and reacquires a reachable control after the auth gate's transient unmount before clicking. Visual inspection of the 390px screenshot found mobile navigation and Marvin above the dialog: the final scoped overlay rule lowers navigation and hides Marvin while notifications are open, with explicit browser assertions. Final screenshots in `test-results/notifications/390.png` and `1440.png` were captured; the mobile screenshot was inspected after the correction. Personal resource keys now explicitly include user and organization IDs rather than relying solely on normal sign-out/switch cache resets. The final build and both affected browser suites were rerun.
- Whitespace checks pass. No production deployment, local Docker rebuild, migration, evidence rewrite or external message. HL-078 is **IN PROGRESS**, not complete: the count is labelled unread on this page, not an exact durable global badge; broader filters/organization limits, channel/quiet-hours policy, operational notices and independent notification-noise/user evidence remain open.

## Deep action-history cursor index — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-action-history-index`, based on fresh `283af88`. Migration `d6c8e0173a94` and ORM metadata add the same six-column organization/analysis/comparison/action/time/ID index. The reader now uses a lexicographic time/ID predicate, preserving the existing cursor format, page order, access checks and exact totals. [Contract and deployment caveats](ACTION_HISTORY.md#deep-page-index--8-september-2026).
- **61 API regressions passed in 57.85 seconds**: `test_action_history_index.py`, `test_action_history_pages.py`, `test_analysis.py`. New tests cover populated index downgrade/upgrade with byte-equivalent continuation responses and deep pages across 2,001 decisions in both tied and distributed time arrangements. Existing complete-history, current/legacy parity, deletion/new-entry boundaries and foreign-parent isolation remain covered. The migration graph has one head, `d6c8e0173a94`; scratch application startup applies that head.
- **Nine PostgreSQL 17.11 scenarios passed** through the disposable-database runner: `action-seek-ties`, `action-seek`, `action-index`, `action-pages`, `action-boundary`, and decision/analysis/comparison/law scope cases. Both seek scenarios insert 100,001 synthetic decisions and read past 95,000 newer records. They capture the actual HTTP-emitted page SQL, execute `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` with the index absent and present, and leave optimizer strategies enabled. Exact total counting is a separate query and is explicitly not a bounded-work claim.
- In the equal-time scenario the baseline used a parallel sequential scan of the decision table; after indexing it used `ix_action_decision_scope_cursor` backward with the time/ID tuple inside `Index Cond`, **21 actual payload rows, one loop, no discarded rows**, and eight shared hit/read blocks. Observed page-selection execution time was **41.893 ms without the index versus 0.274 ms with it**. These single local runs are not p95 or end-to-end latency evidence. With distributed times PostgreSQL legitimately preferred its existing timestamp index plus an incremental sort: **26 payload rows and five discarded tie-boundary rows**, with observed 0.223/0.275 ms before/after. No speedup is claimed for that distribution.
- The initial plan assertion incorrectly required the new index for every distribution. It failed on an already efficient 26-row timestamp-index plan. The final test checks bounded examined rows for both distributions, requires composite tuple index use for the 100k equal-time case, and retains the default optimizer. No planner flags or index disabling are used to manufacture a favorable plan. Full plan observations are in `test-results/action-history-index-postgres.log`.
- Ruff and whitespace checks pass. The labelled loopback-only scratch container was removed; no local application or production database was migrated. No web changes/build/browser claims, real inference, evidence rewrites or messages. Ordinary index creation can block writers and consume disk on deployment; it requires the existing maintenance procedure. Exact counts, current-summary ranking, other data distributions, response-size bounds and target-host concurrent capacity remain open under HL-099.

## Complete action decision history pages — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-action-history-pages`, based on fresh `ac2dfbf`. Comparison reads opt into current decisions/counts plus complete per-action keyset pages; older clients retain full history. Each page explicitly checks organization, report, exact comparison and visible law parents. Current decisions and pages agree for tied creation times. [Contract and remaining limits](ACTION_HISTORY.md).
- **62 affected API tests passed in 56.37 seconds**: `test_action_history_pages.py`, `test_analysis.py`, `test_auth.py`. Ten new parametrized cases traverse all 151 tied-time decisions in bounded pages without ActionDecision ORM hydration, preserve legacy current/history parity and first-page return, exclude new decisions until refresh, survive a deleted boundary, reject cursor scope/page-size changes, and reject foreign decision/analysis/comparison/law state even in privileged sessions. Reads make no additional model calls. Existing auth regressions pass; no new real-browser authentication claim is made.
- **Six PostgreSQL 17.11 scenarios passed on the final backend code** via `scripts/check_inbox_history_postgres.py --suite action-pages|action-boundary|action-decision|action-analysis|action-comparison|action-law` (one suite per invocation). Each ran against a fresh synthetic `hl099_regression` database in a labelled disposable loopback-only container. That container was stopped/removed; neither local application data nor production was used. The unchanged Starlette TestClient deprecation warning is non-failing.
- **Production build passed**. `node scripts/check-comparison-browser.mjs` passed with **215 full-document axe checkpoints, zero reported violations and 143 unresolved incomplete rule results** retained in `test-results/accessibility/comparison.json`. This is not accessibility certification. Twenty new history journeys cover five locales, 390/1440px, administrator/viewer rendering, 20/11 records, previous/next/latest, injected 503 followed by retry, retained content, focused page status, no horizontal overflow, at least 44px paging targets and 14px history text. Existing comparison/Marvin/evidence/legacy-report scenarios remain in the suite. All API calls were intercepted; no real inference or application mutations occurred.
- The first tied-time API test exposed inconsistent legacy ordering; adding the ID tie-break fixed it. The first new axe checkpoint exposed a named action-button div without a role; it is now a named group. An initial browser assertion counted the comparison mount's two expected Marvin initialization POSTs as history writes; the final assertion explicitly allows exactly those context/conversation requests while rejecting any other mutation or inference request. A harness quoting typo was corrected before the successful runs. No gate was removed. Visual review of `test-results/comparison-overlays/action-history-mobile.png` prompted replacing inherited 9px dense history with 14px separated records; build and the whole comparison browser suite were rerun after that CSS change.
- Ruff and whitespace checks pass. No schema/index migration, saved evidence rewrite, provider call, outgoing message or production deployment occurred. HL-099 remains **IN PROGRESS**: distinct-action fan-out, legacy full-history readers, selected report/diff bytes, SQL ranking/count/index cost and target-host capacity are still open. Native-speaker, real-user, physical-device and assistive-technology review remain separate gates.

## Document summary metadata — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-document-summary-reads`, based on fresh `d6429a9`. The current version now uses scoped SQL metadata and statistics. Comparison summaries select only ID/mode/counts; the full diff loads only after finding an accessible saved AI attempt that needs a freshness check. Valid scan-linked comparisons retain priority, with deterministic scan/comparison ties and explicit scan/item organization and exact-law constraints. No-report document responses hydrate neither Version nor Comparison bodies. [Contract and open limits](DOCUMENT_HISTORY.md#document-summary-metadata--hl-099-8-september-2026).
- **72 affected API tests passed in 131.84 seconds**, covering the nine new summary cases, law-history metadata, complete document-history pages, saved-report selection, runtime cache and AI history. **66 further workflow/organization-isolation/analysis tests passed in 75.07 seconds**. Assertions preserve exact current-version metadata and evidence links, current/stale report identity, history page contents and scan behavior. The existing 152-version large-history test now requires zero Version/Comparison body loads, strengthening its earlier allowance for current bodies. A saved-report summary still loads exactly its selected comparison for cache validity, with no extra model/fetch calls.
- **Ten PostgreSQL 17.11 scenarios passed on the same code**, including summary reports, scan ordering, foreign scan/item/comparison rejection, wrong-law links, three invalid current-version references and large history. Each used a fresh synthetic `hl099_regression` database inside a labelled disposable loopback-only container. That container was stopped/removed; the running local application database and stack were not modified.
- An initial new-test invocation failed because a synthetic second URL had not been registered in the fake fetcher. Added that deterministic fixture source; no external network/provider fallback was used. Ruff and whitespace checks pass. The Starlette TestClient warning is unchanged/non-failing. No frontend/build/browser or independent quality claim is made for these backend changes.
- HL-099 remains **IN PROGRESS**: SQL JSON-statistics cost, selected diff/result sizes, compatibility law-list query multiplication, complete action-decision history and intended-host capacity remain open. No migration, evidence rewrite, actual inference, production deployment or outgoing message occurred.

## Impact-matrix saved-history batches — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-matrix-history-reads`, based on freshly fetched `e5240d1`. Replaced full comparison/analysis-history materialization with SQL-ranked latest comparisons and current/previous report selection in 50-law batches. The existing response, impact ordering, totals, exact comparison links and unknown/stale/failed states remain compatible. Explicit organization/profile/watch/law/comparison/analysis constraints apply in privileged sessions too. [Contract and remaining bounds](IMPACT_MATRIX.md#saved-history-reads--hl-099-8-september-2026). HL-099 remains **IN PROGRESS**.
- **53 API tests passed in 96.07 seconds on the final implementation**, across the new ten matrix selection cases, existing matrix, runtime-cache, current-report selection and AI-history suites. Large-history coverage uses 151 archived comparisons and 1,001 later failed attempts; only the chosen comparison body loads and no Analysis ORM objects or plan/provenance columns transfer. The HTTP query totals are nine for one law and thirteen for 51 laws, including three request-configuration reads. These are query-count observations, not constant database cost or latency claims. JSON null/list/scalar/bool results cannot displace a saved object; equal-time comparisons do not borrow old reports; a cache-key correction between metadata selection and payload loading becomes stale. No extra model calls occur.
- **All ten final PostgreSQL 17.11 scratch scenarios passed**: history, JSON shapes, 51-document batches, latest-comparison ties, cache correction, and watch/law/comparison/analysis/profile scope. A separately labelled loopback-only `postgres:17-alpine` container used a new synthetic `hl099_regression` database per case; only that disposable container was stopped/removed afterward. The user's running local stack and its database were not used by these tests.
- Initial regression authoring exposed a zero-row fixture insert attempting to create an invalid Analysis and query assertions omitting the three configuration reads. The fixture now uses a real saved attempt and scope revocation; final assertions include the actual HTTP configuration reads. Earlier 11/44-test and nine-case PostgreSQL runs preceded the final cache-load check and are not counted as final repeats.
- Ruff and whitespace checks pass. The existing Starlette TestClient deprecation warning remains non-failing. No frontend/build/browser rerun or independent language/accessibility certification is claimed for this backend-only change. No migration, production deployment, data rewrite, actual inference or message delivery occurred. Complete matrix pagination, active-watch/response size, selected diff/result byte bounds, SQL ranking/index cost and intended-host concurrency remain open.

## Complete current saved report selection — 8 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-current-analysis-selection`, based on freshly fetched `de5f499`, preserves the preceding HappySnowman integration. Document and comparison reads now select from the complete accessible attempt history, preferring the current successful cache key, then a previous success, then the latest attempt. Three analysis queries materialize only the chosen report body; the separately displayed latest failure remains available. [Contract and open limits](CURRENT_REPORT_SELECTION.md). HL-099 remains **IN PROGRESS**.
- **138 affected API tests passed in 202.98 seconds** across selection, AI history, runtime cache, capability execution, impact matrix, analysis and document-history readers. After the final explicit-UTC timestamp fix and extra empty-history/foreign-current-report regressions, **41 tests passed in 43.33 seconds** across the final eight selection cases and decision-report suite. The earlier 138-test run is not represented as a post-fix repeat. Tests exercise a still-current report behind 1,001 later failures, actual document/comparison HTTP responses, one hydrated Analysis body, unchanged saved result/reuse count, no inference, stale fallback, visibility revocation and newer admissions.
- **All six PostgreSQL 16.14 scenarios passed on the final code:** `saved-analysis-current`, `saved-analysis-fallback`, `saved-analysis-boundary`, `saved-analysis-analysis`, `saved-analysis-comparison`, and `saved-analysis-law`. An initial attempt could not reach Docker's Linux engine; after the user started Docker, the existing runner completed in a fresh labelled `postgres:16.14-alpine3.24` container, exposed only on a random loopback port. Each scenario used a newly created synthetic `hl099_regression` database. Cleanup stopped/removed only that disposable container. No application or production database, real provider credentials or inference were used.
- Ruff and whitespace checks pass. The non-failing Starlette TestClient deprecation warning remains. No frontend files changed and no new browser/build result is claimed. No schema migration, production deployment/restart or user messages occurred. SQL ranking/index cost, other heavy summary reads, action-decision history bounds, independent quality and target-hardware capacity remain open.

## Regulatory-timeline paging and HappySnowman integration — 8 September 2026 (HappyDucky02)

- Recovered the unfinished `codex/HappyDucky02/hl-099-regulatory-timeline-pages` work without replacing its checkout. A fresh fetch found four main commits after `0e7666c`: `94816ef` (stale source-pack streams), `f8f7bec` (Apache-2.0 project artifacts), `54af199` (release note), and `3c62752` (dynamic timeline fixture date). Saved local work in `507d780` and merged current main in `fca1603`, both with `Development-Host: HappyDucky02`. `git cherry` confirms the separate HappySnowman `afe9d4e` source-pack commit is patch-equivalent to main; no duplicate cherry-pick is needed. All remote source-pack code/tests and licensing/package files are retained unchanged.
- The only merge conflict was the timeline seed timestamp. Retained HappySnowman's removal of a fixed calendar date, using the existing UTC clock minus two days so the fixture's next-day observations also precede the new page cutoff. This preserves both the remote regression fix and complete paged coverage rather than choosing an entire file from either side. A comment records the reason. `origin/main` at `3c62752` is an ancestor of the merged task branch.
- The local feature adds complete bounded scalar pages for all five regulatory-timeline lists and a five-language document UI. It preserves exact evidence, aliases, ownership and paused-watch access. [API/UX/cursor contract and remaining work](REGISTRY.md#complete-regulatory-timeline-pages--8-september-2026). HL-099 remains **IN PROGRESS**; response bytes, SQL cost, summary reads and target-host capacity are not claimed complete.
- Before cross-host integration, **67 affected API tests passed in 102.87 seconds** and **ten PostgreSQL 16.14 scratch scenarios passed** (large chronology, relations, private history, hidden law/watch/mapping/work, metadata lists, cursor/access and late admission/deletion). The PostgreSQL run used a labelled disposable loopback-only container and fresh synthetic databases, not application data. No production migration or inference occurred. After merging, **37 affected API tests passed again in 57.29 seconds**, explicitly including HappySnowman's stale source-pack regression and the reconciled dynamic-date timeline test. The earlier broader/PostgreSQL runs are not represented as post-merge repeats. The Starlette TestClient deprecation warning remains non-failing.
- The merged **production build passes** localization, shell, resource/report contracts, TypeScript and Next compilation. The affected production browser suite passes **141 full-document axe checkpoints**, including five locales, 390/1440px, admin/viewer document history, all five complete regulatory lists, keyboard/focus, retained records during loading/error, retry/latest, independent list positions and empty states. Navigation does not reload the document or call mutations/models. **141 other incomplete rule/checkpoint results remain in diagnostics**; zero reported violations and no prohibited-ARIA finding do not constitute independent accessibility or native-language certification. The mobile screenshot was inspected. Other browser suites were not rerun for this integration.
- An earlier browser attempt caught insufficient contrast during list-control state changes. Scoped selected-state colors and removal of the color transition fix the UI; the unchanged accessibility gate passes on the merged rebuild. No rule, severity or assertion was excluded. A mistyped filename in the first wider test invocation was corrected; only the completed 67-test result above is counted. Historical interrupted process handles were missing on recovery; completed log results were inspected instead of restarting an assumed live process.
- This integration preserves the received Apache-2.0 files; it does not independently repeat the old wheel/image licensing probes or retroactively verify the upstream edits to their historical report. No production deploy/restart, production-data change, real model call or message to another person is authorized or performed by this Git integration.

## Personal Marvin history, retention notice and deletion — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-083-private-marvin-history`, based on clean `4d883f0` after fresh origin/main, host alias, worktree and other-host branch checks. Added metadata-only GET pages, owned conversation GET/DELETE, five-language private history UI and explicit deletion confirmation. History never bootstraps the companion or sends model requests; identity changes discard private state. The chat links to visibility/retention/deletion guidance. Matching owned tab drafts and other open same-origin companion contexts are discarded after deletion. Shared Ask records, monitoring topics and saved document evidence remain independent. [Boundaries and remaining work](MARVIN_PRIVACY.md).
- **48 API tests pass in 56.50 seconds**, including 11 new history scenarios/parameter cases plus existing assistant/context regressions. They exercise equal-time keyset ordering, bounded metadata SQL with no private bodies, immutable GET activity time, invalid/scope-changed cursors, viewer/admin ownership, CSRF, anonymous denial when disabled, retained shared Ask/monitor/evidence records and an in-flight response after deletion. **Three PostgreSQL 16.14 scratch-database scenarios pass** for metadata paging, shared preservation and delete/reply racing; a labelled disposable loopback-only container uses synthetic data. No application or production database was used and no migration is added. Existing Starlette TestClient deprecation warnings remain non-failing.
- **Final production build passes** the main 1,919-key localization catalogue, shell/resource/report checks, TypeScript and Next compilation. New history strings use a typed complete DE/FR/IT/RM/EN catalogue; no native-speaker certification is implied. Ruff and whitespace checks pass for changed backend code/tests.
- The **complete final eleven-suite browser chain passes 631 full-document accessibility checkpoints**: topics 30, comparison 155, native evidence 10, inbox 20, onboarding 40, Marvin monitoring 40, capability settings 40, reprocessing 54, document history 83, Marvin lifecycle 68 and private history 91. The new history journeys cover five locales at 390/1440px, viewer/admin, list/older page, read detail, deletion dialog/Escape/error/retry/success, empty/list-error/detail-error and an identity change during a held read. Lifecycle includes a real second same-origin window notifying the companion of deletion. All reported axe violation severities and the prohibited-ARIA gate pass; **523 other incomplete rule/checkpoint results remain in diagnostics**, not accessibility certification. Final screenshots were inspected after shortening the mobile caption, fixing the breadcrumb and folding the privacy panel while reading. All APIs and speech are synthetic; no provider calls or user data are used.
- Final backend review also normalized conversation timestamps to explicit UTC, keeping the SQLite detail view consistent with metadata dates. After that correction, **all 48 API tests pass again in 57.23 seconds**, including the new exact offset assertion. The PostgreSQL cases already exercise the same UTC serializer for metadata; their three runs above preceded this final serializer reuse in detail responses. Scratch services were stopped after testing. No production deployment, production migration, independent privacy/native-language certification or target-hardware capacity claim is made.
- The first keyboard browser attempt omitted Enter's character event and failed to activate the pager; corrected native key events pass. A dialog error-state check initially sampled a disabled-to-enabled opacity transition; the driver now waits for finite UI animations to finish before running the unchanged full-document axe rules. The localization guard caught two literal character-name strings, now translated. An existing source contract was updated to require the authenticated companion mount everywhere except the explicitly read-only history route. No authorization, accessibility severity or assertion was disabled. Screenshot review found a long mobile navigation caption and a misleading default breadcrumb; the final UI uses the short localized character label, retains the full accessible destination name, and folds long retention guidance while reading a conversation.

## Marvin pause, detached context and private-state lifecycle — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-083-marvin-context-lifecycle`, based on `f9a8e80` after checking the explicit host, clean single worktree and fresh main/other-host branches. Fixed the disabled companion's startup context/conversation/runtime calls and job subscription. Explicit pause is separate from muting remarks; a top-bar return control avoids covering page actions. Detachment survives navigation, reload and pause/resume. Context-owned AbortControllers and request generations reject old bootstrap/chat/handoff results, clear pending draft timers and stop old speech. User/organization changes remount private state; job-cache and tab-draft identities include both owners. Unowned legacy tab drafts are not imported or deleted. [Behaviour, ownership and cancellation limits](MARVIN_PRIVACY.md).
- **37 API tests pass in 37.99 seconds**: existing assistant contract and monitoring-assistant-context scenarios, covering authorized personal conversations and typed context. No backend endpoint/schema changes are made here. The existing Starlette TestClient deprecation warning remains non-failing. **Production build passes** 1,919 main catalogue keys, 31 shell checks, 22 resource/delivery checks, 198 report/navigation checks, TypeScript and Next compilation. Three new privacy-control strings are typed for all five locales in the separate companion catalogue; native-speaker quality is not certified.
- The new standalone production browser suite passes **67 full-document accessibility checkpoints** and its surrounding journeys: five locales at 390/1440px, paused entry with zero companion calls, keyboard enable, one bootstrap, no redundant unchanged-draft PATCH, detachment across real client navigation and reload, pause/resume without reattachment, persisted pause, held bootstrap after detachment, held chat after navigation with no old text/speech, real auth-resource identity refresh, organization switch and owner-scoped comparison draft restoration. A replacement user's job response is held while the UI proves it does not reuse the previous job cache. Legacy/other-owner drafts are neither shown nor uploaded. Final review replaced the initial-value draft comparison with edit revisions: the browser verifies saving an edit, saving its subsequent clearing and retaining text typed while history restoration is held. APIs and speech are intercepted synthetic fixtures; no live backend/model or shared monitoring mutation occurs. The inspected 390px screenshot led to moving the initial floating return control into the top bar, where it no longer covers the source-package action.
- The complete ten-suite accessibility chain passed **538 checkpoints** before the last draft-save refinement (the new suite then had 66). After that refinement and the final production rebuild, the two affected suites passed again: **67 lifecycle + 155 comparison checkpoints**, including 40 real Marvin-to-Ask multiline/2,000-character handoffs, slow history, context reset, focus and no-inference behavior. Other final-chain suites retain their verified evidence: topics 30, native evidence 10, inbox 20, onboarding 40, Marvin monitoring 40, capability settings 40, reprocessing 54 and document history 83. The history suite now requires zero disabled-companion initialization rather than allowing its former startup calls. Current diagnostics report zero violations at every severity and no unresolved prohibited-ARIA findings; **421 incomplete checks remain** across the ten files. These are not independent accessibility, native-language, physical-device or screen-reader certification.
- Initial browser fixtures omitted required starter-catalogue metadata; the fixtures were completed without weakening production contracts. The visibility check was corrected to target the actual button rather than its `display: contents` wrapper. New states exposed existing low-contrast behaviour hints and model-unavailable text/settings link; scoped foreground colors now pass the unchanged full-document gates. One stale-auth fixture replaced `Date.now` too late, after the resource store had captured it; the final fixture installs its adjustable clock before the application loads, then exercises the actual auth refresh without a production test hook. An earlier comparison regression attempted to fulfill the now-aborted handoff request. That race accepts only Chromium's exact invalid-interception result, still rejects every other error and asserts that old handoffs never appear. No accessibility rule, severity or interaction assertion is disabled.
- New browser/server instances use isolated temporary profiles and random localhost ports, with cleanup in `finally`. Detailed diagnostics stay under ignored `test-results/`. Browser abort is **not** server-side durable cancellation, history deletion or proof that accepted inference stopped. HL-083–HL-085 remain **IN PROGRESS** for broader intents, retention/deletion UX, explicit record-context selection, durable chat recovery/cancellation, useful-task metrics and independent review. No production deployment/data migration, real model download/inference, source crawl, notification, cross-tab preference-sync guarantee or target-hardware/capacity certification.

## Complete saved document-history pages — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-document-history-pages`, based on `65dba7b`, after checking the explicit host, single worktree, current main and other-host branches. The actual document page now opts into 20-record pages for versions, comparisons and fetch/import observations, with accessible-record totals, fixed-cutoff previous/next/retry/latest navigation and retained old/current version selections. Controls and explanations cover DE/FR/IT/RM/EN. The cutoff includes seconds and the Europe/Zurich zone. Added three history indexes without rewriting saved evidence. [API, UI and explicit limits](DOCUMENT_HISTORY.md).
- **66 final API tests pass in 129.99 seconds:** document-history pages, existing metadata/timeline readers, administration, relation-evidence freshness and authentication. Traversal covers 152 versions, 151 comparisons and 502 observations with stable same-time ordering, all pages, first-page return, exact-cutoff inclusion, boundary deletion and later-save exclusion. Tests reject malformed/oversized/foreign/partially positioned cursors and altered limits; current ownership/watch/membership checks remain effective even for privileged database sessions. A nonempty page performs four history queries plus three existing HTTP service-configuration reads, without historical ORM bodies or artifact storage keys. Populated migration downgrade/upgrade preserves evidence. Removed membership exposed an unhandled `session_invalid` exception; resolving it as an invalid session now returns 401 on protected reads and keeps public session/sign-in routes usable. Reader mutation denial is unchanged.
- **Eight distinct PostgreSQL 16 scenarios pass** on empty case-specific localhost scratch databases: versions, comparisons, observations, detail, deletion, scope, migration and exact cutoff. Seven ran initially; the three traversal cases and cutoff ran again after replacing the first-page sentinel ID with an explicitly unpositioned cursor (11 scenario executions total). Remote/non-scratch and nonempty database guards reject unsafe inputs; the populated refusal leaves its 152 versions present. Both task-only containers used temporary database storage and random localhost ports and were stopped/removed by verified IDs. No working database, stored document or production service was changed. These sequential correctness tests do not establish workload capacity or nonblocking production index creation.
- **Final production build passes:** 1,919 main catalogue keys, 31 shell checks, 22 resource/delivery checks, 198 report/navigation checks, TypeScript and Next compilation. The required nine browser suites passed **472 full-document accessibility checkpoints** (topics 30, comparison 155, native evidence 10, inbox 20, onboarding 40, Marvin 40, capability settings 40, maintenance 54, document history 83). After the final cutoff-time display refinement and rebuild, the affected **83 history checkpoints and surrounding journeys passed again**. That suite covers five locales, administrator/viewer, 390/1440px layouts, keyboard activation/focus, old-version selection retained across pages, current version outside the first page, loading, failed-page retry, exact first-page cursor and no full-page reload or history-triggered mutation. All app APIs are intercepted synthetic fixtures; no backend/model/source or notification calls occur. The regenerated mobile screenshot was inspected: controls wrap within the page and observations retain local horizontal table scrolling for exact evidence/file links.
- Automated accessibility checks report zero violations at every severity and no unresolved prohibited-ARIA findings. **370 incomplete checks remain** in the nine diagnostic JSON files; no native-language, physical-device, screen-reader or independent accessibility certification is claimed. Initial runs found count/disclosure/table/control contrast failures; scoped colors were corrected without disabling rules. The precision assertion initially assumed every locale used a 24-hour clock; Chromium falls back to a 12-hour format for Romansh. The final test accepts either representation of the same exact Swiss time, including seconds. Earlier API fixture errors (future synthetic timestamps, omitted service-configuration reads in query accounting and an incorrect exception attribute) were corrected; the final suite above supersedes them.
- History QA also observed the existing disabled companion initializing context and an empty personal conversation on entry. The history test records and restricts these exact startup requests separately, then rejects any additional mutation during paging; it does not hide them as history behavior or claim entry is write-free. **HL-083 retains this explicit follow-up.** Ruff and whitespace checks pass. Terminal/browser evidence remains under ignored `test-results/`; temporary browser profiles/server processes are cleaned up. **HL-099 remains IN PROGRESS:** regulatory-timeline paging, complete metadata byte bounds, count/query-planner and summary/analysis costs, compatibility full-list consumers and intended-host concurrency remain open. No production migration/deployment, real AI inference, model download, source crawl or messages were performed.

## Administrator reprocessing workflow — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-reprocessing-admin`, based on `208422c`, after checking the explicit host, clean single worktree, current main and unchanged other-host branches. Platform admin now opens a dedicated five-language maintenance page: preview, labelled examples/counts, current-rule apply confirmation, live progress, cancel/resume and recent runs with exact Zurich timestamps. Job links survive reload. The history filter is applied before the 20-row limit, so unrelated recent jobs do not hide maintenance. [Workflow and limits](RELATION_IMPACT_ANALYSIS.md#administrator-reprocessing-page).
- **Incoming-host integration:** fresh pre-push fetch found HappySnowman's `3881aab` main, including `025f07f` (isolated deployment test tooling, self-contained Git evaluation fixture and ORM refresh of trigger-generated evidence revisions). Saved this feature as `3c9a983`, reviewed the incoming changes and preserved both histories in `8dcd6cc` without conflicts or overwriting either task. The combined application suite passes **108 tests in 132.93 seconds**, including reprocessing administration, retained-pair processing, evidence freshness, semantic matching evaluation, jobs and platform administration. Its one SQLite-only cross-worker skip is unchanged; no new PostgreSQL verification is claimed here. The initial combined Windows collection stopped on the deployment manager's Linux-only `fcntl` import. All **six release-manager tests then passed in 0.05 seconds** in a disposable Linux container, without skipping assertions or changing production code: no network, read-only root and three explicit source-file mounts, unprivileged user, temporary `/tmp`, no Docker socket, secrets or application data. Deployment command execution is mocked by these tests; this does not certify a real image build/deployment. Container removal was verified. Incoming commits contain no frontend changes, so the release build and final browser evidence below still cover the integrated frontend. Focused Ruff and whitespace checks also pass after integration.
- **41 API tests pass in 65.40 seconds**, including reprocessing, jobs, administration and the new operator contracts; the one SQLite skip is the previously verified PostgreSQL cross-worker lock. A final **six-test administration rerun passes in 7.90 seconds** after adding explicit cross-organization assertions. Another **24 adjacent relation-analysis/profile/history tests pass in 50.45 seconds**. Tests verify mode/rule/cutoff serialization without exposing the checkpoint, saved document labels, old-rule replay returning the original accepted job, rejection of an unaccepted obsolete request without new work, role revocation, CSRF and filtered history beyond 25 unrelated jobs. A platform administrator with viewer membership can start/cancel/resume only this maintenance operation through its dedicated admin routes: another organization's maintenance and unrelated local jobs remain protected.
- **Production build passes:** 1,919 main catalogue keys, 31 shell tests, 22 resource/delivery tests, **198 report/navigation tests**, TypeScript and Next compilation. The five new helper contracts cover apply admission, malformed/partial/obsolete/no-change results, supersession before a first batch, saved request validation, result routing and complete typed copy in all five locales. They exercise decision logic, not just source-text presence. The additional maintenance catalogue remains separate typed DE/FR/IT/RM/EN copy; it has no native-language quality certification.
- **All eight required browser suites pass 389 full-document accessibility checkpoints:** topics 30, comparison 155, native evidence 10, inbox 20, onboarding 40, Marvin 40, capability settings 40, maintenance 54. After the final narrow admin-action routing refinement, the release build's maintenance suite was rerun and its **54 checkpoints and surrounding interaction assertions pass again**. The real rendered UI is tested with intercepted APIs at 390/1440px in all five languages: passive entry, one preview submission, no page reload, keyboard confirmation/focus, explicit acknowledgement, a lost apply reply, reload and same-UUID/mode/rule recovery, partial counts, cancellation and resume without another job. Additional cases cover exact times, no-change, obsolete, early superseded, malformed-result, unavailable and denied states, plus the link from platform admin. Ordinary organization administrators do not subscribe to maintenance data; the English desktop journey also exercises a platform administrator with viewer membership. Every reported axe violation severity and the unresolved prohibited-ARIA gate pass. **287 incomplete color-contrast checkpoint/rule results remain in saved diagnostics**, not certified accessibility passes. The final mobile screenshot was inspected for readable precise times, contained counts/history and no horizontal overflow.
- The first API fixture used a nonexistent `fetch` queue; it now uses the actual `ingest` queue without changing production queue policy. The initial browser run stopped because the test driver omitted Enter's character event; the corrected native key event sequence passes and retains event diagnostics. Visual review added seconds/time-zone display rather than leaving same-day runs indistinguishable. Final permission review found that generic organization job mutations blocked a platform administrator with viewer membership; dedicated maintenance-only admin routes fix that case without broadening ordinary job permissions. One adjacent-test invocation named a nonexistent resource-contract file and stopped at collection; the corrected three-file run above passed. No assertion, authorization, localization or accessibility gate was suppressed.
- Ruff and `git diff --check` pass. All test processes finished; disposable browser/server processes and checked temporary profiles were cleaned up by their test runners. Logs, accessibility diagnostics and screenshots remain ignored under `test-results/`. No working application/model/Temporal service was restarted; no real provider/source/notification call, production data change, schema migration, model download or deployment occurred. **HL-100 remains IN PROGRESS:** this supplies the operator UI for retained pairs, not new-pair discovery/backfill, historical AI reassessment, catch-up digest delivery, independent semantic/domain/native-language evidence or target-host capacity. Browser session storage can lose its recovery pointer when cleared; the durable job and authorized history remain on the server.

## Retained relation-candidate reprocessing — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-relation-reprocessing`, based on `caa6ab9`. Resumed the uncommitted implementation after checking the explicit host, single worktree, current main and unchanged HappySnowman refs. Added a platform-admin preview/apply API and durable 25-candidate maintenance batches using the same deterministic scoring policy as live discovery. Admission cutoff, cursor, counts and examples persist with candidate changes; retries/cancellation preserve prior committed progress. Old-rule/rejected candidates cannot supply current AI conclusions. Saved reports, reviews, official relations and deliveries remain intact. [Operation and limits](RELATION_IMPACT_ANALYSIS.md#controlled-retained-candidate-reprocessing--6-september-2026).
- **427 affected API tests pass in the broad run (509.45 seconds)**, with two explicit SQLite skips for PostgreSQL cross-worker locks. It covers relation, inbox, digest, jobs, administration, native evidence and offline matching-evaluation suites. The final focused run after the bookkeeping/no-op refinement passes **51 tests in 39.42 seconds**, with its one cross-worker SQLite skip separately passed on PostgreSQL. New cases exercise rule-only freshness before maintenance, preview/apply, invalid checkpoints, concurrent idempotent intent, cancellation/resume, rollback before commit, superseded rules, late admissions/deletions, selected-version refresh, unchanged current-report/cache reuse, operator states and authenticated/CSRF/role-revoked job access.
- **Ten maintenance scenarios pass on isolated PostgreSQL after the final refinement:** unchanged current pair without AI cache invalidation; changed-rule preview/apply; unsupported old lead; 61 candidates in 25/25/11 batches; atomic rollback; rule supersession; late admission/deletion; selected-version refresh; concurrent enqueue with independent worker guards; and preserved operator rejection. Earlier PostgreSQL reader regressions also pass: **50 events/7,474 historical rows use four selection/hydration queries and 111 materialized records**; **10,001 analyses use three queries and two loaded history payloads**. The new batching regression observes at most 25 candidate ORM objects per batch and no hydrated archived version, passage or AI-history payloads. These are functional/query-bound checks, not a target-host load benchmark.
- Initial focused checks passed 46 tests after two earlier suites exposed missing candidate status/rule columns under the existing metadata `raiseload` guard. Only those scalar columns were added; the guard remains enabled. An initial expanded run had 425 passes and two test failures because its operator-state assertions retained an ORM revision from before the database trigger ran; refreshing the stored baseline fixes the assertion and additionally verifies unchanged status/timestamp. One earlier invocation named a nonexistent test file and stopped at collection; corrected runs use the actual evidence-gate suite. Final review also removed a needless bookkeeping-only revision change: the new current-pair test proves both preview and first apply keep a usable report, followed by a zero-generation cached answer. No assertion or safety gate was waived.
- PostgreSQL used only task-labelled temporary `helvetic-hl100-reprocess-pg-20260906` containers, a memory-backed synthetic `hl099_regression` database and localhost:5439. The first instance was removed, then recreated solely to rerun the final no-op/refreshed paths; both exact IDs were verified before cleanup and the final filtered listing is empty. Existing application/model/Temporal services were untouched. Ruff and `git diff --check` pass. The existing FastAPI/TestClient deprecation and Windows line-ending notices are non-failing. Logs remain in ignored `test-results/`; all model/mail fixtures are synthetic or intercepted.
- **HL-100 remains IN PROGRESS.** This increment rechecks retained pairs through an authorized API; it does not provide a dedicated maintenance UI, discover new pairs, automatically reassess historical AI conclusions, deliver catch-up notifications or measure independent semantic/domain quality. Metadata is current per batch, not frozen across the entire run. No web files, schema migration, real provider call, source crawl, model download/promotion, production deployment/data mutation, external message or credential publication occurred. No new browser/accessibility, explanatory-model approval or target-hardware/100-user claim is made.

## Observed local runtime for relation conclusions — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-relation-runtime`, based on `abebe1d`. Checked the explicit host, clean single worktree, fresh main and both unchanged HappySnowman refs. Relation cache/queue/current selection now uses the validated gateway runtime cache identity, replacing inventory-derived fingerprints. Restarting the same immutable model preserves reuse; weights/tokenizer/template/runtime/hardware/context/count-protocol changes invalidate it. Partial identity is bound to one deployment. History text, citations and original generation provenance are retained. [Contract and limits](RELATION_IMPACT_ANALYSIS.md#observed-local-runtime-freshness--6-september-2026).
- **440 affected API tests passed in the broad run (552.25 seconds)**: all relation, inbox, digest and runtime suites plus jobs, analysis and native evidence. After the final offline-request coalescing/row-lock refinement, the new focused suite passed **25 tests in 56.53 seconds**; its one SQLite skip is the cross-worker row-lock scenario, separately **passed on PostgreSQL**. No blanket test skips, assertion weakening, real model calls or mail sends were used. The first 31-test baseline passed. The first new suite passed 16/19; three failures exposed that the synthetic Docker fixture used an endpoint different from the application's derived local gateway. The fixture now uses the same derivation as production. The expanded run exposed a test-only spelling error in `waiting_for_model`; final tests use the actual durable state and exercise automatic resume.
- **Nine isolated PostgreSQL scenarios pass:** immutable-runtime reuse/provenance, same-name tokenizer replacement, offline history and pending-job reuse, checkpoint restart/final digest rejection, organization/configuration observation isolation, worker re-observation before send, concurrent unverified enqueues with independent worker guards, batched inbox history, and large per-candidate history. The final coalescing path was rerun on PostgreSQL, then its cross-worker lock was tested with two concurrent connections/independent guards: both returned one job ID. This is not a production multi-process load benchmark. Existing **50-event/7,474-history-row** bounds remain **four selection/hydration queries and 111 materialized records**; **10,001 analyses** still require **three queries and two loaded history payloads**.
- Real ModelClient tests use intercepted HTTP, synthetic runtime identities and scripted model text. They cover refused wrong-binding responses, stale queued known-runtime jobs failing before generation, unknown-runtime jobs retaining local warm-up without completed-job reuse, one observation per organization fanout batch, missing/legacy bindings, no-inference current reads, probe-free saved citations and immutable history. The digest worker observes outside DB write transactions and observes again before delivery. Changed prepared bindings restart/reject selection; an unavailable local runtime does not advance the recipient's sent period. All mail calls are intercepted. Official urgency/human review remain independent from stale AI conclusions.
- PostgreSQL used only labelled temporary `helvetic-hl100-runtime-pg-20260906` containers, an empty memory-backed `hl099_regression` database on localhost:5439 and synthetic test rows. The container was recreated solely for the final concurrency regression; both task-owned instances were stopped and removal verified. Existing application/Temporal services were not restarted or modified. Ruff and `git diff --check` pass. Logs stay in ignored `test-results/`. No web files changed, so no new build/browser/device-accessibility claim is made.
- **HL-100 remains IN PROGRESS.** These are point-in-time runtime consistency checks, not semantic/domain approval, independent model evaluation or 100-user target-hardware evidence. Candidate refresh/backfill, historical reassessment and catch-up delivery remain open. No production deployment/data mutation, model download/promotion, schema migration, real-source crawl, external message or credential publication occurred. The shipped capability approval registry remains unchanged and empty.

## Same-ID relation evidence revisions — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-evidence-revisions`, based on `c5e9e76`, after checking the explicit host, clean single worktree, current main and other-host branches. Migration `c3a5be941872` adds six evidence revision columns, SQLite/PostgreSQL update triggers and a counter-incarnation epoch. Planner v4 binds candidate/event/work/corpus-version/accessibly linked legacy-version/official-relation IDs and revisions in saved plans, request identity and current read selection. Same-ID corrections no longer leave prior applicability, actions or AI-derived severity current. Original results/citations/history remain unchanged. [Contract and operator boundary](RELATION_IMPACT_ANALYSIS.md#same-id-evidence-corrections-hl-100-6-september-2026).
- **302 affected API tests pass in 377.45 seconds**, including 23 new evidence/migration/race/scope scenarios, all relation/inbox suites, digest selection/resume, native evidence and the offline matching evaluator. Initial suites passed 20 and 61 tests. The first broader run passed 293 and failed nine prior official-field reversion assertions: corrected/reverted evidence now has a new revision and must be explicitly reassessed. Those tests now require retained stale history followed by a distinct successful reassessment. New preparation/ownership tests initially used the wrong error-response wrapper and omitted the required synthetic organization slug; fixtures were corrected without changing application error handling or scope rules.
- **Twelve isolated PostgreSQL scenarios pass on the final backend:** same-ID text, work metadata and official evidence corrections; identical/operational updates; actual legacy fallback; migration downgrade/re-upgrade; corrections during preparation and generation; final digest refusal; private legacy binding in ordinary and privileged sessions; batched history and large per-candidate history. The 50-event/7,474-history-row case retains **four selection/hydration queries and 111 materialized records**. The 10,001-analysis case retains **three queries and two loaded history payloads**. These are scoped query-count/load regressions, not 100-user target-hardware capacity measurements. The old index test now cycles that specific index migration in isolation instead of rolling back unrelated later migrations; the evidence migration has its own full Alembic roundtrip test and epoch-resurrection guard.
- Preparation checks the binding before/after dossier collection, rejecting a concurrent correction before a job or prompt is created. Generation-time corrections retain a succeeded but stale result; a failed reanalysis cannot revive the old result. Direct/Core SQL updates exercise database triggers, including changed text with an unchanged content hash. Same stored values and operational health/fetch/update fields retain the existing request without extra inference. Missing/malformed bindings remain history-only, private fallback identity is not exposed, and prepared digests lose corrected AI-only severity without sending mail. Official urgency remains independent.
- PostgreSQL ran only in the explicitly labelled temporary container `helvetic-hl100-evidence-pg-20260906`, with a memory-backed empty `hl099_regression` database at localhost:5439. Final PostgreSQL triggers are restricted to evidence-bearing UPDATE columns, avoiding large text/JSON comparisons on operational-only updates; the twelve scenarios were rerun after this optimization. The first attempt at port 55491 failed because Windows reserved it; its container was verified absent before using the free port. The successful test container was stopped and its automatic removal verified; all pre-existing services/volumes were untouched. No real inference, model download, external source fetch, message, production migration/data change or deployment occurred. Focused Ruff, the single Alembic head check and staged diff whitespace validation pass. Existing TestClient deprecation and Windows line-ending warnings are non-failing. No frontend files changed, so no browser/build result is claimed.
- **HL-100 remains IN PROGRESS.** Actual runtime/artifact freshness on reads, candidate refresh/backfill, digest catch-up, full retained-history pagination and independent relevance/entailment/target-hardware acceptance remain open. Revision identity is not semantic support or a defense against hostile database administrators. JSON representation changes and linked-but-unused legacy corrections may conservatively invalidate a report. Publishing this code does not authorize applying its required migration to production.

## Offline matching evaluation — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-093-semantic-matching`, based on `023345a`, after checking the explicit host, single worktree, clean checkout, current main and other-host branches. Added frozen dataset/label/prediction contracts, separate reviewer votes/adjudication, hash and split validation, real denominators and error/missing coverage, locale/source/feature strata, and an offline CLI using the actual production topic scorer. [Protocol and limits](SEMANTIC_MATCHING_EVALUATION.md). The legacy title benchmark now identifies itself as unreviewed, reports evidence-policy compliance as unmeasured, and can recommend a semantic trial without enabling embeddings.
- **170 combined API tests pass in 77.67 seconds**, including the new evaluation/baseline contracts, title benchmark, topic preview/live/history matching, capability/triage gates and relation candidates. Tests cover positive errors as recall misses, unresolved gold exclusion with visible pending counts, empty denominators, preserved disagreement/third-reviewer adjudication, self-review and locale declarations, exact source quotations, changed hashes, split leakage, invalid JSON/paths/read budgets, wrong-input/duplicate predictions, declared chronology and nonfinite values. The baseline tests exercise actual production filters, normalized official references, metadata values rather than field names, isolated cases and operation without gold labels. A network-denial fixture and sentinel database URL check that scoring stays in memory without using application connections.
- The checked-in six-case synthetic walkthrough deliberately contains **no reviewers or gold labels**. The actual held-out CLI baseline completed its two cases successfully (one match, one non-match); evaluation returned the expected **exit 1**, six pending reviews, zero scored pairs and null precision/recall denominators. The run honestly records the uncommitted implementation as dirty. This is a runnable infrastructure example, not a measured relevance baseline. Separate synthetic test-only declarations exercise the 200-pair readiness/target calculations, including passing arithmetic and false-positive failures; they are never published as independent review or model approval. `explanation_capability_approved` remains false in every result.
- Earlier focused/combined runs passed 34, 63 and 169 tests. Final review found that Windows Git line-ending conversion could change the frozen example hashes after checkout; scoped byte-preserving attributes and an actual temporary Git index/checkout regression now verify the package survives `core.autocrlf=true` unchanged. The final expanded suite above includes that regression. The legacy benchmark CLI completes successfully, without an independent-quality claim. Focused Ruff and `git diff --check` pass. Existing FastAPI/TestClient deprecation and Windows line-ending warnings are non-failing. Detailed generated predictions, evaluation results and test logs remain in ignored `test-results/`; existing outputs are not overwritten.
- **HL-093 remains IN PROGRESS.** Real independently reviewed public-source pairs, frozen baseline failures, comparison/Ask/report judgments, entailment, material omissions, useful actions, native-language review, target hardware and real-user comprehension are still unmeasured. Reviewer identity/independence and chronology are attestations; hashes do not authenticate people or establish truth. No model profile was approved. No application endpoint, database schema or frontend changed; no browser/build, real model, GPU, external source, notification, production data or deployment claim is made.

## Evidence-bound decision reports — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-092-decision-reports`, based on `aa13515`; resumed its uncommitted work after checking the host, single worktree, status and current remote main. Approved comparison execution now uses the strict decision draft for report v5: actual cited change explanations, organization activity references/conditions, status interpretation, concrete anchored actions and explicit review/no-action/unreviewed states. Paired admitted version evidence is retained in synthesis; unknown references, invented anchors/owners/dates, one-sided before/after claims and false no-action decisions receive one repair or remain failed. No template fallback after failed approved generation. [Contract and remaining limits](IMPACT_REPORT.md#structured-decision-drafts-hl-092-6-september-2026).
- **296 combined API tests pass on the final backend** in 237.43 seconds. The suite includes 33 new decision tests plus capability execution/registry, analysis, AI history, date mentions, selected evidence, jobs, impact matrix, runtime binding/cache and measured evidence allocation. Actual persisted HTTP flows use synthetic approvals/provider responses: success, exact source materialization, two-call generation and zero-call reuse, repair success/failure with identical catalogs, immutable v4 history and explicit v5 reassessment. Five-locale scenarios preserve source words and carry applicability/action conditions into reused Ask answers. Anchor/activity/type identity merges paraphrased titles without merging different activities or types; owner and due date remain unassigned/unreviewed. Partial coverage, missing changes, undercounted aggregate coverage, unknown applicability and invalid anchors cannot establish global no-action. These are contract regressions, not independent legal labels or actual model-language quality measurements.
- **Production build passes**: 1,919 localization keys (three unused legacy no-action labels removed), 31 shell tests, 22 resource/delivery tests, **193 report/navigation tests** including 40 new localized component cases, TypeScript and Next compilation. Both current comparison and saved history show explicit decision state, conditions, status interpretation, source disclosures, legacy warnings and per-change explanation basis. Date meaning remains unreviewed. The richer synthesis stays inside existing provider-call/native prompt budgets; the fake provider's output is not evidence that a real 700-token response can fit this draft.
- **All seven required production browser suites pass 335 full-document accessibility checkpoints**: topics 30, comparison 155, native evidence 10, inbox 20, onboarding 40, Marvin 40, capability settings 40. The comparison suite adds 30 five-language/phone-tablet decision journeys covering all three action states, interpretation/source labels, condition visibility, proposal disclosure, citation-to-evidence focus and unchanged legacy history. Existing modal keyboard/resize/scroll, 200-group navigation, saved-answer monitoring and no-inference handoffs remain passing. All APIs were intercepted; no real source/model/notification call occurred. Automated checks report no violation at any severity and no unresolved prohibited-ARIA issue; **233 incomplete color-contrast rule results** remain in diagnostic JSON for manual review. They are not passed accessibility or physical-device/screen-reader certification. The 390px review screenshot was inspected for readable conditions, source disclosure and contained layout.
- An initial resumed test run passed 119; the focused decision/capability run passed 53 before additional cases; an earlier combined run passed 295 before the undercounted-coverage guard. One combined invocation named two nonexistent test files and stopped at collection; the corrected explicit final suite above is authoritative. The first build identified the three unused translation keys; they were removed rather than bypassing the check. Ruff and staged `git diff --check` pass. Existing FastAPI/TestClient deprecation and Windows line-ending notices are non-failing. Detailed logs and screenshots stay in ignored `test-results/`.
- **HL-092 remains IN PROGRESS.** Exact references are not entailment. Legal date types/absence/relative anchors, cross-anchor semantic deduplication, independent organization/status/action judgments, fluent-language review and actual target-model/GPU acceptance remain open. The shipped explanatory approval registry is unchanged and empty. No model promotion/download, database migration, automatic historical reassessment, production rebuild/deployment, working-data mutation, source crawl or message to another person occurred.

## Capability-bound comparison execution — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-capability-execution`, based on `2a8ba42`. Real comparison Ask/Impact now choose their structured adapter from the verified selected model/task/language scope. Approved output uses grounded explanatory batch schemas; Impact adds bounded synthesis and single-batch Ask avoids an unnecessary synthesis call. Unreviewed/unknown/revoked/invalid profiles or unavailable identity/count protocol remain labelled selected evidence, including custom/cloud transports. [Execution, installation and limitations](AI_CAPABILITY_PROFILES.md).
- Reviewed input/output/safety/context limits apply to native measured selection and every completion, synthesis and one repair. Plans show effective reserves; actual provenance retains the decision and measured review-budget checks. Changed approval/review files abort acceptance and invalidate current cache/report/matrix/job reuse without modifying earlier conclusions. A same-language current report can still be reused with citations and its source report ID without a new Ask generation or fabricated Ask approval.
- **396 combined API/model-manager tests pass** in the final run (280.35 seconds), including 26 new capability execution cases, 63 registry-contract cases and the existing 307 runtime/token/evidence/cache/history/jobs/settings/matrix/analysis/access/isolation/relation/assistant/manager regressions. New real API cases use synthetic review files and synthetic HTTP replies to prove mode selection, exact task/language isolation, output-reserve clamping, native-fitting but review-budget-oversized rejection, one same-evidence citation repair, three mid-execution revocation stages, queued-policy reconciliation, invalid-review/profile-change cache invalidation, immutable historical results and cancellation-safe scope restoration. Settings accept a profile ID but reject trusted registry-path writes; public options omit review-file/reviewer metadata. No synthetic profile was promoted into the shipped registry.
- Production build passes **1,922 main catalogue keys, 31 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. Profile help/status/scope copy is additionally typed across all five locales. The new browser suite exercises the real production settings form with intercepted APIs: keyboard selection, explicit pointer save, reload persistence, empty/invalid/missing profiles, unchanged model ID and read-only viewer controls at 390/1440px in all five locales. Forty full-document axe checkpoints pass every reported violation severity and the unresolved prohibited-ARIA gate. Forty incomplete contrast rule/checkpoint occurrences remain in the diagnostic JSON for review; they are not certified passes. The mobile screenshot was inspected.
- **Final combined `npm run check:accessibility` exits successfully with 305 full-document checkpoints:** Topics 30, comparison 125, original evidence 10, inbox 20, onboarding 40, Marvin 40 and capability settings 40. All existing interaction assertions also complete. Independently counted saved checkpoint files contain zero reported violations; 220 incomplete contrast rule/checkpoint occurrences remain explicitly recorded, not waived or declared accessible. The new settings journey is now part of the required combined command. Disposable test servers, browsers and profiles cleaned up.
- Initial checks exposed a legacy `.field-help` contrast ratio of 3.84:1; it now uses the existing semantic muted foreground. A first browser attempt could not access the isolated debugging port in the sandbox; the authorized isolated rerun proceeded. Subsequent test-harness issues were corrected: the model inventory stub must report a ready runner, the runtime scope is synchronous, native key codes/actual form save must be used, and saved timestamps must be ISO dates. No test gate was disabled or suppressed. The final backend run above supersedes the earlier fixture failures; successful browser output requires all surrounding interaction checks, not just saved axe files.
- Shipped-registry CLI confirms zero approved profiles/grants and explicitly reports no semantic-quality verification. Ruff and diff checks pass. Existing FastAPI/TestClient lifespan deprecations remain warnings. No database schema migration, real provider call, new model download/start, source crawl, notification, production data change or deployment. HL-091 remains **IN PROGRESS** pending independent real-model task/language/domain evaluation and target-hardware acceptance. This verifies the execution policy, not legal usefulness, native-language quality, optimal evidence selection or 100-user capacity.


## Measured local evidence allocation — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-token-evidence`, based on `0e0dcff`. The local selected-evidence adapter now fits candidate dossiers using the bound runner's complete native prompt counts before generation, retains earlier/current change pairs and exact citable windows, and rejects omitted row references without renumbering. Executed coverage/selection and count manifests are persisted separately from proposed plans and billable usage; the saved deterministic diff is unchanged. Planner/protocol identity invalidates unmeasured cache reuse. [Contract and explicit limits](LOCAL_INFERENCE.md#measured-evidence-allocation--6-september-2026).
- **307 combined tests pass on the final code** (219.32 seconds): allocator, measurements, runtime binding/cache, AI history/jobs, selected evidence, impact matrix, analysis/settings, organization access/isolation, relation analysis, local assistant client and the complete model-manager suite. Focused runs also passed **54**, **25** and **19** tests; a previous combined run passed 306 before the last count-error trace event and missing-success-metadata regression were added. Existing FastAPI lifespan/TestClient deprecation warnings remain non-failing. Real persisted API scenarios exercise Impact, change questions and whole-document questions, exact sent row/character coverage, historical selection manifests, successful reuse without new counting/generation, immutable diffs and preservation of an older unmeasured answer after a failed protocol upgrade. Client → actual ASGI manager tests use a synthetic runner to exercise admission/leases, native count routing, unchanged counted/generation bodies, replacement between allocation and generation, and an oversized same-evidence repair blocked before decoding. Count tests cover incorrect hashes, missing metadata, inconsistent body counts, transport errors, cancellation and hard deadline without generation/retry.
- **Real native count-only allocation passes in all five locales** using `scripts/check_local_evidence_tokens.py`, the production allocator/chat-body builder and current gateway measurement helper. Existing Apertus v1.1 1.5B Q4_K_M file mounted read-only, image `sha256:b3c080f5015775404254cf9d153276ba9f3dd01acc9b1caa55c7d00d9a93989b`, build `b10752-b96806d96`, existing tracked Apertus template. Disposable CPU-only runner: two CPUs, 3 GiB memory cap, one 2048-token slot, random localhost-only published port; no model download or application database. Eight long synthetic rows are reduced to one intact earlier/current pair of exact windows in five probes. Initial → final input tokens: DE **9813 → 775**, FR **9013 → 727**, IT **11413 → 877**, RM **11413 → 878**, EN **9013 → 728**. Every final native recount matches the selected complete request, with **700 output + 128 safety tokens** reserved. Source strings remain unchanged. No decoding or semantic-quality assessment occurred. Fixture pins in this script are explicitly not deployment attestations; real pin enforcement is covered separately by the ASGI tests.
- Temporary container `hl091-evidence-native-20260906` was stopped by verified full container ID and label, then auto-removed; a corrected Docker listing confirmed no matching container remained. The first cleanup listing used the wrong formatter capitalization after the stop had already succeeded; it did not restart or modify any working service. Initial new tests used the wrong history field (`error_code` instead of the existing `error` text); the fixture assertions were corrected to inspect the actual API contract, failed allocation and zero provider calls, without changing production serialization. Ruff passes. Detailed terminal logs remain under ignored `test-results/`.
- HL-091 remains **IN PROGRESS**. Selection is conservative bounded halving over existing candidate batches, not optimal packing, independently reviewed relevance or an explanatory capability grant. Character preselection can already omit material; a larger repair prompt can still fail the final guard. Capability/task/language approval budgets, approval freshness, real explanatory evaluation and target-GPU/100-user gates remain open. No frontend or database schema changes, browser/accessibility certification, production deployment/data mutation, source crawl, notification or model promotion occurred.

## Complete local prompt token guard — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-prompt-tokens`, based on `bfe593c`. The gateway measures the full unchanged chat body through the reserved runner's native chat-token endpoint, reads actual slot context and reserves output plus 128 safety tokens. The same lifecycle lease covers preflight and decoding. Count-only diagnostics do not generate text; oversized/unverifiable requests return non-retryable 422 responses. Typed request/deployment-bound measurements flow to Ask/Impact history, completed plans and response-header logs without becoming billable usage. [Contract, compatibility and limits](LOCAL_INFERENCE.md#complete-prompt-token-guard--6-september-2026).
- **283 combined tests pass** in the final run (207.71 seconds): prompt-budget gateway/API tests, execution binding, runtime cache, AI history, jobs, selected evidence, impact matrix, analysis/settings, organization access/isolation, relation analysis, local assistant client and the complete model-manager suite. Boundary tests accept exactly fitting input and block one-token overflow before decoding; actual context is capped by configured context. Tests cover unchanged full payload/schema, default limits, conflicting/unlimited output controls, malformed/missing/boolean/string counts, unsupported endpoint, transport loss, hard preflight timeout, cancellation, lease-protected stop and queue release. Existing end-to-end analysis/Marvin client → ASGI manager tests retain real routing with a synthetic runner. New persisted HTTP tests cover successful/oversized Ask and Impact; invalid request hashes/pins/budgets are rejected without retry, and repair has a distinct measurement without an extra generation count. A prior combined run also passed; the final run follows the numeric error-detail improvement for both OpenAI-compatible and manager/Marvin clients.
- **Real native protocol check passes**, using `scripts/check_local_prompt_tokens.py` and current gateway measurement code mounted read-only into a disposable CPU container. Existing Apertus v1.1 1.5B Q4_K_M model mounted read-only; network disabled, no GPU, two CPUs, 3 GiB limit, child server on its own loopback with one 1024-token slot. Image `sha256:b3c080f5015775404254cf9d153276ba9f3dd01acc9b1caa55c7d00d9a93989b`, runtime build `b10752-b96806d96`, template SHA-256 `4fe9c9ea54eb87418a88457ab3a0961217885092ed4b222917e29dc0ff99e398`. Measured tokens equal actual `usage.prompt_tokens`: DE 49, FR 50, IT 51, RM 56, EN 51, repair 55. Six synthetic completions request at most eight output tokens each; output semantics are deliberately not evaluated. Oversized synthetic text counts 1837 tokens and is never sent for generation. No new model download. Test container `hl091-tokenizer-contract-20260906` completed and was removed; no matching labelled container remains.
- Ruff and `git diff --check` pass. An initial standalone manager run exposed an existing import-under-mocked-`Popen` issue on Windows; moving the analysis import before the fixture fixes standalone execution. The focused rerun passes 82 tests; the final combined run above includes them. Existing FastAPI lifespan/TestClient deprecation warnings remain non-failing. The first metadata-only Docker attempts could not instantiate a pruned working-container image; an already installed image was used instead, without restarting the working container. Test logs stay under ignored `test-results/`.
- HL-091 remains **IN PROGRESS**: final guard enforcement is not yet capability-aware evidence allocation, approved explanation quality or approval-cache invalidation. Legacy gateway responses without measurement remain unmeasured; cached/historical records are not relabelled. No frontend changes or new browser/translation certification, database schema migration, production data mutation/deployment, GPU/100-user benchmark, source crawl, notification or model promotion occurred. The native test establishes this tokenizer/template protocol on synthetic inputs, not general legal usefulness.

## Runtime-aware comparison cache and retained attempts — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-runtime-cache`, based on `ff3a47c`. Shared observed runtime scope now feeds local Ask/Impact keys, completed-job reuse, current report selection, comparison/law summaries and impact matrix. Generation retains the scope used for cache selection. Fully identified identical restarts reuse output; incomplete identities reuse only within a deployment. Failed Ask attempts are appended rather than reset. Completed jobs whose actual result differs from their enqueue cache key retain their ID/result/original key metadata while freeing the logical key for new work. [Contract and boundaries](LOCAL_INFERENCE.md#runtime-aware-cache-and-history--6-september-2026).
- **237 combined tests pass** in the final run: new runtime-cache and binding tests, history, jobs, selected evidence, impact matrix, analysis/settings, organization access/isolation, relation analysis, local client and all model-manager/gateway tests. Scenarios exercise runtime identity changes, identical restart reuse, incomplete identity restart invalidation, one snapshot across lookup/inference, offline readable views/history, no stale report reuse for Ask, failed-attempt immutability, queued model replacement, previously unbound queued work, completed Ask/Impact cached flags, original job retention and cancelled/client-changed scope restoration. A held runtime response verifies a wall-clock deadline and no generation retry after failed resolution.
- **Ten PostgreSQL 16.15 scenarios pass** through the same real API/job code on ten independent empty localhost scratch databases: queued/offline/restart/completed reuse for Ask and Impact, failed history and current/stale/offline views. The new `scripts/check_runtime_cache_postgres.py` accepts only explicitly named local scratch databases and refuses existing tables. Negative checks reject a nonlocal/non-scratch URL before connection and reject the populated scratch database while its analysis remains present. The task-labelled container and its disposable volume were removed; no working database or container was changed. These are sequential correctness checks, not a concurrent-load benchmark.
- Production build passes **1,922 localization keys, 31 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. Existing English/de-CH/fr-CH/it-CH/rm-CH cache and stale-report messages now distinguish generation from metadata reads and avoid falsely asserting earlier settings when current runtime is unavailable. No locale is claimed independently reviewed by a native speaker.
- The production comparison browser suite passes **125 full-document accessibility checkpoints**, its 40 Marvin-to-Ask handoffs, 15 populated locale/width journeys, 200-group navigation/evidence/return and 320px text-zoom check. Other incomplete accessibility checks remain in diagnostic JSON. The first attempt terminated before browser connection (`/json/version` timeout); the completed retry used the allowed local test-browser connection. Temporary browser/server processes and profiles were cleaned up. All app APIs were intercepted; these are existing comparison regression journeys, not new physical-device or screen-reader certification.
- Initial job fixtures lacked the worker's existing inventory-read response; they now provide a synthetic ready inventory without disabling the runtime snapshot check. The earlier expectation that retry rewrites a failed history row was replaced with exact preservation of that row and a separate successful record. A mistyped test filename stopped one collection attempt before any tests ran; the final explicit suite above is the verified run. Focused Ruff and `git diff --check` pass. Existing FastAPI/Starlette/httpx and Node module warnings remain non-failing.
- **HL-091 remains IN PROGRESS.** This is execution-aware cache coherence, not model capability approval or semantic quality. The shipped approval registry is empty, provider-based adapters remain and complete prompt tokens are not yet measured. Capability decision/review fingerprints and relation-assessment runtime freshness remain separate integration work. No real model/GPU inference, model download, production rebuild/deployment, working-data migration, source fetch or messages; no 100-user/target-hardware capacity claim.

## Analysis runtime binding and captured history — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-analysis-binding`, based on `75b795e`. Added a typed, whitelisted launch snapshot and one runtime binding per inference trace. Concurrent batches share one probe; synthesis, retries and the existing JSON repair retain the same execution pin. Missing runtime metadata fails before document transmission; changed/stopped deployments and wrong/missing successful-response pins are not accepted. Captured hardware and immutable identity enter history and completed-plan metadata without a post-run inventory probe. [Contract](LOCAL_INFERENCE.md#analysis-execution-binding--6-september-2026).
- **271 combined regressions pass:** new execution-binding tests plus settings, analysis, AI history, selected evidence, durable jobs, relation analysis, local benchmark, capability contracts, model-manager clients and all manager/gateway tests. The actual `ModelClient` → ASGI gateway test makes two accepted calls under one pin, stops the model and verifies that the next call cannot reach the runner. Actual Ask/Impact HTTP routes persist both successful and rejected responses with the original revision/artifact/deployment even when the test changes inventory immediately after generation. No later inventory call is allowed by those tests.
- Additional cases cover concurrent scopes, one probe across three batches, a transient failure followed by binding rejection without further retry, connection changes during a probe, cancellation releasing the probe lock, invalid/unavailable snapshots with zero generation calls, structured repair under the original budget, metadata whitelisting, inconsistent identity rejection and stable identity across an identical restart. Initial fixture failures came from the existing Docker URL normalizer overriding the synthetic gateway and older settings transports lacking the new runtime endpoint; fixtures now exercise both GET and POST explicitly. No transport or citation check was disabled.
- Focused Ruff and `git diff --check` pass. Existing Starlette/httpx and FastAPI lifecycle deprecation warnings remain non-failing. Tests use intercepted HTTP transports, temporary databases, synthetic artifacts and process doubles. No real GPU/model run, model download, Docker rebuild, frontend build/browser claim, production restart, schema/data migration, source fetch or messages.
- **HL-091 remains IN PROGRESS.** The stable fingerprint is recorded but not yet consumed by cache lookup, completed-job deduplication, report/matrix freshness or result reuse. Capability mode selection still uses the existing provider-based adapter; the approval registry is empty. Full serialized-prompt token measurement, independent semantic review and target-hardware gates remain open. API and model-manager code must be updated together by an operator. This verifies execution identity, not explanatory quality or arbitrary host-level runtime tamper resistance.

## Local runtime binding and admission cleanup — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-runtime-binding`, based on `7f3909a`. Added launch-time artifact verification, measured template/executable digests and a versioned active-runtime snapshot; per-start fingerprints; gateway model/pin checks; lifecycle reservations; cancelled/timed-out waiter cleanup; and stale-runner callback guards. Marvin now matches the selected profile's model ID/revision/artifact/alias to the runtime, pins the request and checks the returned binding. [Contract and compatibility](LOCAL_INFERENCE.md#deployment-binding--6-september-2026).
- **149 combined tests pass:** 44 model-manager/gateway tests and 105 API/capability/assistant/local-benchmark tests. Tests exercise same-model restarts, stale queued pins, same-size artifact tampering before launch, changed template/binary/image/context, unavailable identity inputs, dead slots, wrong aliases, model-busy lifecycle protection, timeout/cancellation cleanup, error responses, incomplete transport and old-process callbacks. Actual `ModelManagerClient` → ASGI gateway wiring is exercised with only the runner response mocked; the snapshot also validates against the capability identity contract. Runtime/assistant client tests verify no added generation retry or fallback on an absent/mismatched response binding, and no POST when the profile/runtime changed before generation.
- Earlier test setup failed because imported fixtures did not register their transitive dependency; the fixture wiring was corrected. The final combined suite includes the existing organization fairness, priority and replicated-readiness regressions. Focused Ruff and `git diff --check` pass. FastAPI lifecycle and Starlette/httpx test-client deprecation warnings are non-failing; the final run uses an ignored writable test-cache directory. The default capability registry remains empty.
- No real LLM/GPU inference, model download, Docker rebuild, production restart, schema/data migration, external source fetch or message was performed. Launch tests use small synthetic artifacts and process doubles; gateway calls use HTTP transports intercepted in-process. No frontend files changed or browser/build verification was claimed.
- **HL-091 remains IN PROGRESS.** Impact/Ask's full plan/batch/retry/synthesis/cache/history binding, measured complete-prompt token accounting and independent explanatory evaluation are still open. The runtime digest binds actual executable bytes plus the launcher's pinned-image assertion; it is not independent Docker image attestation or a hash of every system library. The GGUF artifact conservatively binds its embedded tokenizer. Rehashing is startup disk I/O and does not establish startup latency on target hardware. API and model-manager versions must be updated together by an operator; no silent old-manager downgrade was added.

## Reviewed AI capability contract — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-091-capability-contract`, based on `b7935ac`. Added the versioned registry loader, exact runtime identity and per-task/per-locale approval contract, bounded declared token budgets, review-artifact checksum verification, provider-independent limited/explanatory decision resolver and read-only CLI. The packaged registry is empty: no real model or locale was promoted. [Contract and integration boundary](AI_CAPABILITY_PROFILES.md).
- **63 tests pass** using synthetic review records only. They cover all ten Ask/Impact × five-locale scopes; no profile/task/language/identity fallback; changed model revision/artifact/tokenizer/template/runtime/hardware; candidate/revoked profiles; duplicate IDs/grants; missing, mismatched or modified review evidence; floating revisions; invalid paths; bounded files; strict token budgets including output/safety reserve; aware review dates; deterministic fingerprint changes; and CLI success/readiness failure/redacted error/read-only behavior. An initial negative test exposed that Pydantic `Literal[True]` accepts JSON `1`; an explicit pre-validator now rejects it. The unrelated test helper argument collision was also corrected. No test-generated approval is shipped.
- Focused Ruff checks and `git diff --check` pass. Existing FastAPI/Starlette test-client deprecation warning is non-failing. There are no application runtime or frontend changes, so this slice does not claim a production build/browser/real-model run. No migration, model download/start, production data change, messages or deployment occurred.
- **HL-091 remains IN PROGRESS.** This contract does not yet change the provider-based runtime behavior. Observed runner identity and switch detection, planner/adapter/synthesis/cache/history integration, actual tokenizer-measured request budgets and independent HL-093 explanatory evaluation on real deployment profiles remain open. Hash integrity does not establish reviewer independence or semantic truth; a synthetic pass does not establish target-hardware readiness.

## Semantic structure and accessible names — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-097-semantic-structure`, based on `55a9be3`. Monitoring choices now use explicit standalone H2/H3 or nested H3/H4 levels. Event pagination has distinct localized above/below-results landmarks and one page-count live region instead of two. The deterministic change overview is a named group; omitted excerpt text uses a visually hidden explanation with an aria-hidden visual ellipsis instead of an unsupported span label. [Contract](NAVIGATION_ACCESSIBILITY.md#semantic-structure-and-names--6-september-2026).
- The original build fails the stronger inbox test on `landmark-unique`. The first comparison run after the group fix exposed additional unresolved ARIA on omission spans; those were corrected as text and now checked in the actual Chromium accessibility tree. No headings, navigation controls, explanatory text or axe rules were removed/hidden to evade a finding. The visible ellipsis remains while its localized explanation is readable to assistive technology.
- The shared gate now rejects **every reported violation severity**, plus unresolved `aria-prohibited-attr` entries even when classified incomplete. Missing/duplicate/incorrect fixture counts still fail. Inbox expands from four English layout checkpoints to 20 five-locale/four-width checkpoints; actual accessibility-tree navigation names, both pagination controls and one count announcement are required. Standalone and context monitoring retain all four heading nodes at their correct levels. The comparison tree must expose the named group and omission explanation.
- **Final combined command exits successfully: 265 full-document checkpoints, zero reported violations and zero unresolved prohibited-ARIA findings.** Topics 30, comparison 125, original evidence 10, inbox 20, onboarding 40 and Marvin 40 all complete their existing interaction assertions as well. Saved result JSON was independently counted and checked for engine version 4.13.0, empty violations and absence of prohibited-ARIA incomplete results. The remaining 180 incomplete contrast rule/checkpoint occurrences are retained for review (repeated states/locales, not 180 distinct confirmed defects). Disposable browser/server processes and profiles cleaned up.
- Production build passes **1,922 localization keys, 31 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. The obsolete generic pagination-name key was removed after the unused-key check correctly flagged it; no checker exception was added. Existing Node typeless-module warning remains non-failing. Mobile editor screenshot inspected; `git diff --check` passes.
- No backend/schema/production-data changes, provider calls, model downloads, mail or deployment. All browser APIs use intercepted synthetic responses on disposable test servers. HL-097 remains **IN PROGRESS**: other incomplete checks, broader routes/error/dialog states, physical-device/software-keyboard/audio, native-language and real screen-reader/Firefox/Safari acceptance remain open. The accessible tree and axe results do not certify a complete assistive-technology user experience.

## Full-document accessibility checkpoints — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-097-accessibility-audit`, based on `c5f446e`. Added exact-pinned development-only axe-core **4.13.0** to six required production-browser suites and a combined `npm run check:accessibility` command. Each checkpoint requires its visible fixture, scans the full document without exclusions, rejects critical/serious violations, and saves full findings/incomplete checks. Missing/duplicate checkpoint coverage fails; every required suite must finish. [Scope and checkpoint table](NAVIGATION_ACCESSIBILITY.md#required-automated-accessibility-journeys--6-september-2026).
- Initial real-page scans failed on contrast in Marvin's privacy/help/runtime/speaker labels, saved-answer timestamps and suggested questions, shared eyebrow/muted/status/footer/navigation text, version metadata, synthetic labels and saved-evidence/pagination links. Replaced 27 muted foreground rules with the existing semantic token and darkened one amber label rule. A second finding exposed active shared buttons temporarily retaining disabled opacity: buttons now transition colors only. No axe rule, selector or severity was suppressed to pass.
- **Final combined command exits successfully with 249 checkpoints:** Topics 30, comparison/Marvin-to-Ask/evidence/report/history 125, original saved evidence 10, inbox 4, onboarding 40 and personal Marvin 40. Every saved result identifies axe 4.13.0 and none contains a critical/serious violation. All existing interaction assertions also finish; required fixture/coverage checks are unconditional. Temporary servers, browser processes and profiles cleaned up. Reports retain 40 `heading-order` and four `landmark-unique` rule/checkpoint occurrences (repeated locale/viewport cases, not 44 unique defects), plus 164 incomplete contrast and 35 incomplete ARIA rule/checkpoint occurrences. These incomplete results require review, not a presumed pass.
- Final production build passes **1,921 localization keys, 31 shell, 22 resource/delivery and 153 report/navigation tests**, TypeScript and Next compilation. The separate rendered contrast suite passes **8,060 samples**, minimum **4.79:1**, covering its existing default/selected/hover/focus/error matrix. That focused suite excludes disabled controls by design; the new full-document axe invocation has no custom exclusions. Existing Node typeless-module warning remains non-failing.
- Concrete remaining issues: `heading-order` in the monitoring-choice hierarchy (also exposed behind nonmodal desktop Marvin); duplicate `Inbox pages` landmark names; axe incomplete `aria-prohibited-attr` for `.triage-summary` carrying an `aria-label` on an unroled div; incomplete `color-contrast` cases needing inspection. These are not declared fixed or certified by passing the critical/serious threshold. Original evidence/inbox error combinations and other routes, all-locale inbox, manual screen-reader/physical input/audio, native-language and Firefox/Safari acceptance remain open HL-097 work.
- All app APIs are intercepted synthetic fixtures; scans use a locally installed test engine, not a cloud audit service or model. No backend/schema/production-data changes, real source fetch, messages, deployment or model downloads. The mobile Marvin screenshot was inspected for unchanged controls/layout and readable guidance. Checkpoint JSON files under `test-results/accessibility/` are diagnostic evidence, not standalone proof that the surrounding suite completed successfully.

## Immediate Marvin → cited Ask handoff — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-097-marvin-ask-handoff`, based on `21709dd`. A comparison question now transfers locally and focuses Ask before saving personal history. A slow/unavailable save cannot block editing; background saves have a ten-second deadline, no retry and a request/context generation guard. No automatic Ask submission was added. [Contract](NAVIGATION_ACCESSIBILITY.md#marvin-to-cited-ask--6-september-2026).
- **Red/green reproduction:** the new real-browser test withheld the handoff-history response until Ask was usable. The previous production build failed `Handoff lost or altered question` because it waited for that response. The changed build completes the local handoff before the test releases the response. This is controlled fixture evidence, not a production-provider latency benchmark.
- **95 populated production-UI journeys pass:** 40 new handoffs (five locales × 390/768/1024/1440 px × available/held personal history), the existing 15 comparison/evidence journeys, and 40 Marvin monitoring reader/admin journeys. Handoffs assert exact multiline and 2,000-character payloads, one personal save, focused Ask, no document reload or implicit inference, retained overlay scroll ownership, Escape/opener restoration and close/reopen draft persistence. Late 503 responses do not steal focus; an old successful save after real context detach/reattach does not overwrite the new handoff list. Existing 200-material-group paging/search/evidence/return and 320 px text-zoom checks also pass.
- Production build passes **1,921 localization keys, 31 shell, 22 resource/delivery and 153 report/navigation tests**, TypeScript and Next compilation. Existing Node typeless-module warning remains non-failing. `git diff --check` passes. APIs are intercepted against isolated temporary browser/server instances; no live model, working backend, mail, source fetch, production data change or deployment. Browser instances/profiles cleaned up.
- HL-097 remains **IN PROGRESS**. Broader real cross-feature journeys, whole-route critical/serious accessibility scans, screen-reader, physical keyboard/software-keyboard/audio, native-language and Firefox/Safari acceptance are not claimed. Personal history still has no durable offline retry queue; a failed save is not represented as saved, while the transferred Ask draft remains editable.

## Marvin modal focus and scroll ownership — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-097-marvin-focus`, based on `f2bef54`. Marvin uses a named native modal dialog at widths ≤1350 px and a nonmodal desktop panel. Mobile retains its full-screen layout, tablet its drawer/backdrop; close targets are 44×44 px. Initial focus, forward/reverse Tab, local Escape ownership, opener/fallback restoration and focused unsent drafts across resize/close are handled by the panel. Removed the global Escape listener. Marvin and comparison share reference-counted root/main scroll ownership. [Contract](NAVIGATION_ACCESSIBILITY.md#marvin-panel-focus--6-september-2026).
- Final production build passes **1,921 localization keys, 31 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. Four new scroll-lock tests cover both release orders, duplicate cleanup, detached/replaced main containers, missing main and an existing external lock. Node's existing typeless-module warning appears when directly importing TypeScript tests; it does not fail the build.
- Final populated Marvin browser suite passes **40 journeys**: five languages × 390/768/1024/1440 px × reader/organization-admin. It checks named modal/nonmodal state, visible close target, actual Tab/Shift+Tab/Escape, background focus rejection, nonmodal desktop focus, focused draft across breakpoints, close/reopen persistence, root/main style restoration and explicit personal-message-to-topic navigation without implicit copy or activation. A synthetic nested native dialog verifies only the child closes; a synthetic removed opener verifies fallback to the persistent trigger. Tablet checks distinguish true backdrop dismissal from a drag beginning inside. These synthetic subcases are not claims that every actual cross-feature nested flow was reviewed.
- Final navigation suite passes **30 populated role/language/mobile journeys**; final comparison suite passes **15 populated locale/width journeys**, 200-group navigation/search/evidence/return and 320px doubled-text reflow. The existing scoped contrast suite passes **8,060 rendered samples, minimum 4.79:1**; disabled controls remain excluded from normal-text contrast requirements. Phone and desktop Marvin screenshots inspected. Each suite runs disposable Next/Chrome processes with synthetic intercepted APIs; no live AI, message, organization switch or monitoring write occurs. Test processes completed and temporary profiles cleaned up.
- Initial build rejected the obsolete source assertion requiring the global Escape handler. Replaced it with named-panel/native-cancel checks plus the actual browser focus assertions above; no test bypass. Corrected the browser's old `.marvin-panel` dismissal selector to the real open `.marvin-drawer`, preventing false success while the panel remains open. The final build and all browser suites were rerun after the transient-opener fallback was added. `git diff --check` passes.
- HL-097 remains **IN PROGRESS**. No whole-route critical/serious accessibility scan, physical keyboard/software-keyboard/audio, screen-reader, native-language or Firefox/Safari acceptance is claimed. All actual Marvin/Ask/evidence nested handoffs and broader user testing remain separate gates. No backend changes, model downloads, production deployment or real notifications.


## Law-detail history metadata — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-law-history-metadata`, based on `3a9b797`. The real law-detail endpoint now reads version/comparison/observation lists through three scalar queries. SQL computes character/passage/maximum numeric page counters and selects saved comparison counts without returning historical text/passages/diff bodies. Metadata, evidence/original-file URLs, 50-comparison/100-observation preview sizes and empty-history behavior remain compatible. Equal timestamps now have an ID tie-break. [Contract](REGISTRY.md#law-detail-history-metadata--6-september-2026).
- **53 affected API regressions pass** in the final run: law metadata, full workflow, registry, scalar timeline, corpus evidence and paged evidence suites. The populated HTTP test compares all 152 version summaries, 50 comparisons and 100 observations to the old serializers, with full-key/value equality. ORM instrumentation allows only the current version and selected comparison used by the existing summary; no historical version/diff or observation bodies load. Calling the history readers directly uses three queries and no ORM hydration. Read requests cause no source fetch, model call or data mutation.
- **Six PostgreSQL 16.15 scenarios pass** on independent empty localhost scratch databases: large actual law detail, privileged organization/owner scope, empty page arrays, null/zero pages, integer maxima and preserved fractional numeric page values. Unicode text counts match Python's existing counts on both engines. Foreign history, foreign/no watch and foreign law metadata remain excluded even without the normal session loader filter. Task-labelled scratch containers removed; no schema migration, real provider/mail calls, working data edit or deployment.
- Ruff and `git diff --check` pass. Existing Starlette/httpx deprecation warning remains. No frontend changes and no new browser/build claim: this verifies API metadata compatibility and the actual HTTP loading path, not a whole mobile/UI acceptance gate.
- HL-099 remains **IN PROGRESS**. The version list and metadata JSON can still grow, the existing previews still require explicit paging/coverage disclosure, and SQL counter aggregation must inspect saved data. Current `law_summary` version/comparison bodies and its analysis-history selector remain separate work. No constant-database-work, complete-page memory bound, target-host latency or concurrent-user capacity claim is made.


## Scalar timeline reads — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-timeline-projections`, based on `24ea1f8`. The standalone/nested regulatory timeline selects display metadata, counts normalized versions in SQL and selects the existing 100-observation preview before hydration. Relationship metadata and usable watched-alias links are resolved in one query; no per-edge ORM lookup. Entry identities, source URLs, reciprocal direction and UTC chronological/typed-ID order remain intact. [Contract and limits](REGISTRY.md#scalar-saved-timelines--6-september-2026).
- **55 distinct API regressions pass**: a 27-test registry/timeline run and a 39-test corpus/evidence/timeline run overlap by 11 tests. The new seven cases cover 151 new versions, comparisons and events with large text/JSON bodies; 501 observations; 50 visible related works and one private work; active/paused/foreign/unwatched aliases; private law/watch/mapping/work fallbacks; private saved versions/comparisons and foreign observation records. SQL/ORM instrumentation asserts nine SELECTs for mapped timeline reads, four for legacy fallback, zero ORM object loads and no full text/passages/diff/evidence/metadata columns selected. The source-provenance query has SQL LIMIT and stable equal-time ordering. No source fetch or inference occurs on these reads.
- **Seven PostgreSQL 16.15 scenarios pass** using independent empty local scratch databases: `timeline-large`, `timeline-relations`, `timeline-private`, `timeline-law`, `timeline-watch`, `timeline-mapping`, `timeline-work`. Normal API sessions and explicitly privileged reader sessions agree on visible results; missing/foreign watches or laws return 404 and private related works never leak title/ID/link. Task-labelled scratch containers removed. No migrations, production data edits, actual provider/mail calls or deployment.
- Initial registry testing exposed the existing global loader-criteria interaction with an unnecessary work alias; the relationship query now joins the single work table directly, retaining explicit visibility, and passes both database engines. Two initial scope-test failures were incorrect assertions against `DomainError.status_code`; corrected them to the actual `status` attribute, retaining exact 404/HTTP assertions. Ruff and `git diff --check` pass. Existing Starlette/httpx warning remains. No frontend edits or new browser/build claim.
- HL-099 remains **IN PROGRESS**. Scalar response rows still grow with full timeline/identifier/expression/relation history; complete UI/API paging and metadata limits remain open. Separate law-detail version/comparison lists and law-summary selected payloads are not optimized here. Constant query count does not prove constant database work, full-page memory bounds, p95 latency or target-host concurrent-user capacity.


## Exact official-relation endpoints — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-relation-endpoints`, based on `42f984a`. A shared identity/direction check now guards official model context, final result validation, inbox confirmation/replacement-derived importance and monitor-successor actions. A corrected relation cannot authorize claims for a stale candidate linking different works. Incoming `replaces` relations show the subject as successor and reuse its existing watch. [Contract](RELATION_IMPACT_ANALYSIS.md#exact-pair-and-direction--6-september-2026).
- **95 affected API tests pass** across endpoint/direction, evidence gating, official freshness, relation analysis, generation-cache and bounded inbox/context/history suites. New cases cover missing/unrelated/self endpoints, both valid directions, matching citation IDs with an invalid pair, both inbox APIs, unchanged watch/job counts after rejected actions, reuse of a monitored successor and final digest exclusion after a relation correction. Provider and mail calls are intercepted; no real inference or email. Existing Starlette/httpx deprecation warning remains.
- **Seven PostgreSQL 16.15 scenarios pass** in separate empty localhost scratch databases: invalid source, invalid target, incoming replacement, prepared digest, context, successor aliases and history batches. One/50-event context reads remain 16 SELECTs without full document ORM payloads; 50 events with 7,474 historical records retain four history queries and 111 loaded records. All task-labelled scratch containers removed; no working/production database changes.
- Production build passes **1,921 localization keys, 27 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. The existing successor-monitored notice is corrected in five locales to avoid claiming both documents are monitored. No new browser or native-language review claim for this copy-only frontend change. Ruff and whitespace checks pass.
- Result schema v4 makes earlier reports conservative history-only without rewriting saved reports/citations or triggering reanalysis. A subsequent explicit request uses a new identity. One initial affected-suite failure identified an old positive test fixture lacking endpoint IDs; the fixture now supplies its actual pair, and invalid/missing-pair cases remain strict. Prior run output lost at context rollover was not counted: the final 95-test run and build were rerun after confirming no earlier test/build process remained.
- HL-100 remains **IN PROGRESS**. Exact identity is not semantic entailment. Candidate reprocessing, same-version/evidence/runtime freshness, digest catch-up and independent quality/target-hardware gates remain open. No deployment or real notifications.


## Official-relation freshness — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-official-relation-freshness`, based on `70fe974`. Planner v3 saves an explicit nine-field relation binding in both request identity and plan provenance. History, legacy/paged inbox and shared digest selectors compare the candidate's current relation ID/state/type/authority/provenance/evidence fingerprint/subject/object/source-version IDs. Attachment/detachment, correction and missing provenance invalidate the old AI conclusion without rewriting its report, citations or history. [Contract and migration behavior](RELATION_IMPACT_ANALYSIS.md#official-relation-corrections-on-reads--6-september-2026).
- **124 affected API regressions pass** across the new official freshness suite, relation analysis/profile/configuration/prompt/version freshness, bounded inbox history/page APIs, digest periods and durable digest continuation. **44 additional evidence-gating/generation-cache tests pass**, for **168 distinct tests** in these final runs. New cases cover all bound-field corrections, proposed/rejected transitions, restored-input reuse, explicit known-absent relations versus missing binding, detachment/new attachment, failed/new/cache-reused requests, preserved citation evidence and a prepared digest rejecting obsolete AI-only importance. No real provider or mail calls. The existing Starlette/httpx deprecation warning remains.
- **Six PostgreSQL 16.15 scenarios pass** on independent empty local scratch databases: four new official-relation workflows (`fields`, `links`, `retry`, `digest`) plus the large history and history-batch checks. The 50-event/7,474-history-record case retains four queries and 111 materialized records; 10,001 analyses retain three queries and two loaded payloads, including populated index roundtrip. Scratch containers removed; no working database migration or deployment.
- The first broad run correctly treated synthetic supposedly-current histories without the new relation binding as stale. Updated the three current-history fixture builders to include explicit actual relation snapshots; unchanged bounded-count/current-versus-legacy assertions then pass. Missing-provenance regressions remain strict. The unlinked-case fixture now explicitly detaches its initial proposed relation before generating the no-relation report rather than assuming `confirmed=False` means no relation.
- Ruff and `git diff --check` pass. No frontend/build/browser claim for this backend change. Existing old reports lacking this provenance become history-only; no automatic reanalysis, jobs, notifications or historical rewrite is introduced. Explicit/scheduled requests use a new planner identity and can later reuse an identical completed result.
- HL-100 remains **IN PROGRESS**. Same-version content/metadata changes, evidence JSON changes without a corresponding fingerprint update, candidate refresh/backfill and endpoint/direction consistency before new official claims, runtime artifact freshness, digest catch-up and independently reviewed semantic quality remain open. Current-field comparison does not establish claim entailment, a global transaction lock or target-host capacity.


## Shared visible-page registry details — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-registry-detail-batches`, based on `ead622c`. Discover now selects direct/incoming/outgoing watched-law links in one scalar union/join query, with explicit organization/law/mapping access and duplicate-path removal. Both views share one expression and one typed-date query for their visible rows. Exact event/version dates stay separate; deterministic watch-ID order replaces unspecified link order. [Contract and remaining bounds](REGISTRY.md#shared-visible-page-details--6-september-2026).
- **43 affected tests pass** across the new detail suite, both registry-page suites, original registry, evidence navigation and corpus evidence. Instrumentation verifies exactly **three detail queries for one and 50 events**, no full ORM objects or diagnostic/document JSON, and zero queries for empty details. The 50-source-work fixture includes 150 forward/reverse/repeated relation paths plus a self-edge, direct and related paused watches, and work/expression/event/version dates. Other assertions cover foreign watch/law/mapping access even in a privileged session, shared-work events retaining separate date facts, and an unrelated saved version whose ID equals a work ID. Date lookup now includes entity type, preventing that collision from mixing provenance. The initial fixture's entity label was corrected to the schema's `version` value before the passing run.
- Final code passes **six independent empty PostgreSQL 16.15 scenarios** through the guarded local runner: `registry-details`, `registry-detail-scope`, `registry-detail-dates`, `monitored-pages`, `registry-watches`, `registry-dates`. Each temporary loopback database/container was removed after its scenario. No model/source/mail calls, application-data changes, schema migrations or production deployment.
- Ruff and `git diff --check` pass; the pre-existing Starlette/httpx deprecation warning remains. No frontend code/build/browser claim for this backend-only slice. Existing UI row/link/date contracts are preserved apart from stable link ordering and exclusion of wrong-type date collisions.
- HL-099 remains **IN PROGRESS**: constant query count is not constant database work or bounded single-work fan-out/response size. Large relation/expression/date sets, full timeline reads, planner cost, live-cursor reconciliation and intended-host 100k-event/20-reader/overlap acceptance still require work. This does not certify capacity or alter relation confidence/review semantics.


## Monitored-document SQL pages — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-monitored-pages`, based on `4d47a34`. Monitoring now selects the latest visible-work event in SQL, retaining last-check/creation fallbacks and paused/unmapped watches. Discover and Monitoring share bounded scalar candidate traversal and Unicode literal search. Selected-page comparison IDs, expression IDs and official-date values are batched; no full watch/law/version/comparison/event/state objects are needed for list construction. [Contract and limits](REGISTRY.md#monitored-document-sql-pages--6-september-2026).
- **40 tests pass** across `test_registry_monitored_pages.py`, `test_registry_event_pages.py`, `test_registry.py`, `test_evidence_navigation.py` and `test_corpus_evidence.py`. Seven new scenarios cover 231 watches against 231 equal-time events (100/100/31), SQL/ORM hydration instrumentation, sparse Unicode literal search and legacy defaults, latest-event personal read state, privileged foreign law/mapping/work/watch isolation, the 25-hour Zurich autumn day and creation fallback, visible comparison IDs with equal-time tie breaking, and populated latest-work-index downgrade/upgrade. The traversal asserts five bounded candidate queries and three date/three comparison batches across three pages. The comparison fixture was corrected to create saved comparisons explicitly; importing a baseline alone does not create one.
- After consolidating shared pagination, the final code passes all seven new suites plus the prior event traversal/search suites on **nine independently empty PostgreSQL 16.15 databases** using the guarded local runner (`monitored-pages/search/read/scope/dates/comparison/migration`, `registry-events/search`). Temporary loopback Docker containers only, each removed after its scenario. No working data, source/provider/model calls, mail or deployment. Migration `b294ad830761` adds `(work_id, detected_at, id)` and preserves populated event IDs through downgrade/upgrade.
- Ruff and `git diff --check` pass. One pre-existing Starlette/httpx deprecation warning remains. No frontend code or response-shape change; no new browser/build or physical-device claim is made for this backend slice.
- HL-099 remains **IN PROGRESS**. Derived latest-activity sorting and comparison ranking still consume database work; exceptionally large expression/date fan-out, Discover's relation/date expansion, full document timelines and intended-host 100k-event/20-reader/overlap measurements remain open. Live watch ordering can move when a new event/last-check update arrives between pages; this does not implement snapshot/reconciliation semantics or prove the no-skips-under-mutation acceptance gate.


## Registry event SQL pages — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-registry-event-pages`, based on `86416b3`. Replaced full-history ORM hydration in `view=events` with scalar SQL filtering and timestamp/ID keysets. At most 100 candidates per select; retain one page plus matching lookahead. Unicode literal search scans successive bounded batches rather than dropping later matches. Dates/links expand only returned rows; exact evidence access is unchanged. [Contract and explicit limits](REGISTRY.md#event-list-sql-paging--6-september-2026).
- **33 tests pass** across `test_registry_event_pages.py`, `test_registry.py`, `test_evidence_navigation.py` and `test_corpus_evidence.py`. Six new scenarios cover 231 equal-time records without skips/duplicates or JSON hydration, a sparse Unicode/literal-wildcard search beyond 200 newer candidates, a 23-hour Zurich calendar range and official dates, privileged tenant/personal-read isolation, both directions of related paused watches with foreign exclusion, and populated index downgrade/upgrade. The first implementation incorrectly treated global relation/event/expression models as owner-scoped; model inspection corrected that before the final tests. One existing Starlette/httpx deprecation warning remains.
- All **six scenarios pass independently on PostgreSQL 16.15**, using the empty-local-database guard in `scripts/check_inbox_history_postgres.py` (`registry-events`, `registry-search`, `registry-dates`, `registry-scope`, `registry-watches`, `registry-migration`). Disposable loopback containers with temporary data only; no working databases, provider calls, messages or deployments. The populated migration preserves all event IDs and verifies the exact composite-index columns after downgrade/upgrade.
- Ruff and `git diff --check` pass. No frontend contract/UI changes and no new browser/build claim for this backend slice. The previous registry browser evidence remains recorded separately.
- HL-099 stays **IN PROGRESS**: monitored-document and timeline materialization, per-visible-work lookups/fan-out, total no-match search/planner cost, cross-request snapshot/late-admission semantics and intended-host concurrency/capacity gates remain open. The index supports ordering but does not guarantee the planner avoids every scan or establish 100-user capacity.



## Registry failure recovery — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-095-registry-recovery`. Initial list failures no longer look like empty results. The stable Refresh/Try again control repeats the same request, preserving filters/cursor, with visible pending state and deduplication. Failed refreshes retain readable rows with a warning; previously empty responses have a distinct stale-result explanation and suppress the ordinary empty-state claim. Explicit first-page and filter-clearing actions provide recovery without hidden changes. Error details are expandable. The displayed load time is explicitly not source freshness. [Contract](REGISTRY_FILTERS.md#failed-loads-and-recorded-source-health-6-september-2026).
- Code inspection confirms `RegistryReader` copies health from saved `RegulatoryEvent.connector_health` (or unknown when no event exists). Rows now label this as recorded status, with a list-level explanation that it is not live health or complete coverage. Existing localized kind/lifecycle/health names and native language names replace raw row enums. No current connector status or legal conclusion is inferred from a refreshed UI response.
- Final production build passes **1,921 localization keys, 27 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. The actual browser suite passes **20 existing filter journeys, 20 long-list return journeys and ten new failure-recovery journeys** across all five locales and 390/1440px. Added assertions cover initial failure versus empty, collapsed details, a delayed same-query retry, one request despite repeated activation, no document reload, preserved cached rows, explicit cursor/filter recovery, stale-empty suppression, metadata/health explanations, translated messages and no overflow. Registry requests remain GET-only; all application APIs intercepted.
- Mobile recovery screenshots inspected. The initial copy incorrectly said old results remained below even for an empty cached response; the final version has a separate stale-empty explanation. The synthetic empty-page fixture was corrected to return no next cursor, matching the actual API contract, before the final passing browser run. No source/model/mail calls or working data changes.
- HL-095 remains **IN PROGRESS**. Current source freshness/coverage and actionable identity-mismatch/admin repair still require dedicated work. Official-date provenance and arbitrary server descriptions are not fully localized by this slice. Physical-device/assistive-technology/native-language and independent usability gates remain open. No production deployment.


## Registry evidence-return position — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-095-registry-return`. Saved evidence now offers a matching **Back to your results** link. Explicit return and native Back restore the originating registry link and exact result URL, including filters/cursor/locale. A same-tab 30-minute marker stores IDs and navigation/filter values only, checks user/organization and route/destination shape, and is consumed once. Missing rows receive an honest notice; storage denial keeps ordinary navigation. Very tall cards center the link, not the entire card. Monitoring/Discover use normal navigation links with current-page state instead of incorrect tab semantics. [Contract and limits](REGISTRY_FILTERS.md#return-to-a-registry-record-6-september-2026).
- **Seven helper regressions pass** for URL/row preservation, user/organization isolation, expiration/future time, external/invalid routes, malformed/oversized data, denied storage/one-time consumption and bounded replacement. Production build passes **1,907 localization keys, 27 shell, 22 resource/delivery and 153 report/navigation checks**, TypeScript and Next compilation. The first build correctly caught an obsolete source assertion requiring tab semantics; it was replaced with current-page-link assertions plus actual browser keyboard checks, not bypassed.
- Final registry browser suite passes the original **20 filter journeys** and **20 additional long-list round trips** (five languages × 390/1440px × Monitoring/Discover). Each uses 20 records, a final card taller than the viewport, explicit evidence return and native Back. Assertions require unchanged URL/cursor, no full reload, correct focused row/link visible outside sticky navigation, consumed marker, normal Tab order and missing-record notice on a new page. **Ten native-evidence journeys plus legacy viewer smoke** also pass after the final rebuild, covering later-page citations, delayed/failed paging, original-file availability, missing targets and display milestones. All APIs intercepted; no actual document/provider/mail calls or working data mutation.
- Scope is reading continuity, not a frozen result snapshot or a new authorization boundary. Resource cache policy remains unchanged. The helper rejects other scopes; returned UI state is additionally keyed to the current user/document identity. Browser storage is optional and may survive browser session restoration. Cross-device history, independent native-language/mobile/assistive-technology validation, complete source-health recovery and remaining HL-095/097 criteria stay open. No production deployment.


## Focused saved-word excerpts — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-096-focused-deltas`. Material cards show the first actual saved changed fragment with aligned before/after context and insertion/deletion markup. A changed deadline at the end of a long article is no longer hidden by its unchanged prefix. Saved parts must reconstruct and align with the exact passages; invalid/missing parts use an explicitly labelled plain excerpt. Each side is bounded to 248 source code points plus omission markers; the caption counts fragments only in the first exact change. Full exact evidence and other group changes remain reachable. No inference, re-diff, API mutation or database migration. [Contract](MATERIAL_READING.md#focused-saved-word-excerpts-6-september-2026).
- **Eight new helper regressions pass**: late deadline, insertion/deletion with shared context, entire one-sided passage, additional omitted fragments, malformed or misaligned parts, unchanged/missing evidence, Unicode bounds and unchanged input. Production build passes **1,905 localization keys, 27 shell, 22 resource/delivery and 146 report/navigation checks**, TypeScript and Next compilation.
- Populated isolated browser suite passes **15 five-language × 390/768/1024px journeys**, desktop transitions, 200-group paging/search, six legacy exact-row pages, exact-evidence focus/return and 320px doubled-text reflow. Each locale/width explicitly checks that the saved `10 → 30` deadline after 900+ unchanged characters appears as actual del/ins text. Existing contrast suite passes **8,060 samples, minimum 4.79:1**; this is scoped regression evidence, not a complete accessibility audit. Generated mobile screenshot inspected; awkward singular/plural preview copy was corrected before final rebuild and rerun. API responses intercepted throughout, no real inference or data changes.
- HL-096 remains **IN PROGRESS**. This preview does not rank legal significance or summarize the whole group. Complete response/search-memory size, very large group selectors, broader density/layout consolidation, native-language/physical-device/screen-reader checks and independent user acceptance remain open. No production deployment.


## Saved legal-unit headings — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-096-legal-headings`. Material groups now reuse saved before/after legal hierarchy rather than raw unit IDs or guessed article numbers. The read-only helper verifies passage attachment on each side, omits anonymous positions, retains distinct old/new numbering and disambiguates repeated article numbers with chapter ancestry. It displays three distinct labels plus an additional count while searching all labels. Extraction provenance and neutral fallback remain explicit. [Contract](MATERIAL_READING.md#saved-legal-unit-headings-6-september-2026).
- **22 existing deterministic-diff tests pass in 0.82 seconds**, including saved legal-unit attachment and inserted/renumbered articles. The comparison detail serializer returns the existing persisted diff projection. **Eight helper regressions pass**, covering article/subdivision inheritance, absent/unknown hierarchy, wrong-side passage attachment, renumbering, repeated continuation deduplication, more than three labels, chapter-only hierarchy, malformed/oversized data and equal article numbers in different chapters. A read-only transformation of the existing diff projection needs no database migration or extraction rerun.
- Production build passes **1,902 localization keys, 27 shell, 22 resource/delivery and 138 report/navigation checks**, TypeScript and Next compilation. The populated comparison browser suite passes **15 five-language × 390/768/1024px journeys** plus desktop transitions, 200 groups / 201 material rows, all 40 group pages, six legacy exact-row pages, last/secondary-change search and evidence focus, and 320px/doubled-root-text reflow. It additionally asserts localized `199bis → 200ter` article headings and searching those headings. All API responses are synthetic; no inference, email or saved-data mutation.
- The parser's stored hierarchy can still contain extraction mistakes; attachment and schema validation do not prove that an official legal label was correctly recognized. Independent source verification, full design/density consolidation, other-browser/physical-device and participant acceptance remain open under HL-096. No production deployment, performance-capacity certification or claim that the whole task is complete.


## Bounded material-change reading — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-096-material-pages`. Comparison Material first now renders five saved groups at a time with direct page choice, full saved-text search, labelled before/after excerpts and expandable technical IDs. A map replaces repeated whole-array ID lookups. Exact evidence remains in the complete deterministic pager; returning preserves the material query/page. An old comparison without group metadata now exposes its exact-row pager even in Material first, fixing inaccessible rows after the first 40. [Contract and remaining scope](MATERIAL_READING.md).
- Production build passes **1,894 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The initial catalog audit rejected unused dynamic classification keys; explicit localized classification mappings fix that without weakening the audit.
- Mandatory Chromium fixture: **200 groups / 201 material rows**, including a two-change final group, long saved text and an ambiguous warning. Browser journeys cover every group exactly once across 40 pages, search beyond the initial page and within a non-leading exact change, empty/clear search, first/secondary exact-evidence focus, return with query/page preserved, localized labels and 44px targets. Older ungrouped data remains complete across six exact-row pages. Existing **15 five-language × 390/768/1024px overlay journeys**, desktop transitions, Ask draft retention, cited evidence and saved-answer monitoring also run with intercepted APIs; no live AI, email or database mutation.
- The first combined 320px/doubled-root-text reflow test exposed nonwrapping buttons. Reading controls now wrap with bounded width, and pagination headings account for sticky navigation. The final browser suite requires no material-reader horizontal overflow and an uncovered focused heading. Screenshots in ignored `test-results/comparison-overlays/` were inspected. This is a scoped reflow check, not full-browser 400%/physical-device/manual accessibility certification.
- Existing contrast regression passes **8,060 rendered samples**, minimum **4.79:1**, across default/selected/hover/focus/error states at four widths. It is not a complete WCAG audit or proof of user reading comfort. Full response/index size, exceptionally large single-group selectors, authoritative legal-unit headings, wider visual-token/density consolidation, Firefox/Safari and participant acceptance remain open. HL-096 is **IN PROGRESS**, not complete. No production deployment or saved-evidence rewriting.


## Personal digest quiet hours — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-079-digest-quiet-hours`. Optional local quiet start/end in the existing schedule JSON, validated as a distinct complete pair. Old-client omission preserves policy; quiet-only updates preserve cadence. Final dispatch checks actual time twice, including after rendering. The same durable job/outbox is deferred without consuming a failure attempt; duplicate early claims cannot start it. History labels quiet waits. [Contract and limitations](DIGESTS.md#optional-recipient-quiet-hours-hl-079-6-september-2026).
- **81 API regressions pass in 76.74 seconds** across quiet hours, local clock, digests, bounded preview, resumable preparation and jobs. After adding explicit HTTP/CSRF/atomic-save coverage, the dedicated quiet suite has **17 passing tests in 7.09 seconds**. Boundaries include overnight/daytime, start inclusive/end exclusive, missing DST endpoints and both repeated-hour occurrences. Durable scenarios retain the completed checkpoint, update a pre-existing pending outbox, reject early dispatch/claim, send once at wake, honor unsubscribe and defer if rendering crosses into quiet time. Direct unqueued delivery cannot bypass the policy. All mail intercepted; no model calls. An initial test collection/default-argument error and one nonexistent test filename were corrected before these passing runs.
- Four separately labelled **PostgreSQL 16.14** scratch suites pass: `digest-quiet-resume`, `digest-quiet-unsubscribe`, `digest-quiet-boundary`, `digest-quiet-save`. Temporary loopback-only databases started empty and all four containers were removed. No working database or production data touched.
- Production build passes **1,873 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Real production-browser suite passes **10 journeys (five languages × 390/1440px)**: quiet fields survive preview paging and explicit save, localized waiting history, existing navigation/focus/error recovery and touch targets. Every API intercepted; no real email or inference. Native-speaker and user acceptance remain open.
- No new schema migration, deployment or external messages. Pending jobs retain their existing wake-up after a preference edit, then reevaluate current quiet hours and admission. Accepted SMTP mail cannot be recalled, and queue delay remains possible. This does not finish HL-079: topic inclusion, broader filters, organization limits and measured notification noise remain open.


## Personal digest local clock — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-079-digest-local-schedule`. Adds an explicit optional IANA timezone/local time to personal daily/weekly digests, with UTC persistence and local calendar recurrence. Nonexistent times move to the first valid minute and ambiguous times use the first occurrence; legacy elapsed schedules remain unchanged until explicitly changed. Beat checks due preferences every minute using the existing bounded scheduler and durable deduplication. Queue delays, manual Send now and already queued periods are not rewritten or presented as quiet-hours guarantees. [Contract](DIGESTS.md#optional-local-delivery-clock-hl-079).
- **49 API regressions pass in 50.25 seconds** across clock scheduling, digest delivery, preview and resume. The final dedicated suite passes **21 cases in 4.90 seconds**: Zurich spring/fall and weekly recurrence, nonexistent/repeated hours, Tokyo date boundary, Samoa skipped date, invalid and host-specific zones/times, viewer CSRF/forged fields, stable retries, omitted-field compatibility, clear-to-interval behavior, unchanged interval due date when only dormant timezone metadata changes, scheduler idempotence and additive migration round-trip. All fixtures synthetic; no real SMTP/provider calls. The first broad command referenced a nonexistent singular preview-test filename and did not collect tests; the passing rerun uses the actual plural test file.
- Two **PostgreSQL 16.14** scratch suites pass: local-clock save/retry/legacy omission/clear, and populated migration round-trip preserving existing opt-in/filter/due-time values. Each ran in its own labelled ephemeral loopback database; both containers removed. Migration `ff72eb61754d` only adds nullable schedule JSON; existing production data were not migrated.
- Production build passes **1,865 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Production browser journeys cover DE/FR/IT/RM/EN at 390/1440px with a chosen local clock retained across paging and saved explicitly, no background preference writes, bounded preview navigation/recovery and no horizontal overflow. All APIs intercepted; no email or inference. Native-language and real-user acceptance remain open.
- Ruff and whitespace checks pass. Topic inclusion, expanded notification filters, organization limits, quiet hours at dispatch and notification-noise measurement remain open; HL-079 is in progress, not complete. Code publication is not production deployment or permission to send messages.


## Current-topic feed pages — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-feed-topic-pages`. Today returns five current topics plus explicit continuation. SQL batches candidate selection across events, validates groups of at most 100 saved matches, and continues past stale evidence/current rejections instead of silently dropping valid later matches. The detail endpoint accepts at most 50 matches per page, binds event/principal/organization/list type, and rechecks admission and visibility. Topic and watch navigation share one scoped asynchronous hook. [Contract and limits](INTEREST_FEED.md#current-topic-pages-6-september-2026).
- **49 API regressions pass in 63.97 seconds**, two PostgreSQL-only review cases skipped in SQLite, across topic paging, existing feed/watch pages and shared topic reviews. The six dedicated new cases separately pass in 7.18 seconds. A 201-topic fixture returns all matching IDs exactly once through continuation, while the first preview describes only 20 candidates. A 131-topic fixture skips 120 stale and one currently rejected record, then returns every remaining current match; invalidating those removes the topic-only card. Other checks reject cross-event/watch-list/principal/org cursors, revoked admission and invalid page inputs. No live AI calls. The initial fixture copied a unique topic idempotency key; corrected fixtures use distinct keys before the passing runs.
- Two **PostgreSQL 16.14** suites pass: `feed-topic-sparse` and `feed-topic-scope`, each in a separately labelled empty ephemeral loopback database. Both task containers removed. Working and production data untouched.
- Build passes **1,860 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Production-browser checks pass **10 five-language × 390/1440px journeys** covering both watch and topic page failures, retained old content, retry, 20-row next page, exact topic-review links, previous page, no full document reload and no horizontal overflow. All API requests intercepted. Native-language/user acceptance remains open.
- Ruff and whitespace checks pass. Total validation work still scales with stale candidates; SQL window ranking and large individual evidence fields are not constant-cost. Related-law fan-out and intended-host concurrency remain open. No inference, emails, migration or deployment, and no claim that HL-076/099 are complete.


## Direct-watch feed pages — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-feed-watch-pages`. Feed cards return five direct-watch links and explicit continuation. One SQL window query bounds scalar preview rows per selected work; the new endpoint uses stable watch-ID keyset pages (20 default, 50 maximum), organization/principal/event-bound cursors and repeated work/law/mapping/watch visibility checks. The UI replaces pages in place, retains the current list during errors, and supports retry/back/preview. [Contract and limits](INTEREST_FEED.md#direct-watch-pages-6-september-2026).
- **35 API regressions pass in 44.89 seconds** across watch paging, existing interest feed and evidence context. The final seven dedicated cases additionally pass in **7.57 seconds**, including cross-event cursor rejection and complete watch revocation. A 1,001-watch fixture traverses every link exactly once, with at most 50 per page, no full Law/DocumentWatch/LegacyDocumentMapping ORM hydration, and no model calls. Other cases cover renamed/paused/late/foreign watches, private mappings/laws/work, principal scope and invalid inputs. The first test run exposed a missing required reader constructor configuration; it was fixed before the passing runs.
- Two **PostgreSQL 16.14** scratch suites pass: `feed-watch-pages` and `feed-watch-scope`. Each used its own empty, labelled ephemeral loopback container; both were removed. Working and production data were untouched.
- Production build passes **1,855 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The production browser suite passes **10 five-language × 390/1440px journeys**, including synthetic watch-page 503, retained preview, retry to 20 links, back, no full-document reload and no horizontal overflow. All application API requests intercepted; the mobile screenshot was inspected. These tests do not establish native-language acceptance or independent usability.
- Ruff and whitespace checks pass. Database window-ranking cost, topic-match/related-law fan-out and intended-host concurrency gates remain open. This is bounded direct-watch transfer and Python hydration, not a claim that the entire feed is bounded or that HL-099/076 are complete. No inference, mail, migration or deployment.

## Server-paged saved evidence — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-099-evidence-pages`. Native/legacy viewers use separate page resources instead of transferring the whole document. Exact saved citations resolve in SQL; pages contain at most 50 saved passages or 16,000 unnumbered-text characters. Existing access policies, full APIs and originals remain intact. Current text stays visible during delayed/failed next-page loads. [Contract and limits](EVIDENCE_PAGES.md).
- **48 API regressions pass in 71.86 seconds**, one PostgreSQL-only skip, across new evidence pages, corpus evidence, workflow and personal display milestones. The final optimized JSON query additionally passes all six new paging cases in 7.29 seconds. These seed 10,001 passages plus large full text for both storage models, check exact late targets and last-page counts, assert zero full ORM version hydration and a small selected response, reconstruct all Unicode characters, and reject invalid parameters/private evidence. Tests use synthetic saved documents; no live model calls.
- Three **PostgreSQL 16.14** suites pass: legacy/native JSON paging and unnumbered Unicode text paging. Both large-passage suites pass again after replacing repeated per-index extraction with one SQL JSON expansion. All five separately created, labelled, ephemeral loopback databases were removed; no working/production data or migration changes.
- Production build passes **1,850 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Production browser checks cover ten five-locale 390/1440px native journeys plus legacy smoke: direct citations, actual server page requests, no full-version fetches, retained old text during a failed next request, retry/back, PDF links, missing/sample/empty states and personal display boundaries. The final harness also reconstructs multi-page unnumbered text. All APIs intercepted. These checks do not establish native-language acceptance, physical-device performance or target-host concurrency.
- Ruff/whitespace checks pass. Database-side JSON parsing still scales with document size, and passage count is not a strict byte cap for unusually large individual passages. The independent capacity and broader HL-099 acceptance gates remain open; no production deployment.


## Personal source selection review — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-073-source-review`. The Sources section provides an explicit private save of the displayed catalogue/package revisions and enabled flags, separate from source activation/request controls. The guide retains the latest acknowledged selection and first/latest dates and identifies changed selection. A separate one-row-per-principal table avoids inventing an onboarding intent or completed state. [Contract](PERSONAL_ONBOARDING.md).
- **34 API tests pass in 39.36 seconds** across source review, source packages, recorded milestones and personal onboarding; two PostgreSQL-only cases explicitly skipped in SQLite. Cases include passive entry, empty choice without jobs/subscriptions/requests, stable retry dates, changed catalogue/definition/subscription/removal, duplicate rejection without overwrite, actual viewer CSRF/identity protections, cross-organization reads even in a privileged session, and additive migration down/up preserving prior intent. No live source/model/mail calls.
- **PostgreSQL 16.14** scratch suites `source-review`, `source-review-scope` and `source-review-migration` pass on three separately created, empty, labelled, ephemeral, loopback-only databases. All task containers were removed. Working and production data were untouched. These tests check ordinary idempotence and migration/scope; no new simultaneous-write stress result is claimed for this slice.
- Production build passes **1,849 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The extended production-browser suite passes **30 five-language × 390/1280/1440px × admin/viewer journeys** through document/topic/package choice, explicit preview/activation/request boundaries, personal review conflict/retry, disabled duplicate save, JSONB-style reordered object fields, changed revision and return to the guide. All APIs are intercepted synthetic responses. Final copy labels the action as saving rather than promising navigation; the mobile layout was inspected. These checks do not establish native-language or independent user acceptance.
- Ruff and whitespace checks pass. HL-073 remains in progress for further recovery/first-value integration and independent observation. Code publication does not authorize production deployment or migrations.


## Personal recorded-action milestones — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-073-recorded-milestones`. Adds first personal interest, notification-choice and saved-text-display timestamps. Topic/document and preference milestones share their existing transaction; passive GETs do not write progress. The viewer reports only visible non-sample text; the server checks current access and scalar text/metadata again. Three unique kinds bound storage. [Contract and remaining scope](PERSONAL_ONBOARDING.md).
- **56 API regressions passed in 66.21 seconds** across milestones, personal onboarding, native evidence, digest preview and topic matching; two PostgreSQL-only cases explicitly skipped under SQLite. The final strengthened milestone suite separately passes **12 tests in 14.46 seconds**, one PostgreSQL-only skip, including explicit `synthetic: false` and whitespace-only text. Prior auth/workflow/personal regression passed 28 tests, one PostgreSQL-only skip. Checks cover rollback of topic/job/progress together, duplicate/replay stability, scope/revocation, demo and empty rejection, viewer preference opt-out, CSRF/forged fields, and migration preserving topic/intent. Initial test errors used the wrong digest HTTP verb and an unmapped version-owner attribute; fixtures now use the actual PUT contract and native work ownership.
- Three **PostgreSQL 16.14** scratch suites pass: `milestone-native`, `milestone-migration`, `milestone-race`. Native evidence was repeated with explicit false synthetic metadata, covering PostgreSQL JSON text extraction as well as SQLite booleans. Migration down/up preserves existing topic and intent; concurrent writes converge on one first-action row. Each database was empty, task-labelled, ephemeral and loopback-only; all four task containers were removed. No working or production migration was applied.
- Production build passes **1,840 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. **20 onboarding browser journeys** cover five languages, 390/1440px and admin/viewer roles, including three personal dates and passive reopening. **10 native-evidence journeys plus a legacy-viewer smoke** cover visible text, controlled background visibility, missing passage targets, sample/empty text exclusions, unnumbered text and no pagination repeat. Visibility state is deliberately controlled in the headless harness; viewport intersection is real. These are functional tests, not independent human-reading or native-language acceptance. All API traffic is intercepted; no source, model or mail calls.
- Ruff and whitespace checks pass. Existing tooling warnings remain. HL-073 stays open for explicit source-package review, further first-value integration and independent real-user evidence. No production deployment is included.


## Actionable empty-feed recovery — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-073-empty-feed-recovery`. Today now separates nine empty-state reasons and uses existing recovery flows. A passive, bounded `/api/interest-feed/readiness` projection reads current active topics, current-revision/evaluator saved history, visible direct watches and enabled source status; it does not claim live freshness or quiet periods. [Scope and remaining work](EMPTY_FEED_RECOVERY.md).
- **36 API regressions pass in 39.72 seconds** across readiness, interest feed and source coverage. The final eight readiness cases additionally pass after strengthening same-session cross-organization and 21-package limit assertions. Cases include failed/backfilling sources, no model/fetch/job side effects, 24 topics, current versus old revision/evaluator, legacy incomplete success, exact saved cutoff/processed-through/counters, private diagnostics excluded and no ORM payload hydration. Initial failures exposed incorrect test seeds (required slug/status/idempotency fields, the document owner field and the existing nested topic response); fixtures were corrected rather than relaxing product constraints.
- Three **PostgreSQL 16.14** suites pass: `feed-readiness-history`, `feed-readiness-bounds`, `feed-readiness-sources`; bounds/source suites were repeated after final test strengthening and the actual `backfilling` enum correction. Disposable labelled containers used empty ephemeral databases and dynamic loopback ports and were removed afterwards. No migration or working/production data change.
- Production build passes **1,832 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The new production-browser suite covers **90 five-language × mobile/desktop state combinations**: nine distinct reasons, exact action links, saved-history details, layout bounds and no mutations. The existing populated-feed suite passes **10 journeys**, including private read state without reload, saved evidence links, period filters and sparse continuation. APIs were intercepted; these are functional/layout checks, not native-language or real-user acceptance. Mobile recovery screenshot inspected.
- Ruff and whitespace checks pass; existing tooling warnings remain. HL-073/094 stay open for the explicitly documented broader coverage, first-value milestones and independent gates.

## Personal getting-started guide — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-073-personal-onboarding`. Personal topic/law/explore intent and deferral now survive returning and login, independently of shared watches. The workspace menu exposes the guide; role-specific source/interest/notification/evidence links reuse existing flows without activating anything on entry. HL-073 remains **IN PROGRESS**, not completed.
- **33 API tests pass in 52.98 seconds** across personal onboarding, authentication, workflow and source packages; one SQLite run explicitly skips the PostgreSQL-only race. Cases cover passive reads, active/paused watches, same-action retries, defer/resume, forged identity fields, CSRF and viewer access, two users in the same organization, workspace switching, logout/login, and additive migration preservation. No working database or real source/model/mail calls are used.
- Three separately created, empty **PostgreSQL 16.14** scratch databases pass `onboarding-state`, `onboarding-migration` and `onboarding-race`: unique personal records and stable retry timestamps, migration down/up retaining existing watch data, and two concurrent first-use requests converging on one record. Containers use labelled task ownership, ephemeral storage and dynamic loopback ports; all are removed afterwards. Migration is not applied to production.
- Final production build passes **1,804 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. **20 new production-browser journeys** cover five locales × 390/1440px × admin/viewer roles, passive entry, changed organization availability, all three choices, defer/reopen, failed-save retry and exactly one personal PATCH without hidden shared writes. The initial browser fixture omitted the nested health `apertus.configured` contract and failed before rendering; correcting that fixture resolved the failure. Final runs use the built page with every API intercepted. Mobile and desktop screenshots were inspected; cards align consistently after a small layout adjustment.
- Existing shell browser regression also passes **30 populated language/role/mobile-width journeys**, including keyboard skip/focus containment, nested Escape, role-filtered links, restored focus and desktop-resize cleanup: **50 production-browser journeys total** for this slice.
- Ruff and whitespace checks pass. Existing test-client/module-format warnings remain. These automated/synthetic checks do not prove native-language quality, actual evidence-opening milestones, first useful update in five minutes, live source freshness or research acceptance. See [personal onboarding](PERSONAL_ONBOARDING.md) for the remaining boundaries.

## Explicit monitoring-type choice — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-monitoring-choice`. Production build passes **1,785 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The shared chooser is available for resolved contexts and blank topic setup. It reuses existing document, topic and package workflows rather than adding write endpoints.
- The new `check:monitoring-choices:browser` suite passes **30 five-locale × 390/1280/1440px × admin/viewer journeys**. Tests assert no writes/fetch on entry or opening the document dialog; explicit preview before enabled save; exact source URL/name/provider and direct created-document navigation; reuse of an existing paused watch; viewer document restriction; cancelled/accepted leave of a dirty topic; focus below the sticky header after delayed source-package loading; and explicit admin activation versus viewer package request. All APIs are intercepted synthetic contracts; the test does not contact sources, invoke models or change working data. Wrapped buttons avoid narrow three-column clipping. Mobile and 1280px screenshots were inspected.
- Existing contextual topic (20), personal Marvin entry (20), and comparison overlay/history (15) browser journeys also pass after the final build: **85 browser journeys total**. Existing backend workflow, source-package and monitoring-context regressions pass **31 API tests in 52.73 seconds**. No server/schema code changed, so no new PostgreSQL or migration claim is made. Whitespace checks pass; existing tooling warnings remain.
- No production deployment, real source fetch/model call, outgoing message or data migration. The document URL is editable and generic private messages do not prefill a document name without a valid source URL. The existing document duplicate/preview/save policy remains unchanged; these frontend tests do not prove a new cross-kind transaction or concurrent uniqueness guarantee. Package scope/cadence comes from its existing saved catalogue and does not promise complete/live coverage. Richer uniform preview metadata, broader duplicate semantics, persistent origin handoff and independent/native/physical-device acceptance remain open; HL-077 stays **IN PROGRESS**.


## Personal Marvin messages as monitoring drafts — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-marvin-monitoring`. **60 affected API tests pass in 75.63 seconds** across personal-message context, saved cited-answer context, existing monitoring context and assistant contracts. Fifteen new cases cover a real deterministic saved user-message flow, unchanged chat/topic/job counts and timestamps, no model calls, separate user/organization guards even in a privileged session, assistant-role rejection, absent/expired/duplicate/blank/oversized/invalid-date/over-retention messages, current document owner and watch resolution, identifier limits, and an authenticated viewer who can inspect their own message but cannot activate a shared topic or inspect a colleague's message.
- Two isolated **PostgreSQL 16.14** suites pass: `assistant-context` and `assistant-context-private`. Temporary labelled databases were empty before startup, used ephemeral storage and dynamic loopback ports, and were removed afterwards. No working database, real inference or outgoing message was used.
- Production build passes **1,773 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. The new `check:marvin:monitor:browser` suite passes **20 five-locale × 390/1440px × admin/viewer journeys**: local runtime stopped, actions only on saved user messages, companion closes on navigation, identifiers without prose in URLs, personal-sharing notice, explicit goal copy with empty terms, different-message cache isolation, and both cancelled and accepted replacement of an existing draft. Initial browser verification incorrectly expected dirty-draft controls to be disabled; the existing workflow intentionally asks for discard confirmation. The final test exercises that real accept/cancel boundary. Thirteen existing companion/delivery checks also pass.
- The unchanged contextual-monitoring suite passes twenty localized role/mobile journeys and the comparison suite passes fifteen localized overlay journeys after this change. All browser APIs are intercepted synthetic contracts, not live deployed end-to-end evidence. Mobile/desktop Marvin action screenshots were inspected. Ruff and whitespace checks pass; existing Starlette/Node tooling warnings remain. Final changes after the build are documentation, test/script registration and indentation only.
- No schema migration or deployment. The server reads the existing bounded conversation message JSON to locate one user message and returns only that selected text/date plus authorized context; it does not serialize assistant replies, drafts or handoffs. Existing Marvin route-conversation opening behavior remains unchanged. The endpoint is not permanent chat history, automatic intent detection, a legal answer validator or a common law/topic/pack chooser. A generic message has no invented evidence/back-to-chat link. Shared topic save remains explicit and admin-authorized. Native-language, physical-device and independent acceptance remain open; HL-077 stays **IN PROGRESS**.


## Saved cited answers as monitoring context — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-saved-answer-monitoring`. **30 affected API tests pass in 51.65 seconds**, including thirteen new cases for actual synthetic Ask-to-context, unchanged inference/record/use counts, scalar-only reads, failed/pending/unsupported/uncited/malformed records, unrelated citation versions and explicit answer/comparison/law/version ownership even in privileged sessions. Existing context/history regressions remain green. Initial verification exposed SQLite returning integer 1 for JSON true; the eligibility guard now accepts that representation and PostgreSQL booleans while rejecting truthy strings.
- Two isolated **PostgreSQL 16.14** suites pass: `answer-context` and `answer-context-private`. Both generate a saved answer using the scripted model before reading context; context itself makes no inference or writes. Empty, task-labelled ephemeral containers used dynamic loopback ports and were removed. No working database or provider was used.
- Production build passes **1,771 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Twenty contextual-monitoring browser journeys (five locales × mobile/desktop × admin/viewer) pass saved-question goal copying together with existing draft, source-readiness, duplicate-review and explicit activation behavior. Fifteen final comparison browser journeys pass saved-answer eligibility in Ask/history, actual navigation from both entry points to the original question, no question text in URLs, no implicit form copy/activation, and existing focus/modal/citation/draft behavior. All browser APIs are synthetic/intercepted, not a live deployed end-to-end test. Final 390/1024px destination screenshots were inspected; there is no horizontal overflow. Native-language, physical-device and independent usability acceptance remain open.
- Ruff and whitespace checks pass. Existing tooling warnings remain. No migration, production deployment, real inference, source fetch or outgoing message. The destination preserves the question and comparison context but does not copy answer prose into search rules or claim to revalidate legal meaning. The return link opens the saved comparison's Ask panel, not a specific old turn beyond the existing history window. Global-assistant entries, common law/topic/pack choice, durable saved-topic origin and broader HL-077 acceptance remain unfinished.


## Topic source readiness before activation — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-topic-source-readiness`. **49 affected API tests pass in 66.99 seconds** across source readiness, same-rule warnings, preview/scorer parity, source-pack subscriptions and contextual creation. Six new readiness cases cover missing schedules/no writes, custom saved intervals and permitted hours, past scheduled attempts, old successes versus degraded/partial current records, paused scheduling, privileged organization isolation, unknown streams, twenty-pack bound and four scalar queries across one/all five packs (23 known streams). Queries do not hydrate diagnostic messages, policies, cursor bodies or full histories; no model calls occur.
- Four **PostgreSQL 16.14** scratch suites pass: `topic-coverage-empty`, `topic-coverage-state`, `topic-coverage-scope`, `topic-coverage-bounds`. Each uses a separate empty task-labelled container with ephemeral storage and a dynamic loopback port; all were removed. The missing-schedule fixture deletes only its synthetic Fedlex schedule rows after startup seeding. No working database or services are changed.
- Final production build passes **1,768 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Twenty final browser journeys (five locales × mobile/desktop × admin/viewer) pass source readiness visibility before save, attention summaries, inactive subscriptions, disclosure controls, custom 120-minute intervals, permitted hours, explicit Zurich times, and existing contextual/duplicate-review/draft recovery behavior. The final existing editor suite also passes ten five-locale mobile/desktop journeys, forty discard decisions, retry-key recovery, storage denial, navigation and account/organization isolation. All APIs are intercepted; this is separate frontend/backend contract evidence, not a live deployed end-to-end or independent usability trial.
- Initial API development caught a fixture assumption: startup seeds schedules, so the no-schedule case must explicitly remove its own synthetic schedule rows. Import-order lint issues were corrected. Visual review found that the date formatter default omitted hours; timestamps now include localized Zurich time, verified in all browser locales. Final 390/1440px screenshots were inspected; detailed streams remain collapsed by pack and use two columns only on wide screens. Ruff and whitespace pass. The existing Starlette warning remains.
- No migration, deployment, live source/model request or outgoing message. This snapshot reports recorded operations, not live liveness, guaranteed notification times, complete source/language coverage or predicted volume. No silent subscription activation. The bounded query/row checks are not target-host throughput proof; mature run-history costs and independent/native-language acceptance remain open. HL-077 remains **IN PROGRESS**. See `docs/MONITORING_TOPICS.md`.


## Same-rule monitoring topic warnings — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-topic-duplicate-review`. **44 affected API tests pass in 63.48 seconds** across duplicate review, topic lifecycle, scorer/preview parity and contextual creation. Fifteen duplicate cases verify case/order-insensitive rules with distinct descriptions, every matching filter, concept-versus-synonym roles, current revision only, active/paused/archived states, self exclusion, both organization owners even in privileged sessions, actual 501-topic bounds, scalar metadata without goal/history hydration, no writes/inference and authenticated viewer read-only behavior.
- Three **PostgreSQL 16.14** scratch suites pass: `topic-duplicates`, `topic-duplicates-scope`, `topic-duplicates-bounds`. Each runs against a separate empty task-labelled container with ephemeral storage and a dynamic loopback port. All three were removed; working services and data remain unchanged. Import-order lint fixes after these runs do not change query behavior.
- Final production build passes **1,747 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. Twenty final browser journeys cover five locales × 390/1440px × admin/viewer: existing-topic links/targets, paused status, bounded-check disclosure, no automatic save, required pointer/keyboard acknowledgement, reset after a repeated preview, and viewer warning without activation. Context copying, source scope, draft recovery and missing-context regressions remain covered. All APIs are intercepted with synthetic data; no deployed end-to-end or independent-user acceptance is claimed. Mobile and desktop screenshots were inspected. The final existing topic-editor suite also passes ten localized mobile/desktop journeys, forty discard decisions, idempotent retry, reload recovery, storage denial, navigation and account/organization isolation. The authenticated viewer test passes again after import sorting.
- Development checks caught a test using POST instead of the existing PATCH status endpoint and unsorted test imports; both were corrected. The initial sandboxed browser attempt could not reach its temporary debugging port; the same isolated test passed with local process/network access. Ruff and whitespace checks pass. The pre-existing Starlette deprecation warning remains.
- No migration, deployment, real source/model call or outgoing message. The warning checks at most 500 recently updated organization topics and shows ten matches, with independent completeness flags. It is a point-in-time advisory, not semantic equivalence, server-side duplicate rejection or concurrency locking. Existing intentional duplicate saves/idempotency remain supported. HL-077 remains **IN PROGRESS**, and native-language/independent usability/target-capacity gates remain open. See `docs/MONITORING_TOPICS.md`.


## Contextual topic setup and personal viewer drafts — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-077-contextual-topic-entry`. **35 affected API tests pass in 52.97 seconds** across contextual source resolution, topic lifecycle, scorer/preview parity and organization access. Ten context cases verify native source/evidence identity, metadata-only scalar reads, existing paused watches, private event/law/comparison rejection including privileged sessions, unknown/oversized inputs, publisher fallback, bounded preview without writes and idempotent explicit activation. The authenticated viewer regression now proves preview is permitted while AI drafts, shared topic creation and history scans remain forbidden.
- Final **PostgreSQL 16.14** scratch suites `monitoring-context` and `monitoring-context-activate` pass against separate empty databases. They execute the actual context query and preview/activation/retry API. Task-labelled containers use ephemeral memory storage and dynamically assigned loopback ports; all were removed. Existing services and working data are unchanged.
- Final production build passes **1,741 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next compilation. **Twenty new browser journeys** (five locales × 390/1440px × admin/viewer) verify explicit context copy, existing-watch/evidence links, required user-chosen concepts, enabled source defaults, preview without activation, explicit save/direct link, viewer reload/restore without shared writes, unavailable context and no horizontal overflow. Final ten existing topic-editor journeys retain their draft, discard, retry-key, account/organization isolation, storage denial and recovery checks. Additional existing suites pass twenty registry, ten feed and fifteen comparison journeys plus inbox next/back/sparse/error navigation across four widths. All browser APIs are intercepted; backend and browser contracts are tested separately, not as an unmocked deployed end-to-end trial.
- Screenshots inspected at 390 and 1440px. The first mobile view had an overly long context introduction; it was shortened and supporting explanation moved into disclosure. An intermittent new-test click raced the editor's animation-frame focus/scroll; the browser test now waits for frames and checks pointer hit targets, then passes. Initial build checks caught an invalid localization key and a missing optional registry row type; API fixture setup initially chose an existing legacy version instead of the newly ingested native version. All were corrected before final passing checks. Ruff/whitespace pass; pre-existing Starlette/Node tooling warnings remain.
- No migration, deployment, real source/model request, source subscription or outgoing message. Personal viewer drafts remain scoped browser-tab storage with explicit 24-hour/closing-tab limits, not shared monitoring or cross-device state. Read-only preview has session/CSRF enforcement and a 30-per-five-minute rate policy. HL-077 stays **IN PROGRESS**: unified new document/pack choices, equivalent-topic warnings, cadence/degradation context and per-answer/assistant entries remain separate work; independent/native-language/physical-device gates are not claimed. See `docs/MONITORING_CONTEXT.md`.


## Saved evidence across registry, inbox and Today — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-076-evidence-navigation`. **88 affected API tests pass in 109.12 seconds** across native evidence, new navigation guards, feed, inbox/context, registry and connector ingestion. Nine new cases cover exact native/legacy links, an unrelated newer saved version, revoked delivery, private work/legacy Version/Law, wrong-work mapping, no full version-body hydration, page-only registry lookup and bounded/empty scalar batches. Existing one-versus-fifty-event inbox query bound remains green. No inference is performed while reading these surfaces.
- Four **PostgreSQL 16.14** suites pass: `evidence-navigation`, `evidence-navigation-legacy`, `evidence-navigation-private`, `evidence-navigation-revoked`. Each executes the actual registry, Impact inbox, Today and viewer API against a separate empty scratch database. Task-labelled containers used ephemeral memory storage and dynamic loopback ports; all were removed. No working service, data or schema was changed.
- Existing production-browser suites pass with new native-link assertions: **20 registry journeys** (five locales × mobile/desktop × Monitoring/Discover), inbox next/back/sparse/deep-link/error navigation with four viewport widths, and **ten five-locale native-viewer journeys plus the legacy deep-link smoke**. APIs are intercepted with synthetic fixtures. Application frontend files are unchanged; these checks reuse the preceding successfully built production frontend. This is separate backend-contract and browser-rendering evidence, not an unmocked deployed end-to-end trial or independent usability study.
- Development tests caught missing/unique fixture version fields and an invalid fixture that made the monitored target itself private; fixtures now use a separate private source binding to test link authorization without damaging target invariants. Final Ruff and whitespace checks pass. The existing Starlette test-client warning remains.
- No migration, deployment, provider call, real source request or outgoing message. Shared SQL guards now govern links and actual native reads. Registry link lookup is page-bounded, but other registry corpus/filter reads are still unbounded and must not be presented as 100-user capacity proof. Assistant/timeline link propagation, server-side evidence paging and remaining HL-076/099 and external acceptance requirements are not marked complete.


## Native saved-source evidence viewer — 6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-076-native-evidence`. **60 affected API regressions pass in 68.76 seconds** across native evidence, feed/context, connectors and organization access. The fixture runs the actual connector persistence/extraction pipeline using synthetic responses, then reads original bytes and saved passages with zero legacy Law/Version copies or model calls. Cases cover private work/organization/revoked or wrong-work admission, relation-only delivery/removal, paused watches, foreign legacy bindings, synthetic attribution, missing/invalid artifact keys, text-only/metadata-only records and metadata-only hydration for file responses.
- Three **PostgreSQL 16.14** scratch suites pass: `native-evidence`, `native-evidence-private`, `native-evidence-relation`. They exercise the correlated authorization queries, native connector storage/read/download roundtrip and relation-delivery revocation. Isolated task-labelled containers use ephemeral memory storage and dynamic loopback ports; all were removed. Existing services/databases were not changed.
- Production build passes **1,730 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next. The new browser suite passes **ten five-locale 390/1440px journeys** with a cited passage on page two, PDF page links, next/back, missing-passage recovery, missing original, unnumbered text/metadata-only states and unsafe-source omission; the original legacy evidence viewer also passes its deep-link smoke. The existing ten Today browser journeys remain green. All browser APIs are intercepted; no runtime exceptions or horizontal overflow. Screenshots inspected locally; this is not independent/native-language/physical-mobile research.
- Initial development checks caught a query-parenthesis syntax error and a test assertion using the wrong DomainError attribute. Both were corrected before the passing suite. Ruff and whitespace checks pass; pre-existing Starlette/Node tooling warnings remain. No migration, production deployment, real source request, inference or outgoing message.
- Native originals are served with sandbox CSP/nosniff; HTML downloads as text/plain. The viewer never fabricates passage IDs, legal dates, available files or fresh-source claims. Per-document response/client pagination remains the existing design, not a large-document/concurrency benchmark. Native-link propagation to older surfaces, server-side passage paging, target-host and independent acceptance gates remain open. See `docs/NATIVE_EVIDENCE.md`; HL-076 remains **IN PROGRESS**.


## Daily-feed evidence context and exact event links — 5–6 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-076-feed-evidence`. **65 affected API tests pass in 84.00 seconds**, covering new evidence/deep links plus feed, shared topic reviews, inbox context/navigation and topic validity. Two unchanged review row-lock cases skip SQLite (their PostgreSQL proof remains in the preceding record). Seven new cases verify exact older-event lookup, cursor/event scope, revoked/foreign admission, recorded rather than inferred jurisdiction, event-only year-precision dates with provenance URLs, and exact version links excluding foreign version/law, wrong-work expression and absent viewer mapping. Large source text/passages are not materialized by link lookup; model calls remain zero.
- **PostgreSQL 16.14** suites `feed-evidence`, `feed-private-evidence`, `feed-event-link` pass in three separate empty databases, including private-version exclusion and a direct older-event link with revoked admission. Task-labelled scratch containers used memory storage and dynamic loopback ports and were removed. No working data, existing services, production deployment or messages changed.
- Final `npm run build` passes **1,726 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation checks**, TypeScript and Next. The expanded production-browser suite passes **10 journeys across five locales and 390/1440px**, opening recorded scope, year-precision date/source link and saved evidence; direct event/return-to-all links; personal state without reload; next/back, sparse pages and cursor recovery. APIs are entirely synthetic/intercepted. No browser exceptions or horizontal overflow. Expanded mobile/desktop screenshots were inspected; known provenance methods now have readable localized labels and the evidence disclosure has a sticky-header scroll margin. No native-language, physical-device or independent user-research claim.
- Initial new-test failures came from attempting to create a second unique corpus mapping for an already mapped legacy version, then a missing test import; the fixture now reuses its actual saved mapping. Localization validation caught dynamic-only date keys, and TypeScript caught an optional status value; explicit date/precision labels and null fallback fix those issues. Final Ruff and whitespace checks pass. Existing Starlette/Node tooling warnings remain.
- No schema migration or new inference. Old feed cursors intentionally fail scope validation and offer first-page recovery. This is a currently authorized relevance link, not permanent access to revoked/stale interests. Work metadata remains current; health is labelled as a record at detection, not a live check. Native-only corpus versions without a legacy evidence-viewer mapping remain explicitly unlinked. HL-076 stays **IN PROGRESS** for the remaining coverage, normalization, fan-out, AI briefing, notification and independent acceptance work; see `docs/INTEREST_FEED.md`.


## Shared topic relevance review — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-076-topic-reviews`. **87 affected API regressions pass in 113.96 seconds** across reviews, organization/viewer access, daily feed, topic matching/validity/live/history. The two row-lock cases explicitly skip SQLite and pass separately on PostgreSQL. Coverage includes reject/restore with unchanged private reading state, evidence-bound append-only history, idempotency and payload conflicts, changed evidence/plan/paused/expired/colleague decisions, revoked admission/foreign organization exclusion, actor metadata/payload rejection, complete cursor pages and retention through operational cleanup. No AI calls.
- Four separate empty **PostgreSQL 16.14** suites pass: `topic-reviews`, `topic-review-migration`, `topic-review-race`, `topic-review-retry`. Two real independent concurrent sessions prove one competing decision wins and identical retries create one history row. Migration downgrade/re-upgrade preserves a populated original match with an empty review table and restores the history index; it does not claim a lossless downgrade of populated review history. Labelled scratch containers use ephemeral memory storage and dynamic loopback ports; all were removed. No existing database/container or production deployment changed.
- Final `npm run build` passes **1,701 localization keys, 27 shell checks, 22 resource/delivery checks and 130 report/navigation checks**, TypeScript and Next compilation. The new browser suite passes **10 production-UI journeys (five locales × 390/1440px)** with required rationale, same-key retry after a failed save, evidence conflict/reload, append-only next/first history pages, no document reload and viewer history without write controls. Every API is intercepted with synthetic data. No runtime exceptions, untranslated review placeholders or horizontal overflow; mobile/desktop screenshots inspected. Existing Today and topic-editor browser suites also pass ten localized mobile/desktop journeys each, preserving feed paging/private state and topic preview, activation, draft recovery and account isolation. These are not physical-mobile, native-language or independent usability/accessibility studies.
- Browser verification exposed an actual refresh race: decisions could re-enable before newly requested evidence arrived. Writes now stay disabled during the refresh. The expiry serializer also required the DST fixture to freeze the shared reader's clock along with the feed clock; both Zurich transition-date regressions pass. An initial pytest invocation had a mistyped test filename and ran no cases; the corrected full run above is the reported result. Ruff/whitespace checks pass; existing Starlette test-client and Node module warnings remain.
- `fb38a72e4109` adds the review table only on a later planned application migration; no working/production migration was run. Old snapshots are not fabricated into new audit rows. HL-076 remains **IN PROGRESS** for source-story grouping, richer coverage/jurisdiction, organization AI briefs, topic notification delivery, large interest fan-out and independent field evidence. See `docs/TOPIC_MATCH_REVIEWS.md` and `docs/INTEREST_FEED.md`.


## Unified daily interest feed — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-076-unified-interest-feed`. **95 affected API regressions pass in 116.27 seconds** across topic history/live/validity, tenant/viewer access and inbox context/navigation/pages. After adding the direct active-watch admission path, **22 final feed regressions pass in 25.31 seconds**. They cover multiple topics/law under one event, topic-only personal state, private principal isolation, revoked admission, corrected evidence/work, revised/paused/archived topics, expiry, rejected/muted current review, sparse/tied cursor pages and scope rejection, malformed requests, explicit private-work isolation and both Zurich DST transition dates. Direct watched-document events remain visible without a topic or relation candidate; paused watches stop admitting them.
- Final **PostgreSQL 16.14** suites `feed`, `feed-pages`, and `feed-watch` pass in three separate empty scratch databases. They execute the grouping/current-match SQL, stable scoped keyset pages and direct active-watch path. Task-labelled containers use ephemeral memory storage and dynamically assigned loopback ports and are all removed. No existing container, database, volume, provider or production deployment is changed.
- The production web build passes **1,680 localization keys, 27 shell checks, 22 resource/delivery checks, 130 report/navigation checks**, TypeScript and Next compilation. `npm run check:feed:browser` passes **10 production-browser journeys (five locales × 390/1440px)**: a grouped card with topics/law/direct watch, source and review links, personal read-state mutation without document reload, next/back, sparse continuation, filter/cursor reset and invalid-cursor recovery. Every API call is intercepted with synthetic data. Browser exceptions are absent; controls meet the 44px check and no horizontal overflow occurs. Mobile/desktop screenshots were inspected locally; visual inspection caught and corrected literal translation placeholders and overly tall first-screen guidance. This is not a real-user/native-language study.
- Ruff and whitespace checks pass. Existing Starlette test-client/Node tooling warnings remain. No migrations, real AI calls or messages. Topic fingerprint serialization is shared between workers and the bulk reader without changing the fingerprint algorithm; no historical match rewrite is required. The old overview remains available at `/overview`.
- HL-076 remains **IN PROGRESS**. This is event-ID grouping over currently saved/evaluated evidence, not publisher-story/language consolidation, organization AI briefing, complete source coverage, whole-feed immutable snapshot, per-event fan-out capacity validation or an independent UX/accessibility sign-off. Shared topic review, notification delivery, richer jurisdiction/provenance/coverage and direct event-artifact navigation remain open; see `docs/INTEREST_FEED.md`.


## Candidate document-version freshness — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-version-freshness`. **230 relation/inbox/digest API regressions pass in 260.37 seconds**. After the final predicate rename and explicit-null/missing-binding assertion, **16 focused version/context tests pass in 30.33 seconds**. Twelve new scenarios cover replacing/removing either candidate version, preserved citations/history/job counts, restoration to the original binding, failed retry followed by a fresh report, missing/malformed bindings, explicit absent corpus IDs, final digest filtering after preparation and independent official urgency.
- Three separate empty **PostgreSQL 16.14** checks pass: `scripts/check_inbox_history_postgres.py --suite versions`, `--suite versions-digest`, and `--suite batches`. The SQL predicate excludes mismatched bindings before archived payload hydration; the 50-event/7,474-history-record fixture still uses four history queries and materializes 111 selected records. A prepared event whose only qualifying severity came from the obsolete AI report is dropped at final delivery, with zero real email/model calls. Task-labelled containers use ephemeral memory storage and Docker-assigned loopback ports; all are removed after testing.
- Ruff and whitespace checks pass. This slice has no frontend or migration change; the preceding prompt slice's production build is not claimed as a new build. The existing Starlette test-client deprecation warning remains. Tests use synthetic documents and scripted models/mail, never production data/services.
- Planner v2 causes a new identity on the next explicit/scheduled request; older reports with missing binding remain inspectable history rather than being rewritten or automatically regenerated. Matching candidate IDs is only one evidence-freshness boundary: content corrections under the same ID, legacy fallback changes while the candidate ID stays absent, candidate refresh lag, official-relation corrections and runtime artifact replacement remain open. Final delivery filtering is not whole-period catch-up for newly eligible events. No production deployment occurs; HL-100 stays **IN PROGRESS**.


## Effective relation prompt freshness — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-prompt-freshness`. **187 affected API tests pass in 244.03 seconds**, including relation/configuration/profile/cache, inbox/context/bounded history, digest preview/delivery/coverage, AI history and administration. After tightening empty-override precedence, **29 final focused prompt/resume tests pass in 51.59 seconds**. Fifteen new cases cover both consumed instructions, four irrelevant prompt controls without duplicate inference, platform inheritance/organization override/reset, failed retry/recovery, missing/malformed prompt provenance, digest restart/refusal, privileged tenant isolation and empty/JSON-null stored overrides.
- Separate empty **PostgreSQL 16.14** checks pass for `scripts/check_inbox_history_postgres.py --suite prompts`, `--suite prompts-digest`, and `--suite batches`; the final digest helper is rechecked on another empty PostgreSQL instance. The 50-event/7,474-history-record corpus retains four history queries and 111 hydrated selected records. All task-labelled scratch containers use ephemeral memory storage, bind only to dynamically allocated loopback ports and are removed after testing. No working database/container is changed.
- Production web build passes **1,662 localization keys, 27 shell, 22 resource/delivery and 130 report/navigation/calendar/draft checks**, TypeScript and Next compilation. Only invalidation tags change in the prompt UI; no new visual/browser review is claimed. Ruff and whitespace checks pass; pre-existing Starlette and Node tooling warnings remain.
- Old prompt-less report provenance is history-only, with preserved citations; the cache identity changes once for the next explicit/scheduled request. Reads do not regenerate, rewrite histories or send messages. Scripted models/mail doubles only; no real AI call, migration or production deployment. Evidence and runtime-artifact freshness plus independent quality gates remain open; HL-100 stays **IN PROGRESS**.


## Relation configuration freshness — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-settings-freshness`. **134 affected API tests pass in 189.66 seconds** across relation analysis/freshness/cache, inbox history/navigation/pagination and digest preview/period/batching/resume; **28 additional context/coverage tests pass in 6.22 seconds**. The new configuration suite covers all eleven answer-affecting changes, four credential/transport no-ops, saved settings/failed retry/reset and completed digest selection invalidation. A final tenant-isolation case checks privileged-session resolution as well (final focused suite: 18 cases).
- Three separate empty **PostgreSQL 16.14** databases pass `scripts/check_inbox_history_postgres.py --suite configuration`, `--suite configuration-digest` and `--suite batches`. Settings changes exclude obsolete current conclusions without jobs, inference or historical rewrites; digest preparation restarts and refuses an obsolete completed selection before mail. The 50-event/7,474-history-record fixture still uses four history queries and hydrates 111 selected records. Each task-labelled scratch container uses ephemeral memory storage and is removed afterward. The first fixed host-port attempt was rejected by Windows; the failed task container was removed and the successful runs use Docker-assigned loopback ports. Existing services/data are untouched.
- Production web build passes **1,662 localization keys, 27 shell checks, 22 resource/delivery checks and 130 report/navigation/calendar/draft checks**, TypeScript and Next compilation. Only cache invalidation tags change in the settings UI; no layout change or new browser visual check is claimed. Ruff and whitespace checks pass. Existing Starlette test-client and Node module-type warnings remain.
- Read-time configuration matching fails closed for historical reports lacking the new fingerprint. Results/citations stay in history; a subsequent explicit/scheduled analysis gets the new request identity. No migration, production deployment, real model request or message delivery occurs (mail and AI are doubles). Prompt/evidence/runtime-artifact freshness, historical backfill and independent quality/hardware gates remain open; HL-100 is not closed.


## Topic tab-draft recovery — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-095-topic-tab-drafts`. Final production build passes **1,662 localization keys, 27 shell checks, 22 resource/delivery checks and 130 report/navigation/calendar/draft checks**, TypeScript and Next compilation. Thirteen new parser/storage cases cover account/organization keys, original revision/AI attribution/retry identity, expiration/bounds, malformed shapes, unknown data removal and denied/quota storage. Six existing monitoring-topic API tests pass (6.38 seconds), retaining backend idempotency, immutable revisions and authorization.
- **`npm run check:topics:browser` passes on the final build**: ten required DE/FR/IT/RM/EN × 390/1440 populated journeys now each include a genuine accepted reload and explicit restore, preserving the original baseline and requiring a fresh preview. Existing 40 discard decisions and cancelled native reload still pass. Additional scenarios cover failed activation/reload/re-preview/same-key retry, actual Next menu navigation plus browser Back, storage failure visibility/recovery, different user/organization isolation, original-account recovery, explicit draft deletion and read-only viewer controls. No topic write is sent just by recovering; all application API calls, including existing companion bootstrap, are intercepted synthetic responses before the Next proxy.
- Visually inspected ignored screenshots `test-results/topic-editor/recovery-390.png` and `recovery-1440.png`: restoration choices appear above the disabled editor and remain reachable on mobile without a modal. Changed component/helper/test files pass Prettier; whitespace checks pass. The test initially counted existing assistant bootstrap POSTs as topic mutations; the assertion now checks monitoring endpoints. Final QA has no runtime exceptions. No new live data, source activation, real model/mail call, migration or production deployment occurs; QA browser/server cleanup runs in the script's finally block.
- Session storage is an unencrypted, same-browser convenience copy scoped by user and organization, not a browser-profile security boundary, cross-device sync or durable backup. Expiry is checked when read; abrupt termination can lose the final edit and closing/evicting the tab can remove everything. Complete route discard warnings, physical mobile/crash testing, native-language review and independent usability gates remain open. HL-095 stays **IN PROGRESS**; see `docs/TOPIC_EDITOR.md`.


## Relation generation cache identity — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-generation-cache`. **61 affected API tests pass in 47.95 seconds**, including 20 new checks. Eleven answer-affecting provider/model/context/product/generation changes produce distinct cache keys; four credential/transport/concurrency changes preserve identity. Five actual analysis-job endpoint scenarios generate a new durable report after each previously ignored generation control changes, then reuse it on repeat and token rotation. Each uses a scripted model, never a paid/local inference endpoint.
- Saved plans preserve the exact generation controls for each attempt without credentials; the previous report's parameters remain unchanged. Profile freshness, failure/history and evidence-gate regressions remain green. Ruff and whitespace checks pass. No frontend change in this slice; the preceding profile slice's production build is not claimed as a new build here.
- This fixes **request identity**, not complete read-time settings/runtime freshness. Old cache keys miss on the next explicit/scheduled request but are not rewritten or automatically bulk reanalysed. No migration, production action, real model call or message delivery. HL-100 remains in progress.


## Relation profile freshness — 5 September 2026 (HappyDucky02)

- Branch `codex/HappyDucky02/hl-100-profile-freshness`. **147 affected API tests pass in 141.58 seconds**: relation generation/history/evidence, inbox selection/context/paging and digest preview/period/resume/coverage. Nine new regression cases cover actual profile PATCH, preserved citations, stale severity filtering, no read-time jobs/model calls/history rewrites, failure followed by recovery, missing/malformed/mismatched provenance, official facts and explicit organization scoping even in privileged sessions. Existing synthetic performance reports now contain valid profile provenance rather than omitting it.
- **PostgreSQL 16.14**, two isolated empty loopback databases: `scripts/check_inbox_history_postgres.py --suite profile` passes the actual history/inbox/profile endpoint scenario; `--suite batches` passes 50 events / 7,474 historical analysis/review rows with **four history queries and 111 loaded records**. Scalar SQL profile checks do not hydrate archived evidence or introduce a query per report. These are correctness/bounded-hydration tests, not a target-server CPU/p95 benchmark.
- The public read behavior keeps previous reports and citations inspectable while removing obsolete organization-specific conclusions from current selection. Profile-save cache invalidation now also covers relation histories and digest previews. No schema migration, real model call, external message, production deployment or production data change.
- Production build passes **1,655 localization keys, 27 shell checks, 22 resource/delivery checks and 117 report/navigation/calendar checks**, TypeScript and Next compilation. The final nine profile regressions pass again (16.92 seconds) after strengthening official-urgency coverage. Ruff, changed-component Prettier and whitespace checks pass. Both owned temporary PostgreSQL containers were removed.
- Scope is saved **profile revision** freshness. Complete evidence/prompt/settings/runtime invalidation and historical candidate backfill remain open under HL-100; independent legal-quality/user studies are not replaced by these synthetic checks. Existing Starlette/httpx deprecation warning remains unchanged.


The source-to-diff workflow, Settings page, and live Apertus path are verified.

## Everyday registry filters — 5 September 2026 (HappyDucky02)

- Task branch: `codex/HappyDucky02/hl-095-registry-filter-disclosure`. Final production build passes **1,655 localization keys**, **27 shell**, **22 resource/delivery** and **117 report/navigation/calendar checks**, TypeScript and Next production generation. Six new calendar cases verify Zurich midnight, both DST transitions, year/leap boundaries, inclusive 7/30-day ranges and independence from Honolulu/Tokyo runtime zones. The four existing registry API regressions pass unchanged in 3.16 seconds; existing Starlette deprecation warning remains.
- **20 required populated production-Chrome journeys pass**, covering Monitoring and Discover in DE/FR/IT/RM/EN at 390/1440 px. They verify collapsed advanced controls, real pointer-reachable presets/disclosures/chips, native search submission, URL-driven selection and Back restoration, empty-result clear, preserved locale, unsupported deep-link values and cursor reset. Viewer mutation controls remain absent; an administrator retains the existing read-state control. No registry mutation endpoint is called.
- Browser visibility checks use actual `checkVisibility()`: collapsed native details can retain layout boxes despite invisible content. Essential controls meet the focused 44 px checks, long DE/FR title fixtures do not overflow either width and runtime exceptions remain absent. Ignored representative screenshots under `test-results/registry-filters/` were visually inspected. Every API call is intercepted; no running backend, real source, model or recipient is contacted.
- Changed component/helper/test files pass Prettier and whitespace checks. This is a frontend filter/navigation slice with no API/schema/deployment change. It does not certify all raw metadata translations, source-health recovery, complete keyboard/screen-reader behavior, physical mobile input, independent usability or mature-corpus registry query performance. HL-095 remains in progress.

## Bounded interactive digest preview — 5 September 2026 (HappyDucky02)

- Task branch: `codex/HappyDucky02/hl-099-paged-digest-preview`. **95 affected API tests pass in 90.38 seconds**, covering digest pages/periods/coverage/resume/delivery and inbox pages/context/private state. Thirteen new cases cover authenticated preference save and sparse 50/50/21 navigation, stable equal-time order/captured first-page revisit, compatibility output, malformed/boundary/scope/preference cursors, late admissions and current personal mute state. Preview reads produce no model/mail calls or job/delivery/read-state writes.
- A separate **PostgreSQL 16.14** check (`--suite preview`) passes the authenticated 121-event scenario on an empty task-owned loopback database. The first two pages inspect 50 events each with zero severity matches; the last inspects 21 and returns all 21 matches. Exactly the selected event payloads are hydrated. The disposable container was removed; existing application/production containers were untouched.
- **Ten required Chrome production-UI journeys pass** (DE/FR/IT/RM/EN × 390/1440 px). Real pointer hits verify Next/Back through empty pages, a fixed captured first period, retained unsaved choices, explicit bounded save, cursor-error recovery and 44 px navigation targets. Page changes focus the preview heading; no horizontal overflow or runtime exceptions were observed. Every API request is intercepted; no real recipient or provider is contacted. Ignored screenshots are in `test-results/digest-preview/` and both representative widths were visually inspected.
- Final `npm run build` passes **1,629 localization keys**, **27 shell**, **22 resource/delivery**, **111 rendered report/navigation checks**, TypeScript and Next production generation. Ruff, focused changed-file Prettier and whitespace checks pass. Existing Starlette/httpx deprecation warning remains. An earlier affected run exposed the existing exclusive-admission fixture's Windows clock-tick race; the multiple-law fixture now explicitly predates capture rather than relying on elapsed wall time. The final rerun passes without changing admission semantics.
- This is an event-key work bound, not a fixed request-time/memory guarantee, immutable evidence snapshot, target-host capacity result, full assistive-technology/native-language review or complete HL-099 delivery. A selected event may still affect many laws; compatibility previews remain unbounded across their period. No schema migration or deployment occurs.

## Topic draft switch and reload protection — 5 September 2026 (HappyDucky02)

- Final production build passes **1,619 localization keys**, **27 shell**, **22 resource/delivery**, **111 rendered report/navigation checks**, TypeScript and Next compilation. Changed TS/browser-test files pass Prettier and whitespace checks. There is no API/schema/deployment change.
- The required ten populated topic browser journeys (five locales × 390/1440 px) now additionally pass **40 real localized discard confirmations**, with accept/cancel paths for switching saved topics and starting a new one. Declining retains the exact draft and focuses the editor; unchanged, reset, successfully saved and reverted plans do not raise false warnings. A real Chrome `beforeunload` dialog on reload is cancelled and the failed-preview draft survives. The successful revision save clears the guard before the viewer navigation test.
- Previous assertions remain active: actual hit-tested pointer controls, 30-topic deep-list editing, catalogue polling, manual no-AI preview/explicit activation, scoped source selections, busy protection, expected revision, hidden values and viewer controls. The harness now waits for requested focus restoration and successful-save completion before sending the next pointer/navigation event; these are real browser timing requirements, not disabled product checks.
- All APIs are intercepted with synthetic data. No real organization/monitoring changes, source activation, provider call, message or production restart occurred. Existing contrast evidence belongs to the preceding editor/layout slice; this change adds only a shared muted-text status, with no new color/layout system.
- This closes only same-page replacement and browser reload/unload protection. Client-side route/history changes, crash recovery and physical mobile termination can still lose a draft. HL-095 remains in progress; no persistent-draft or complete navigation-safety claim is made.

## Progressive topic editor — 5 September 2026 (HappyDucky02)

- Final production build passes **1,617 localization keys**, **27 shell**, **22 resource/delivery**, **111 rendered report/navigation checks**, TypeScript and Next compilation. This frontend slice leaves matching, API authorization, idempotency and revision enforcement unchanged; no migration or deployment occurs.
- **`npm run check:topics:browser` passes ten required populated Chrome journeys** (DE/FR/IT/RM/EN × 390/1440 px) using 30 synthetic saved topics and enabled/disabled source-pack fixtures. It checks collapsed advanced controls, catalogue labels, enabled-only defaults, a cleared selection surviving actual polling, explicit missing-scope recovery, manual preview/activation payloads without AI, idempotency keys, pending-form protection and editing from below the form in the actual main scroller. Essential action/disclosure/checkbox tests use hit-tested pointer events, not only programmatic clicks.
- Additional checks preserve a failed preview's edited goal, retry/save with `expected_revision=3` and unchanged hidden synonyms/exclusions, and show saved topics without editing controls to a viewer. Every API is intercepted before Next's proxy; no actual monitoring, source activation, provider call, organization change or message delivery occurs. The initial mock health response was corrected to match the real nested provider schema; test polling was fixed to accept both synchronous and asynchronous conditions.
- Synthetic 390/1440 screenshots were inspected. Inspection found the editor heading hidden beneath the sticky toolbar; scroll alignment now uses the measured toolbar height, and the browser assertion requires the form to sit below it. Existing **8,060 PostCSS contrast samples (minimum 4.79:1)** passed after the new styles were compiled; these are existing component samples, not a whole-topic accessibility scan. Changed TS/test files pass Prettier; whitespace checks pass.
- HL-095 stays in progress: complete draft/navigation-loss protection, registry controls and source recovery remain open, as do native translation approval, screen-reader/physical mobile input and independent user study gates. The synthetic UI suite proves flow and payload behavior, not legal relevance, source completeness, AI quality or a production acceptance result. See `docs/TOPIC_EDITOR.md`.

## Mobile navigation and skip-to-content — 5 September 2026 (HappyDucky02)

- Production web build passes **1,590 localization keys**, **27 shell**, **22 resource/delivery**, **111 rendered report/navigation checks**, TypeScript and Next compilation. No API change, schema migration, model invocation or production deployment occurs.
- **`npm run check:shell:browser` passes 30 populated Chrome journeys**: DE/FR/IT/RM/EN × viewer/organization administrator/platform administrator × 390/768 px, each also resizing to/from 1440 px. Real keyboard input verifies the visible first-stop skip link, focus isolation, forward/reverse Tab, nested workspace Escape, returned opener focus and restored scrolling. Role-filtered links, modal name, viewport geometry, background navigation stacking, backdrop/close-button dismissal and actual source-route navigation pass. Every application API is intercepted; no workspace is switched or real message/model call made.
- Browser verification found and fixed a real off-screen menu regression: compiled CSS retained the shared dialog's percentage translate. Explicit zero-translation utilities now remove those defaults before compilation. The harness was also corrected to send native Enter text, wait for opening animation/listener readiness and use distinct route URLs for each authenticated-role fixture.
- Existing **15 populated comparison browser journeys**, the paginated inbox browser suite and **8,060 PostCSS contrast samples (minimum 4.79:1)** pass on the final build. Synthetic screenshots at 390/768 px were inspected; the long platform menu scrolls within the viewport. Evidence stays in ignored `test-results/shell-navigation/`. Prettier for changed TS/test files and whitespace checks pass.
- HL-097 stays in progress. This slice does not claim complete axe/WCAG clearance, nested Marvin/evidence-overlay coverage, all required product journeys, screen-reader/native-speaker review, physical mobile input/audio or Firefox/Safari behavior. See `docs/NAVIGATION_ACCESSIBILITY.md` for the reproducible scope.

## Comparison overlay focus and persistent Ask draft — 5 September 2026 (HappyDucky02)

- Production web build passes TypeScript/Next compilation and **1,589 localization keys**, **27 shell**, **22 resource/delivery**, and **111 rendered report/navigation checks**. No new API behavior, migration, model invocation or production deployment is involved.
- New **`npm run check:comparison:browser` passes 15 required populated journeys** (DE/FR/IT/RM/EN × 390/768/1024 px), each also crossing to/from 1440 px. Actual Chrome key events verify forward/reverse Tab moves among controls without escaping, background programmatic focus is rejected, Escape/close restores the opener, root scrolling is unlocked and a saved citation returns focus to its evidence. Native tablet backdrop clicks dismiss the panel. The draft survives close/reopen and responsive changes without remounting the Ask subtree. Desktop stays nonmodal and allows normal outside focus.
- The committed fixture contains only synthetic API-shaped versions, a fictional retention policy and a ScriptedModel report. Every application API request is intercepted before Next's proxy; the run cannot silently omit its comparison or citation fixture. Initial harness issues (sandbox DevTools access, incorrect admin role/unconfigured fake provider, incomplete platform key events and an autofocus race in a multi-call assertion) were corrected before the passing run; real role/provider checks were not weakened.
- The actual PostCSS/component contrast suite passes **8,060 samples, minimum 4.79:1** across four widths and enabled states. Inspected synthetic screenshots at 390/1024/1440 px confirm the full-screen/drawer/nonmodal layouts and visible draft. Evidence stays in ignored `test-results/comparison-overlays/`; no screenshots or real user data are committed.
- This focused comparison slice does not claim axe/whole-route accessibility clearance, WCAG certification, nested Radix/Marvin overlays, global navigation focus isolation, a native-speaker sign-off, physical mobile software-keyboard/audio behavior or Firefox/Safari/screen-reader review. Those remain open under HL-097. Existing tablet/desktop compact tab labels and desktop pet placement still warrant the broader visual/accessibility work.

## Batched inbox document/link context — 5 September 2026 (HappyDucky02)

- **103 affected API tests pass** in **110.35 seconds** across inbox context/history/navigation/pages/reviews, relation analysis and digest period/page/coverage/resume flows. Four new tests cover constant query count, selected-column/materialization limits, 100/21 context batches for 121 legacy-route events, stable visible links and successor alias selection without foreign monitoring state.
- A real HTTP page with synthetic one-/50-event selections executes **16 SELECTs in both cases**, including configuration and empty-history metadata. It hydrates no Law, Version, Comparison or RegulatoryDocumentVersion ORM objects; candidate evidence and work metadata remain deferred. Large synthetic document/passage/diff bodies do not load merely to generate links. Counts for populated histories, replacements, authentication and additional candidate batches differ; no universal fixed-query/latency guarantee is claimed.
- Separate empty, isolated **PostgreSQL 16.14** checks pass for `scripts/check_inbox_history_postgres.py --suite context`, `--suite links` and `--suite successors`. They verify actual endpoint SQL, stable comparison time/ID ranking, owner visibility and active/paused/unwatched successor selection. Link fixtures test synthetic identifiers and visibility, not legal-document identity quality. Each verified task-owned database/container volume is removed after use; no production data/services, paid model or actual email is involved.
- Ruff and whitespace checks pass. No migration, API shape change or frontend layout change is required; no new frontend build is claimed. The pre-existing Starlette test-client warning remains. The initial new query-count assertion guessed 15; inspection accounted for all 16 (three configuration, three page/state/delivery, two empty-history, eight context), then both database checks confirmed the corrected bound.
- HL-099 remains in progress: a single event can still fan out to many laws, response assembly still retains its selected delivery groups, SQL windows/sorting are not constant CPU, sparse previews can traverse a full period, and the target-host 100k-event/20-reader workload is still unverified.

## Batched inbox analysis and review histories — 5 September 2026 (HappyDucky02)

- **99 affected API tests pass** in 138.06 seconds across inbox pages/navigation/history/reviews, relation analysis and digest period/page/coverage/resume flows. Five new regression cases cover mixed current/failed/legacy/empty histories, retained human decisions beneath annotations, 100/21 batching, explicit organization scope in privileged sessions and concurrent analysis/review appends after scalar selection.
- A real 50-event API page backed by **7,474 historical records** uses exactly **four history selection/hydration queries** and materializes **111 selected analysis/review records**. An isolated **PostgreSQL 16.14** run (`scripts/check_inbox_history_postgres.py --suite batches`) passes the same assertions and response semantics. The task-owned database container and volume were verified and removed afterwards. No AI requests, actual mail or production data/services were used.
- Ruff and whitespace checks pass. This backend-only slice has no schema migration or public response/layout change; no new frontend build is claimed. The existing Starlette test-client warning remains. The test corpus initially retained the seed helper's official relation, which correctly took precedence over legacy AI status; the history-only fixture was corrected to remove that unrelated confirmation before the passing run.
- History metadata/IDs/counts use one statement snapshot per table, followed by bounded payload retrieval. This is not an immutable snapshot of selected records, a fixed CPU cost for SQL windows, a bound on per-event law fanout, elimination of remaining per-law entity/comparison queries, or a target-host 100k-event/20-reader capacity result. HL-099 remains in progress; see `docs/INBOX_READS.md`.

## Interactive inbox pagination and independent law search — 5 September 2026 (HappyDucky02)

- **53 affected API tests pass** in 126.98 seconds: public pages, inbox/reviews, relation analysis, digest pages/resume and two new navigation prerequisite scenarios. A 61-watch search uses scalar columns only, displays 50 plus a selected result outside that cap, escapes literal wildcards, includes paused watches and excludes another organization. A 61-event corpus opens its oldest event directly through an organization candidate link; unknown/foreign links remain empty and mismatched cursors fail.
- A separate **PostgreSQL 16.14** run (`scripts/check_inbox_history_postgres.py --suite options`) confirms real law-search response and materialization bounds. Its verified task-owned container and volume were removed. No production database or paid model was used.
- Production web build passes with **1,589 localization keys**, 27 shell checks, 22 resource/delivery checks and **111 report/navigation checks** (26 new navigation cases, including 20 real-component localized renders). New copy has explicit DE/FR/IT/RM/EN values; native-speaker review remains a separate gate. The existing module-type/Starlette warnings are unchanged dependencies/tooling notices, not hidden test failures.
- `npm run check:inbox:browser` exercises the actual compiled page in disposable Next/Chrome processes with every application API response intercepted. It passes next/back navigation, law options independent of event pages, sparse-page continuation, old-event links, filter reset, invalid-cursor recovery and layout at 390/768/1024/1440 px. It asserts the UI never requests the old whole-history inbox. The harness initially selected a Chrome extension target and had an incomplete fake health response; both fixture issues were corrected before the passing run.
- Actual localized components with production PostCSS pass **8,060 contrast samples**, minimum **4.79:1**. All 15 new navigation locale/state fixtures fit their pane at the four widths; enabled navigation links remain at least 44 px high. Ruff/whitespace checks pass. This is focused UI/regression evidence, not complete accessibility/usability certification or the intended-host 100k-event/20-reader benchmark.

## Bounded public inbox page API — 5 September 2026 (HappyDucky02)

- **105 affected API/queue tests pass** in 160.68 seconds, including 17 new public-page cases. Real endpoint tests traverse 121 equal-time events without skips/duplicates, defer concurrent new admissions, advance empty severity-filtered pages, preserve legacy watched-law/severity semantics and isolate personal states. Source/authority and event/document-kind aliases agree with the legacy response. Malformed, oversized, wrong-filter/account/organization cursors and invalid limits are rejected.
- A separate disposable **PostgreSQL 16.14** run (`scripts/check_inbox_history_postgres.py --suite inbox`) verifies 50/50/21 HTTP pages and exactly the selected event/delivery payloads materialized. The verified task-owned container and volume were removed. No model or real email calls, production service changes or production data were involved.
- Ruff and whitespace checks pass. This additive backend route has no migration or frontend change; the existing web consumer still uses the legacy route until its next migration. Existing Starlette test-client warning remains. The prior production frontend build is not claimed as a new build for this slice.
- Page-only counters are explicitly labelled in the API. Cursors choose positions rather than authorize access; independently scoped SQL enforces organization/private-state boundaries. Existing evidence is live, and per-event law fanout, per-law queries, severity scan cost, frontend navigation and target-host 100k-event/20-reader capacity remain open HL-099 work.

## Resumable digest preparation and recipient revalidation — 5 September 2026 (HappyDucky02)

- Final affected API/queue run: **88 passed** in 138.33 seconds, including 14 new resumable-digest regressions. A failure injected before checkpoint commit rolls back the page; completed pages survive cancel/retry; a completed selection prevents a full rescan after mail failure. Tests cover fair outbox dispatch, preference changes during/after preparation, opt-out, current private-state/admission checks, foreign/corrupt checkpoints, stale worker ownership, cancellation before dispatch, revoked membership/disabled user and monotonic delivery watermarks.
- The final isolated **PostgreSQL 16.14** run (`scripts/check_inbox_history_postgres.py --suite resume`) exercises real worker/job/outbox persistence with valid organization members: yield after 50 keys, dispatch the second recipient first, resume to 61 events, record one send through a mail double and preserve success idempotency. Task-owned PostgreSQL containers were removed. No production data/service or actual recipient mailbox was used.
- Ruff and whitespace checks pass. This slice changes backend worker behavior without a schema migration or frontend contract/layout change; the prior coverage-notice production build remains the frontend evidence. Existing Starlette test-client warning remains.
- Worker execution now bounds preparation by event page, not by per-event law fanout, database CPU or mail-transport duration. Sparse web previews are still synchronous traversals. SMTP acceptance followed by process/transport failure before the database commit remains an ambiguity window; no exactly-once email or target-host capacity claim is made. See `docs/INBOX_READS.md` for these boundaries and remaining HL-099 work.

## Visible digest selection limits — 5 September 2026 (HappyDucky02)

- **61 focused API tests pass**, including 24 new eligible-law count, legacy-summary and localized text/HTML email tests. Per-event law counts apply after severity filtering; exactly five laws do not trigger overflow. No model or actual email delivery is used.
- `npm run build` passes TypeScript, Next.js production compilation/static generation and the integrated 27 shell, 22 resource/delivery and **85 rendered component** checks (20 new digest notice cases). Localization audit passes **1,577 production keys** with no unapproved English inheritance.
- Actual component output with production PostCSS in headless Chrome passes **7,500 text-contrast samples**, minimum **4.79:1**, at 390/768/1024/1440 px. All 15 digest language/overflow fixtures fit their panes, and their `/impact` links are at least 44 px high. This is focused component QA, not a complete assistive-technology, native-speaker or end-to-end usability certification.
- Ruff, changed-component formatting and whitespace checks pass. Existing records lacking per-law coverage metadata remain readable; UI/history/email do not invent omitted-law counts. The full-inbox link does not claim to preserve a historical period/preferences snapshot. Production services/data remain untouched.

## Digest event keyset pages — 5 September 2026 (HappyDucky02)

- **37 focused tests pass** across event pages/periods/delivery, inbox/history and relation analysis, including seven new paging regressions. A 260-event synthetic period with 120 earlier nonmatching severity groups stops after four 50-ID pages once the 51st eligible group is found. The summary retains 50 events, every law for a selected event stays together and invalid page sizes are rejected.
- SQLite and isolated **PostgreSQL 16.14** checks traverse 121 equal-time event IDs without duplication/omission, advance across empty presentation pages and exclude a newly admitted backdated event until the next traversal. PostgreSQL runner: `scripts/check_inbox_history_postgres.py --suite pages`; its task-owned container was removed afterwards.
- Ruff/whitespace checks pass; no schema migration or external API/UI shape change. Tests use synthetic evidence and model/mail doubles. No existing service or production data was changed.
- Bounds apply to event pages retained in memory, not total CPU/SQL/worker duration or the number of related laws per event. Public inbox pagination, batched lookups, durable continuation, actionable truncation and intended-host workload gates remain open. The admission ceiling is not a frozen snapshot of mutable evidence/private state or a policy for backdated historical admissions.

## Saved digest periods and SQL eligibility — 5 September 2026 (HappyDucky02)

- **30 focused tests pass** across digest periods/delivery, impact inbox, history selection and relation analysis. Seven new cases cover half-open saved periods, retry after a test-only mail failure, success idempotency, empty preview source discovery and exact 50/51-event truncation. Existing Starlette deprecation warning remains.
- A 10,000-old-event corpus on SQLite and a separate disposable **PostgreSQL 16.14** run (`scripts/check_inbox_history_postgres.py --suite periods`) asserts only three eligible delivery/event records and one selected personal-state record are hydrated. Start-inclusive/end-exclusive boundaries, source/authority matching, personal and organization isolation and payload-free source menu reads pass. No model calls or real email deliveries occurred.
- Ruff and whitespace checks pass. This backend slice preserves the external API/UI shape and has no schema migration. The task-owned PostgreSQL container was removed; existing services and production data were untouched.
- This is period eligibility, not bounded event pagination: a busy selected period still loads all its eligible candidates. Durable continuation, visible/actionable truncation, batched lookups, late/backdated admission policy and intended-host 100k-event/20-reader gates remain open in HL-099. No full-suite or target-host capacity claim is made for this slice.

## Bounded per-candidate inbox analysis history — 5 September 2026 (HappyDucky02)

- The final focused inbox/history/digest/relation-analysis run passed **23 tests**, including six new history-selection/migration regressions. A 10,001-analysis synthetic history loads only the latest attempt and the latest current-schema success (two ORM records) using three selection/count queries. Empty/current/legacy histories, tenant isolation, equal-timestamp ordering and a newer attempt injected between queries preserve the intended result.
- An independent disposable **PostgreSQL 16.14** run of `scripts/check_inbox_history_postgres.py` verified the same 10,001-row materialization/query bounds through the real selector and inbox endpoint. A populated downgrade/upgrade of index migration `fa27c61d3098` preserved selected IDs and the history count. The verified scratch container was removed afterwards. No paid model or production database/service was used.
- Ruff and whitespace checks pass. This backend-only slice leaves the existing UI/API shape unchanged and does not require a frontend rebuild. The full 532-test Windows run recorded for match validity belongs to the preceding validity change; the final history slice was checked with its affected 23-test suite and PostgreSQL gate rather than claiming another full run.
- These are per-candidate payload bounds, not whole-inbox pagination or fixed database CPU cost: current organization candidate enumeration, related-entity lookups and digest source-window selection remain HL-099 work. The intended-host 100k-event/20-reader latency, memory and overlap gates remain open; see `docs/INBOX_READS.md`.

## Topic match validity and review preservation — 5 September 2026 (HappyDucky02)

- History and live evaluation now preserve human confirmation/rejection/mute and the original reviewed evidence when a formerly matching event becomes a non-match or receives changed positive evidence. The matches API distinguishes machine eligibility, historical confidence and the current applicability of a human decision; changed inputs are already labelled stale while a worker is pending. Revoked admissions cannot expose retained matches.
- The focused topic history/live/preview/validity run passed **58 tests**, including 16 new regressions for reviewed negative/positive transitions, source/rule/plan changes, tenant visibility, transaction rollback/retry, legacy recovery, A → B → A source corrections and populated migration round-trip. The new admission generation prevents incorrect reuse of a completed job after reversion; unchanged source health alone does not invalidate relevance. No model calls are made by these scenarios.
- The final Windows-compatible API suite passed **532 tests** in 420.69 seconds. Only the unchanged Linux-only release-manager module requiring `fcntl` was excluded; the existing Starlette test-client deprecation warning remains. The resumed read-only sandbox could not create SQLite migration temporary files; the successful isolated regression runs used the approved test execution permission, without changing database behavior to bypass the failure.
- Disposable **PostgreSQL 16.14** passed `scripts/check_topic_history_postgres.py --suite validity` using actual live jobs and a populated downgrade/upgrade. Migration `f9c208b5a431` preserves legacy evidence/decisions and leaves unverifiable eligibility `unchecked`. The verified task-owned database container was removed. Production databases/services were untouched.
- `npm run typecheck` passed the 1,572-key five-language catalogue/value audit, 27 shell checks, 22 resource/delivery checks, rendered report/history/preview checks and TypeScript. After adding the final five old-evaluator recovery cases, `npm run check:reports` passed **65 rendered checks**. API/script Ruff and whitespace checks also pass. This slice changes only existing recovery copy, not layout or routes.
- The shared topic review command/feed presentation, complete coverage warnings and target-host load gates remain open. These checks are not a user-pilot result, legal-relevance certification, Ubuntu deployment test or authorization to run production migrations.

## Topic preview and activation parity — 5 September 2026 (HappyDucky02)

- Preview now scores only the current organization's admitted saved events using the same deterministic scorer as historical and live matching. Normalized official references, lexical and metadata signals, exclusions, jurisdiction, language, source pack and event/document-kind rules agree across all three paths. Public corpus records admitted only to another organization do not appear. Preview creates no records, jobs or AI calls.
- **516 Windows-compatible API tests pass**, including 13 preview parity regressions. A 501-event case distinguishes the 500-event preview sample, ten displayed results and all 501 events processed on activation. The unchanged Linux-only release-manager module requiring `fcntl` was excluded; the existing Starlette deprecation warning remains. This is not an Ubuntu or target-host load certification.
- A disposable **PostgreSQL 16.14** database passed `scripts/check_topic_history_postgres.py --suite preview`, verifying organization scope and identical official-reference reasons/confidence across preview, history and live processing. The task-owned test container was removed afterwards.
- `npm run typecheck` passes: 1,572 recognized production translation keys, the five-language value audit, 27 shell checks, 22 resource/delivery checks and 60 rendered component checks (including 20 new preview cases). Limited, complete, empty and older-API previews render in DE/FR/IT/RM/EN without unresolved placeholders or invented coverage counts.
- The actual Chrome/PostCSS contrast check passed **6,700 rendered samples**, with a minimum **4.79:1** ratio for assessed text and no preview overflow at 390, 768, 1,024 and 1,440 pixels. Disabled text is excluded from contrast assessment. The Next.js production build, Python Ruff checks and `git diff --check` also pass.
- No production services or databases were changed and no paid provider was called. Preview explicitly describes its saved-event scope, sample cutoff and separate display limit; it does not promise complete external-source coverage. Feed-wide freshness/degradation and the validity of previously matched evidence remain open HL-094 work.

## Resumable live topic matching — 5 September 2026 (HappyDucky02)

- Connector fan-out now commits organization-owned live matching jobs/outbox records instead of running a permanently capped shortlist. PostgreSQL organization pagination reaches the 101st owner; live topic batches continue beyond 50 considered topics and 20 matching results. The unchanged deterministic scorer and shared evidence writer make zero model requests.
- The Windows-compatible API regression passed **501 tests**. After adding the final connector-replay, spooling rollback and cross-organization fairness assertions, the focused live/history/matching/synchronization/job run passed **47 tests** (including all nine new live regressions). The unchanged Linux-only release-manager module requiring `fcntl` was excluded from the Windows run; this is not an Ubuntu or 100-user performance certification. The existing Starlette test-client deprecation warning remains.
- A disposable **PostgreSQL 16.14** database passed `scripts/check_topic_history_postgres.py --suite live`: 51 metadata-relevant topics persist through three outbox executions; duplicate delivery reuses results; 101 organizations are spooled without truncation; the last owner’s worker sees only its data and the first owner’s API receives 404 for that job. Actual joined live-batch topic locks, lease recovery, completed-checkpoint replay and a populated history-index downgrade/upgrade also passed. The test container was removed afterwards.
- Failure after writing a match rolls back the entire batch; failure while spooling rolls back jobs, steps and outbox together. Cancellation/retry retains the prior cursor, successful batches do not exhaust the failure budget, and a large organization yields behind another organization’s waiting event. Tests also cover changed-evidence supersession, revoked admissions, foreign checkpoints and creation/edit races between the history and live paths.
- API/script Ruff checks and `npm run typecheck` pass, including the five-language catalogue/value audit (1,567 recognized production keys), 27 shell checks, 22 resource/delivery checks and 40 rendered report/history checks. New live/history queue names and steps have DE/FR/IT/RM/EN translations. No layout or routing changes were made in this slice.
- No production service was restarted, no production schema/data changed and no paid provider was called. Organization metadata spooling remains proportional to admitted event/organization pairs; target-host load/capacity, preview parity, feed-wide coverage and reviewed evidence validity remain open backlog work.

## PDF reader replacement and Apache 2.0 migration — 4 September 2026

- Replaced the current PyMuPDF runtime/test dependency with MIT-licensed pdfminer.six. The shared reader supplies text blocks, Unicode metadata, and physical page references to document ingestion and Federal Criminal Court metadata extraction. ReportLab generates test fixtures only; it is absent from the production API environment.
- 48 focused extraction/PDF/court tests pass. The Windows suite passes 398 tests, excluding the five Linux-only release-manager tests (`fcntl`). Python lint passes across the API.
- A full Linux run with `--network none` executes 403 tests: 388 pass and 15 connector tests fail because their public-address validation needs real DNS even with mocked HTTP. Re-running all four affected connector files with ordinary Docker networking passes all 24 tests, including those 15. A separate Linux run of extraction, PDF, both court connectors, and release-manager tests passes all 60 tests. These are complementary runs, not a claim that the suite is fully offline-independent.
- Eight saved PDFs (2–176 pages) and a fresh official Fedlex NFA PDF (269 pages) extract successfully. The NFA result contains 677,990 characters and 5,689 passages, taking 17.14 seconds to extract on the development host. These checks establish readability and regression behavior, not independent legal completeness. See [source boundaries](SOURCES.md).
- API, web, and model-manager Docker builds succeed. The web build includes its shell/resource/i18n checks and TypeScript compilation. Runtime probes confirm the Apache 2.0 text in all three images and no PyMuPDF, ReportLab, or host uv cache in the API runtime. Existing live containers and data volumes were not restarted or deleted.
- The Python wheel and sdist build successfully, include license/notices, and exclude host caches. Root/API license copies and wheel notices match. Package metadata uses SPDX `Apache-2.0`. Third-party license wording is preserved separately; only trailing whitespace is normalized in the reproduced dependency notices.
- The API lock removes `pymupdf` and adds `pdfminer-six`, `charset-normalizer`, and development-only `reportlab`/`pillow`; existing locked dependency versions are unchanged. The existing FastAPI/Starlette test-client deprecation warning remains unrelated to this change.
- No paid AI calls, working database reset, automatic evidence rewrite, or public deployment was performed for this verification. Old images/commits retain their historical dependencies and terms; rebuild/recreate the API and workers before using the new reader.

## Durable PostgreSQL jobs and Redis/Celery execution — 3 September 2026

- 155 API regressions pass. Job tests cover transactional outbox creation, duplicate idempotency keys, one-worker claims, broker failure, stale leases, bounded attempts, queued and running cancellation, safe retry, persisted scan steps, and saved Impact/Ask results.
- Production commands create `Job`, `JobStep`, and `OutboxMessage` records before returning. Scan, Impact, and Ask return HTTP 202 with a durable job; only the isolated test/development mode executes inline.
- Compose starts healthy PostgreSQL, Redis 7.4 with AOF/no-eviction, the API, separate CPU and AI Celery workers, Celery Beat, and the web app. Six queues are declared: `interactive`, `ingest`, `parse_diff`, `ai_interactive`, `ai_background`, and `maintenance`.
- A live scan returned queued immediately, was claimed once by the ingestion worker, and finished with a persisted result link. Its Activity page displayed the queue, attempt, progress, persisted step, final state, and result.
- With Redis stopped, a new scan stayed queued in PostgreSQL. Restarting Redis caused the outbox to be dispatched and the job to succeed. A second queued scan survived stopped workers/scheduler plus an API restart, remained queued with zero attempts, and completed once after the processes returned.
- Restart initialization preserves job-backed scans and active AI records. A stale lease is reconstructed from PostgreSQL, while completed evidence stays immutable and exhausted jobs remain inspectable and safely retryable.
- Python linting, all 155 API tests, Compose configuration validation, `git diff --check`, TypeScript checking, frontend formatting, and the Next.js production build pass.

## Single-host local-first contract — 3 September 2026

- `docs/ARCHITECTURE.md` is ratified as the HL-029 implementation contract for one i7/32 GB/two-GTX-1080 Linux host and one repository/Compose deployment.
- Only Caddy ports 80/443 are public in the target. Database, Redis, workers, scheduler, model management, API, and inference stay private; development endpoints remain loopback-only.
- Clean install is `local_only`: unavailable inference creates `waiting_for_model` work and cannot silently call a configured cloud integration.
- The release gate now defines measurable API, queue-admission, GPU-slot, memory, recovery, connector-freshness, and queued-AI behavior. It explicitly accepts one-server downtime and the absence of high availability.

## Legal-unit semantic diff — 3 September 2026

- 148 API regressions pass with diff schema v6. The stored result contains both complete exact passage/word evidence and a legal-unit projection with title, chapter, section, article, paragraph, littera, and number paths.
- Each paired unit records why it matched, a bounded score broken into stable label, content, neighbour, and parent-context signals, plus an ambiguity flag. Stable article labels can retain a real full-text replacement that low lexical similarity would otherwise split into unrelated add/remove rows.
- One inserted article remains one material cluster while following unchanged articles are classified as renumbered. A moved unchanged section is classified as moved with zero material items.
- A real 30-to-60-day obligation change remains substantive. A true end-of-line split word and repeated page headers are formatting-only, while an ordinary hyphen-space edit, punctuation, decimals, table values, and numbers remain material.
- Complete-replacement coverage asserts every old and new unit remains in the exact audit and in the material add/remove set. Existing randomized and 1,000-passage coverage tests still pass.
- AI cache fingerprints include semantic class and match metadata. The default dossier includes only substantive, added, removed, and uncertain units; complete exact differences remain reopenable in the UI.

## Document-identity gate — 3 September 2026

- 142 API regressions pass. Seven identity-specific cases cover persisted authority/ELI/language/extraction evidence, audited confirmation of unknown assignments, immutable rejection of an official mismatch, scan quarantine, AI cache invalidation after reassignment, and deletion of a mistaken non-current import.
- The tracked naturalization-decree regression imports an artifact that identifies itself as SR 910.13. Preview identifies the mismatch; a normal save is rejected; an inspection-only save remains available; saved-version comparison, Impact, and Ask are all blocked before any AI history record or provider request is created.
- A scan that fetches an artifact for another work saves it for inspection without moving the current live pointer or creating a comparison. Existing good evidence remains current.
- Every version stores a revisioned identity passport with authority, canonical work identifier, kind, title, language, dates, source URL, extractor, content type, filename, bounded evidence, and fingerprint. Every comparison stores its pair decision and fingerprint.
- An `unknown` artifact remains labelled unknown after a user confirms its assignment. The separate audit record permits the pair as probable; a contradictory official identifier cannot be confirmed.
- Re-evaluation changes the comparison identity fingerprint, so previous AI output is returned as historical and visibly stale instead of being reused.
- Python linting, the full API suite, TypeScript checking, frontend formatting, and the Next.js production build pass.

## Current bounded-triage regression — 3 September 2026

The earlier large-comparison checks below are retained as historical evidence of the failure mode: they proved complete passage coverage, but also showed why passage-volume-driven inference is not a useful product contract. The current pipeline supersedes that strategy while preserving the complete exact diff for inspection.

- 135 API regressions pass. New coverage verifies document-identity conflicts, multilingual titles under one ELI work, conservative numeric/date/punctuation handling, PDF line-wrap dehyphenation, unchanged passage moves, exact old/new coverage on 1,000 randomized comparisons, and stable large-input alignment.
- Impact has a hard budget of five provider HTTP attempts and Ask has a hard budget of three. Batches, retries, one structured-output repair, and any synthesis share the same counter and 180-second deadline; configuration cannot multiply these limits into one request per passage.
- A 3,600-plus-evidence comparison produces a bounded material-change dossier of at most three Impact batches or one Ask batch. The UI reports reviewed material units and limited coverage while the full deterministic audit remains available.
- A single evidence unit larger than the model window is sent as an exact contiguous excerpt tied to the full saved passage, so it cannot bypass the context budget and its citation is still server-validated.
- Vague conversational input returns localized clarification choices with zero model calls. A targeted general question with no matching saved passage also returns a useful narrowing prompt without arbitrary document excerpts.
- Formatting and structural noise stays out of the default AI dossier. Numbers, decimals, table values, punctuation, and uncertain differences remain reviewable and are never normalized away as layout noise.
- Impact permits zero suggested actions and removes exact duplicates. The UI explains when no evidence-backed action was found instead of generating repeated generic review instructions.
- TypeScript checking, frontend formatting, Python linting, the full API suite, `git diff --check`, and a Next.js production build pass.

## Completed checks

- 107 Python regression cases pass using isolated SQLite databases and actual API/service/migration code.
- State checks cover initial baselines, unchanged scans, 30 → 60 changes, A → B → A reuse, duplicate imports, repeated historical comparisons, and saved comparisons without source requests.
- Failure checks cover unavailable/empty sources, partial batches, overlapping scans, paused laws, invalid/cross-law input, and interrupted-run recovery.
- Evidence checks cover complete article/passage alignment, every saved passage exactly once, a 1,406-passage diff split into bounded change-aligned batches without omissions or duplicates, legacy persisted-diff upgrades, artifacts, PDF pages, missing records, old/new citations, exact quotes, and whitespace-only bogus quotes.
- Test-only model doubles cover timeout with an intact diff, analysis-only retry, exact Ask/Impact cache reuse, prompt cache boundaries, profile invalidation, saved successful and failed question history, complete batched Ask/Impact coverage, full extracted text from both saved originals for general questions, rejection of insufficient-context answers for complete change questions, rejected fabricated citations, schema echoes, provider wrappers, double-encoded JSON, trailing noise, invalid JSON repair, and failure after exactly one unsuccessful repair. Numeric model selections are range-checked, deduplicated, repaired once when wholly invalid, and materialized into exact saved citations by the server. Harmless list, intermediate-text, and citation overflow is clamped to the retained limits before content and citations are validated. These doubles do not establish that Apertus works.
- Settings checks cover immediate application, restart persistence, credential preservation/removal/environment inheritance, Infomaniak Product ID endpoint derivation, API-loaded model choices, derived local-Docker endpoints without credentials, configurable retries and batch concurrency, persisted prompt revisions, invalid-input handling without secret echoing, and configuration changes while an analysis is in progress.
- HTTP transport doubles exercise the actual model adapter's endpoint, credential and User-Agent headers, generation options, JSON mode, timeout, connection failures, and automatic recovery from a timeout followed by HTTP 503. They verify separate authentication, access, missing-route/model, quota, and upstream 504 errors without exposing provider bodies. Every retry is saved as a separate diagnostic attempt. They are test-only protocol checks, not a live Apertus deployment.
- Integration-log checks cover request/response capture for native websites, Fedlex, Firecrawl, and model-provider calls; recursive secret redaction; bounded text and binary summaries; provider/outcome filters; server-side sorting; detail retrieval; and clearing diagnostics without touching operational data.
- Deletion checks cover detaching monitored documents when a source is removed, permanently removing a document's versions/observations/comparisons/Impact analyses/saved questions/scan entries and unreferenced artifacts, and refusing document deletion while a scan is queued or running.
- TypeScript checking, formatting, Python linting, and a Next.js production build pass, including the Settings, Prompt settings, AI history, and Integration logs routes.
- The Helvetic Lens rebrand passes all 107 API regressions, TypeScript checks, formatting, Python linting, and a production build. Live HTTP checks return the new API title and product/tagline markup while the preserved workspace still exposes 4 sources and 4 monitored laws. The renamed `helvetic-lens-db-1` container sees 4 sources, 4 laws, 8 versions, and 9 comparisons in the existing PostgreSQL volume; the existing local SQLite file is also selected through its compatibility path. The optional local model is healthy under `helvetic-lens-local-apertus-1` without redownloading its cache.
- Linux API/web container builds pass. An isolated Compose stack starts with healthy PostgreSQL and API services; an existing database upgrades through both migrations. In that stack, a stable Fedlex ELI URL created one baseline, a subsequent scan completed as unchanged, the official resolved file stayed attached to the version, and the observation count increased without duplicating the version.
- A second app instance retains sources, paused state, versions, observations, comparisons, and completed scans in the same test database.
- The native API extracted a FINMA introduction, FDPIC FAQ, 108-page FDPIC PDF, and Fedlex ELI publications. The FINMA source was previewed and saved through the browser UI. These are source-compatibility checks, not claims of full legal coverage. See [SOURCES.md](SOURCES.md).
- Fedlex regression checks cover stable/current, language-neutral, and explicitly dated ELI parsing across the `cc`, `oc`, and `fga` collections; official HTML resolution and provenance; PDFs above the former 250-page limit; unavailable languages/formats; and rejection of search, print, and out-of-scope file routes before any download.
- Live resolver checks selected the current applicable English FADP expression (7 July 2025; 39,543 characters and 331 passages), retained the explicit 1 September 2023 expression separately (38,851 characters and 328 passages), used a 238-page PDF fallback for a French Federal Gazette ELI resource, and resolved the bare `fga/2002/316` work URL to the official 269-page German PDF/A (674,707 characters and 3,042 passages).
- A saved PublicAI credential connected to `swiss-ai/apertus-v1.5-8b`. A live changed-wording question identified all five deterministic changes with seven validated citations. A fresh, non-cached Impact run succeeded with medium impact, seven citations, and complete 7-of-7 changed-passage coverage. A question about the earlier version cited its exact saved passage; an unrelated question returned unsupported with no citations. The saved generation temperature was restored after cache isolation.
- The configured Infomaniak Product ID returned 11 models from its live `/models` route. Its only Apertus entry, `swiss-ai/Apertus-v1.5-70B`, was selected from that response and saved. A real non-streaming chat completion succeeded with Infomaniak's `max_completion_tokens` contract. Ask Apertus then returned a supported answer with seven validated citations and complete 7-of-7 changed-passage coverage; Impact succeeded with three actions and the same complete, untruncated evidence set. The saved API token remained absent from browser/API responses and Git.
- After explicit evidence-row numbering was added, the saved Infomaniak connection passed another real model test in 1,375 ms. A fresh one-batch Ask response and a fresh Impact response each used all 7 of 7 changed passages and produced seven server-validated citations; Impact also produced three actions. Repeating either request returned the saved result without another provider call.
- The affected large saved comparison then completed through Infomaniak after intermediate digest overflow handling was added: Ask processed all 1,854 of 1,854 changed evidence passages across 23 batches, returned a supported answer with 10 validated citations, and reported no truncation. Repeating the identical question returned the saved answer in 200 ms with its reuse count incremented and no provider work.
- The optional local Docker profile loaded the 1.5B Apertus instruct model as a roughly 3 GB BF16 GGUF through the official CUDA-enabled llama.cpp server. Its OpenAI-compatible `/models` and `/chat/completions` routes returned successfully, including an exact `{"status":"ok"}` structured response using the Apertus chat template. The Helvetic Lens draft-settings model list and connection test also passed against `http://127.0.0.1:12435/v1` in 172 ms. The integration log contained no `Authorization` header, and the saved Infomaniak configuration remained unchanged.
- After the resilience/history migration, a temporary small public-source comparison was exercised again with the saved Infomaniak configuration. Ask returned a supported answer with six validated citations, Impact succeeded with medium impact, and the unified history contained both records. Repeating the identical Ask and Impact requests produced zero new provider calls and returned their saved records. The temporary monitored document and its artifacts were deleted afterward.
- A non-persistent synthetic live check sent a complete 1,406-passage comparison through Ask Apertus in 14 bounded batches. It completed in 79.9 seconds with full coverage, no truncation, no HTTP 504, and 10 server-materialized citations. A separate Impact check deliberately used a 2,000-character batch target: all 120 passages passed through 30 batches in 63.5 seconds and produced three actions with 10 validated citations.

## Versioned official connector contract — 3 September 2026

- Fixture-backed checks exercise discovery, metadata, language expressions, official artifact retrieval, explicit relation extraction, and health for the shared v1 contract. Replaying a committed page creates no duplicate works, versions, receipts, or relations.
- A forced second-item failure commits the first safe boundary, leaves the source cursor unchanged, stores the exact item error and next index, and resumes from that index successfully on retry.
- Shared HTTP checks cover allowlisted HTTPS URLs and redirects, streamed response limits, bounded retry with jitter, one redacted diagnostic per attempt, official artifact hashing, extraction, immutable storage, and contract-drift degradation.
- Bounded live probes on 3 September 2026 passed the Fedlex RSS, Parliament catalogue, and Federal Supreme Court latest-index contracts. An initial denied Parliament response was surfaced as `degraded` without advancing ingestion state; after the contact user agent and compressed-response handling were corrected, the official catalogue probe passed.

## Fedlex catalogue connector — 3 September 2026

- Fifty-five focused tests cover RSS watermarks and overlap, stable ELI identity, `cc` keyset reconciliation, cycle reset, DE/FR expressions, dated and future versions, manifestation priority, ELI artifact redirects, SR/RS secondary identity, correctly directed JOLux basic-act/impact relations, registry events, contract drift, six stream definitions, invalid operational input, and idempotent merge with the unchanged Add law flow.
- The bounded read-only live smoke passed against the current official German RSS and SPARQL services. The feed exposed 50 items; the sampled `oc/2026/448` work returned DE/FR/IT expressions, one version, and one official relation. A one-item `cc` reconciliation page returned a stable next key without writing to the corpus.
- `scripts/check_fedlex_connector.py` performs this live check without downloading a document or advancing a database cursor. `POST /api/connectors/fedlex/{stream}/sync` runs one persisted page; status and recovery remain visible through `GET /api/connectors/status`.
- The complete API suite passes with 193 tests, Ruff passes, the Next.js production build passes, and the rebuilt Compose stack reports healthy API, PostgreSQL, Redis, and model-manager services at migration `c27f8d91a6e4 (head)`.

## Swiss Parliament connector — 3 September 2026

- Fixture regressions cover 50-row ID-ordered paging in smaller commit slices, complete/recent/known-active cursors, actual-language fallback, localized title and source text, stable/short identifiers, type/state, authors, committees, sessions, descriptors, official documents, exact ELI/SR/RS/related-affair candidates, status-only updates, immutable versions, partial-item recovery, and idempotent reruns.
- The source-contract spike confirmed that the affair list is ordered by ID rather than `updated` and offers no reliable global update-only feed. Pages 1 and 2 each returned 50 ascending IDs while their `updated` values were not ordered. This is why complete reconciliation is retained alongside recent-tail and known-active streams.
- The bounded read-only live smoke passed against API version 3.0.2. It observed 1,348 affair pages, 50 rows on page 1, actual DE/FR/IT records for a recent-tail affair, and 36 official artifact entries plus three unique downloadable document expressions for affair `20250069`. It wrote no corpus data and downloaded no linked document.
- The first live attempt exposed a double-decompression defect in the common bounded HTTP transport: `httpx` had already decoded the Parliament gzip body before the response was reconstructed with the original encoding header. The transport now removes stale encoding/length headers and has a compressed-response regression.
- The complete API suite passes with 199 tests, Ruff passes, and the Next.js production build passes. The rebuilt Compose stack reports healthy API, PostgreSQL, Redis, and model-manager services; both workers and the scheduler are running at migration `c27f8d91a6e4 (head)`, and the deployed OpenAPI contract exposes the Parliament synchronization route.

## Swiss Federal Supreme Court connector — 3 September 2026

- Fixture regressions cover bounded latest-index overlap, day-by-day current/previous-year reconciliation, exact Aza/docket identity, separate decision and insertion dates, actual German and French metadata, authoritative HTML reopening and extraction, exact SR/RS and article/act citations, two-second request pacing, safe partial-item recovery, idempotent reruns, challenge detection, and robots-policy drift.
- Source review confirmed the official latest/date index and decision HTML contract. It also corrected the earlier roadmap assumption: the sitemap declared in the court's `robots.txt` lists website content rather than yearly decision records, so reconciliation walks bounded official insertion-date pages and never fabricates `lastmod` coverage.
- The bounded read-only live smoke observed the newest insertion date, fetched one official decision, retained its stable Aza/docket metadata and German language, reopened the immutable HTML, and extracted 65,231 characters. It wrote no corpus data and respected the published two-second crawl delay.
- Degraded health now prevents discovery for every official connector, so a challenge page, changed template, or crawl-policy mismatch cannot advance a cursor. The per-decision safe checkpoint still allows a later item to retry without losing earlier committed decisions.
- `scripts/check_federal_court_connector.py` performs the live check. `POST /api/connectors/federal-court/{latest|reconcile}/sync` runs one persisted page; connector status remains available through `GET /api/connectors/status`.
- The complete API suite passes with 206 tests, Ruff passes, and the Next.js production build passes.

## Swiss Federal Criminal Court connector — 3 September 2026

- The court was selected for material relevance to criminal procedure, mutual assistance, sanctions, financial crime, and compliance monitoring, and because its official website exposes a maintainable server-rendered latest-decision list with stable dockets and direct PDF links.
- Four focused fixture tests cover bounded overlap paging, corrected-listing revisions, stable UUID/docket identity, DE/FR language and dates, court hierarchy, exact cited norms, idempotent receipt deduplication, template drift, discovery provenance, and reopening the original persisted PDF.
- The bounded read-only live smoke observed 50 unique latest decisions. It opened `SK.2025.29`, validated its French decision dated 24 November 2025, retained its 217-page PDF with SHA-256 provenance, extracted 614,405 characters, and produced 29 evidence-backed citation candidates.
- Health and admin metadata state the source boundary: this covers the current official latest list, not a complete historical catalogue. Absence cannot be interpreted as proof that no decision exists.
- `scripts/check_federal_criminal_court_connector.py` performs the bounded live check. The hourly `federal-criminal-court/latest` schedule uses the shared durable queue and one-second request floor.
- The complete API suite passes with 238 tests, Ruff passes, and the Next.js production build passes.

## Scheduled official synchronization — 3 September 2026

- Five focused synchronization regressions cover eleven idempotently seeded schedules, deterministic jitter, Swiss-time windows, pause/resume validation, manual active-run reuse, queue/disk backpressure, durable execution, safe retry boundaries, and two-organization event/feed fan-out over one shared official version and event.
- The full API suite passes with 211 tests, Ruff passes, and the Next.js production build exposes the new `/connectors` route.
- The rebuilt Compose stack upgraded the existing PostgreSQL database to `d81e7a42bc10 (head)` and started healthy API, database, Redis, model-manager, web, CPU worker, AI worker, and Celery Beat services. The worker subscribed to `ingest`; Beat admitted a due Fedlex run, the outbox dispatched it once, and the durable three-step job completed in the worker.
- The live Fedlex page encountered an individual current artifact that extracted too little text. The connector retained a partial/degraded run with one failed item and no cursor advance; the admin page showed that state instead of reporting a false successful watermark.
- Browser QA showed all eleven streams grouped under Fedlex, Swiss Parliament, and the Federal Supreme Court, with intervals, jitter, windows, pause/resume, **Sync now**, health, next runs, counts, free disk, and backpressure. The deployed page had no console errors or horizontal overflow at desktop width.

## Browser and PostgreSQL checks

- HL-031 adds a three-entry immutable Apertus catalogue, private model-manager API, dedicated model volume, hardware/compatibility probe, license acceptance, resumable `.part` downloads, free-space and SHA-256 checks, atomic activation, pinned llama.cpp runtime, retained-analysis removal guard, and durable download/start jobs. Four manager lifecycle regressions and an end-to-end admin API regression pass.
- On the development GTX 1070, Compose reported 8 GB VRAM, 15.6 GB RAM, a healthy pinned CUDA llama.cpp runtime, and three allowlisted models. The maintenance worker adopted the existing 981 MB Apertus 1.5B Q4 cache without a network download, verified `b03762d07f711220498174b970a927bb2e49798397289daaa2cc9c628a737ed3`, activated it atomically, and completed each persisted job once. The model became ready in about 17 seconds and returned valid `{"status":"ok"}` structured JSON in 4.0 seconds. The manager was then disconnected from its Internet-capable Docker network while retaining only the internal no-gateway model-control network: a Hugging Face request failed at DNS as expected, while the application connection check, a complete one-change Impact job, and a cited Ask job all succeeded through the local model. The external network was restored afterward for future allowlisted downloads.
- Browser QA opened **Local models** and verified GPU/RAM/disk/runtime visibility, active deployment, immutable revision/hash details, compatibility explanations, cached-copy labels, per-revision license acceptance, and lifecycle controls. The page had no browser console errors or horizontal overflow at desktop width.

- Used the unchanged **Add a law** dialog to preview the stable Fedlex FADP ELI URL. The browser displayed “Federal Act on Data Protection”, `text/html`, 39,543 characters, 331 passages, and enabled **Add to watchlist**; the preview was not saved to the user's watchlist.
- Used the same dialog with the language-neutral Fedlex `fga/2002/316` URL. It displayed “BBl 2002 2291”, `application/pdf`, 674,707 characters, 3,042 passages, and 269 pages instead of the 239-character JavaScript fallback; the preview was not saved.
- Opened the live Apertus Impact result in the comparison page, confirmed complete 7-of-7 evidence coverage and seven accessible citation links, then followed a citation to the exact referenced passage in the saved earlier version.
- Connected and previewed a website, discovered the synthetic policy, added it to the watchlist, pasted its earlier text, and selected the historical baseline through the actual interface.
- Fetched the current public GitHub fixture and inspected the actual diff: **2 added, 1 removed, 2 modified**, including deletion **30** and insertion **60**. Change filtering, jump navigation, and the saved old passage link were exercised.
- Repeated the historical scan while the current source stayed unchanged. Two immutable snapshots remained; each real fetch added an observation. An ordinary check separately reported **Unchanged**.
- Verified that a duplicate import with a newly stated date preserves original snapshot metadata and records the new date/provenance in the observation table.
- Discovery inspects bounded candidate pages, retains previews and errors, observes section/redirect limits, and does not create watched laws automatically. Tests cover 55 candidates with a 50-page limit, unavailable/empty candidates, time-budget exhaustion, and avoiding deeper traversal.
- Saved Apertus parameters through the Settings form in a disposable PostgreSQL workspace, using an explicitly synthetic, nonfunctional credential for key-handling verification. Editing another parameter preserved the key; saved key text did not reappear in the form.
- Tested an unsaved, unavailable local model address. The UI showed an error, and reloading restored the saved address and parameters. No model answer was fabricated.
- Selected the Public AI preset in the browser. Only the draft URL/model changed; key handling and generation values stayed intact, and saved configuration was unchanged.
- Opened the saved Infomaniak integration in the Settings UI and confirmed the provider selector, Product ID, generated read-only endpoint, API-backed model dropdown, credential controls, timeout, evidence threshold, completion length, temperature, Top P, presence penalty, reasoning effort, JSON mode, and fixed one-response/non-streaming note. The page showed the saved Apertus model without returning the token and logged no browser errors. Switching to custom mode and back restored the Product ID and generated URL; the completed form had no horizontal overflow at a 390-pixel viewport.
- Switched the live Settings draft to **Local Docker Apertus**, loaded the single `local-apertus` model from its API, and received a real connection-test reply in 281 ms. The page kept all request and generation controls visible, explained that no local credential is sent, and showed the derived host endpoint. Reloading without saving restored the saved Infomaniak provider and a clean form.
- Opened **Prompt settings** and confirmed all five editable instruction groups, automatic full-document versus changed-passages context selection, revision/source metadata, fixed validation explanation, original-artifact explanation, and disabled save/reset controls for unchanged defaults. Changing the draft context mode enabled save, and reverting it restored the unchanged state without persisting a mutation.
- Opened an existing comparison with four historical Impact attempts. The AI history displayed successful and failed records with comparison/version attributes, model, prompt revision, evidence coverage, original-artifact links, citations, and reuse count. The history grid was verified at desktop and 390-pixel widths with no overflow; the comparison and Prompt settings pages logged no browser errors.
- Opened Integration logs with successful Website, Fedlex ELI, and Infomaniak AI calls. Provider filtering and duration sorting returned the expected rows, and the Infomaniak detail showed both request and response while replacing the Authorization value with `[REDACTED]`.
- Opened and cancelled the clear-log confirmation, the source-removal confirmation, and the permanent document-history deletion confirmation. The source dialog correctly stated that its monitored document remains independently tracked; neither browser check mutated saved workspace data.
- Added and exercised a Hugging Face router preset for the same Apertus 8B model. Browser checks confirmed the URL/model, unchanged key handling and generation parameters, and no automatic save. Desktop/mobile widths had no overflow; TypeScript and formatting checks passed. No existing provider credential was sent to Hugging Face, so this is configuration verification, not successful inference.
- Checked the diff and Settings layouts at desktop and 390-pixel viewport widths; no horizontal overflow was found.
- Ran [the real HTTP smoke check](../scripts/smoke_http.py) multiple times through the web/API/PostgreSQL stack. The final run retained **2 versions and 13 observations**, with matching comparison IDs across repeated historical scans.
- Stopped and restarted the isolated web, API, and database services without deleting volumes. The recorded sources, paused state, snapshots, artifact content, completed scan, and browser-saved model settings all matched afterward.

The Windows agent's default pytest temporary directory had a permission conflict when switching execution contexts. The documented test command was rerun with a fresh task-local temporary directory and cache; no global permissions or user data were changed.

- Account recovery tests verify a hashed, expiring, single-use email-verification link; identical reset-request responses for registered and unknown addresses; reset expiry and rate limiting; Argon2id password replacement; revocation of all existing sessions; and the resulting security event. The production configuration rejects the development mailbox, while the browser build covers verification, resend, forgot-password, and reset screens.

## Fixed-budget AI analysis planner — 3 September 2026

- Focused regressions prove that a pending Impact record and its complete plan are committed before the first model request, then finalized without overwriting the planned inputs.
- A large synthetic comparison retains the complete deterministic audit, selects a bounded semantic dossier, records limited reviewed/total coverage, and plans no more than four normal Impact requests under the hard five-call ceiling. Ask retains its independent three-call ceiling.
- A formatting-only split-word fixture produces the deterministic low-impact result with zero model requests and no filler actions.
- Saved Impact and Ask history expose expected/actual calls, hard limits, evidence groups, queue wait, inference time, reported tokens, repair count, context fingerprint, and the exact comparison result link. Cached repeats retain the same plan and make no new provider request.
- All 245 API tests pass, including legacy-to-head migration coverage; Ruff, TypeScript, and the Next.js production build pass. The rebuilt Compose stack is healthy at `e31b8f6a2c90 (head)` with the plan columns present on both AI-history tables.
- Browser QA submitted a fresh cited question through the managed local Apertus runtime. It completed in one provider call and displayed `1 expected / 1 actual / limit 3`, 759 ms inference time, 2,158 reported tokens, zero validation repairs, and complete planned coverage in the saved comparison history.

## Actionable impact-report contract — 3 September 2026

- The validated `impact-report-v2` contract covers headline, potential materiality, old/new legal-unit changes, organization applicability, business areas, dates and deadlines, uncertainties, evidence strength, coverage, and zero to five structured review suggestions.
- Server-side finalization verifies exact saved citations, materializes their URLs/pages, explicitly records dates that were not found, separates evidence grade from severity, and rejects duplicate final action keys.
- Duplicate suggestions from different changes merge under one stable action key while retaining all related change IDs and distinct citations. A formatting-only comparison still yields zero actions and zero model calls.
- The cache boundary includes semantic diff, profile and prompt revisions, report/prompt schema, model runtime and generation parameters, and output locale. A failed rerun remains in history without replacing the last valid visible report.
- All 247 API tests pass, including legacy-to-head migration coverage; Ruff, TypeScript, and the Next.js production build pass.
- The rebuilt Compose stack is healthy at `e31b8f6a2c90 (head)`. Browser QA created a fresh local-Apertus report in one model call and displayed potential severity separately from supported evidence, the cited old/new passage, possible organization applicability, explicit `not found` date/deadline states, zero filler actions, full 1-of-1 coverage, and no desktop horizontal overflow.

## Intent-routed Ask — 3 September 2026

- Parameterized routing checks cover `explain_changes`, `organization_impact`, `actions`, `specific_unit`, `whole_document`, `vague`, and `off_topic`. The router sees zero document characters and makes zero provider calls.
- The exact Russian vague phrase from product feedback returns four useful choices in under one second. Off-topic input returns the document-review boundary with zero inference. The queued UI endpoint executes these two deterministic routes immediately even when other Ask work uses Celery.
- An existing current `impact-report-v2` satisfies canonical English change, applicability, and action questions without another model call. The saved answer records its source report, intent, scope, locale, selected change/evidence IDs, zero-call plan, and exact comparison; identical repeats use the Ask cache.
- Specific-article checks force targeted selection of the requested unit plus adjacent passages. Small whole-document checks use both complete saved versions, while existing large-context regressions verify explicit targeted scope when the serialized prompt and output reservation do not fit.
- Follow-up API checks pass the previous validated answer and its server-materialized citations, rather than question text alone. Change questions still use semantic-diff evidence and retain the hard three-request ceiling including repair and synthesis.
- All 256 API tests pass, including legacy-to-head migration coverage; Ruff, TypeScript, and the Next.js production build pass.
- The rebuilt Compose stack remains healthy at `e31b8f6a2c90 (head)`. Browser QA reused the fresh Impact report for **Explain the material changes simply**, displayed two exact citations and a zero-call fixed plan, then answered `Ничего не понятно но очень интересно` with a Russian clarification and four choices, also with zero calls. The comparison retained no desktop horizontal overflow.

## Decision-ready comparison review — 3 September 2026

- All 258 API tests pass. Coverage includes append-only action decisions, required dismissal/not-applicable rationale, actor/organization/report/action linkage, current-state reconstruction, durable job recovery from the comparison resource, real evidence-group progress, validation stage details, and safe resubmission of cancelled Impact jobs.
- Ruff, TypeScript, the Next.js production build, and the Compose image builds pass. The running stack reports healthy API, PostgreSQL, Redis, and model-manager services with both workers and the scheduler up at migration `f42c9d71a805 (head)`.
- Live browser QA on a saved local-Apertus comparison showed the five deterministic category counts before AI content, one compact material cluster instead of raw rows, a saved `Ready` job outcome, the current **What changed / Why it may matter / Review plan** report, exact before/after units, organization relevance, explicit not-found dates, assumptions, evidence grades, full report provenance, and a compact prior-reports link.
- Selecting **View exact evidence** switched from **Material first** to **All exact changes**, focused the correct saved passage pair, and retained the validated report alongside it. The live report legitimately contained zero suggested actions; the action workflow itself is covered through the real API, database, migration, and authorization boundary in the automated suite.

## Local-AI relation impact analysis — 3 September 2026

- Focused regressions create a real organization delivery from a Parliament event and monitored Fedlex-style law, then execute the persisted three-stage `ai_background` job below interactive Ask priority.
- One bounded dossier retains exact event/version evidence, monitored-law lifecycle and passages, deterministic retrieval reasons, the organization profile, and any confirmed official relation. The saved plan permits one normal generation plus one repair under the hard five-call ceiling and reports selected/available rows and characters.
- Server-side finalization materializes exact evidence URLs and quotations, separates potential severity from evidence grade, caps and deduplicates structured review suggestions, accepts a justified unsupported result with zero actions, and rejects out-of-range evidence rows after one repair.
- Cache regressions prove zero-call reuse when all dependencies match and a new analysis/history record when the organization profile revision changes. Failed attempts retain their evidence and cannot replace the latest successful report.
- A confirmed official `amends` relation remains unchanged and separately visible while the model output stays a labelled `potentially_impacts` proposal.
- The rebuilt Compose stack upgraded PostgreSQL to `a63e1c92b7f4 (head)`. A real existing candidate ran through the managed Apertus 1.5B Q4 model on the GTX 1070 in one background attempt and one provider call. It retained seven exact evidence rows, downgraded the model's unsupported `confirmed` evidence claim to `possible`, removed a generic filler action, produced a cited review-lead explanation, and persisted the `relation-impact-v2` result without changing the proposed corpus relation.
- All 265 API tests pass, including legacy-to-head migration and organization isolation. Ruff, TypeScript, and the Next.js production build pass; the final Compose stack is healthy with the API, both workers, scheduler, PostgreSQL, Redis, web app, and private model manager running.

## Organization impact inbox — 3 September 2026

- The inbox groups every delivered regulatory event once and nests all affected monitored laws beneath it, with source, severity, event type, personal state, and watched-law filters.
- Each law card separates why it appeared, the potential effect, the next review step, exact saved evidence, analysis history, and official-source/timeline/comparison links. Confirmed corpus relations remain distinct from organization review decisions.
- Read, dismissed, and muted state is isolated per user. Organization administrators can reanalyse, confirm or reject a lead, and add a confirmed successor to monitoring while retaining the predecessor and its full history.
- Forced reanalysis bypasses the cache, records a new immutable attempt, and leaves the last valid report visible if the new attempt fails.
- All 269 API tests pass, including grouping, filters, user-state isolation, review separation, reanalysis history, replacement monitoring, authorization, and legacy-to-head migration. Ruff and the Next.js production build pass.
- The rebuilt Compose stack is healthy at `c84d2e19a6f1 (head)`. Live API checks returned four grouped source events and four law impacts. Browser QA verified the complete inbox at desktop and mobile width, including Registry and Impact mobile navigation, with no console errors or horizontal overflow.

## Remaining gates

- Validate optional Firecrawl with a key and available credit.
- JavaScript-only portals, OCR, authentication-gated pages, and exhaustive multi-page discovery remain outside this narrow implementation.

## Fail-closed local-inference promotion gate — 3 September 2026

- The v2 benchmark cannot report success solely because 22 requests returned usable JSON. It also verifies the explicitly requested hardware profile, minimum CUDA inventory, optional GPU-name constraint, manager slot count, and runner-slot identities returned by the concurrent pair.
- Replicated acceptance requires two visible CUDA devices, at least two accepted slots, and two distinct runner slots. Split fallback requires two visible CUDA devices, exactly one accepted slot, and one observed runner slot; its report is explicitly non-equivalent to replicated acceptance.
- A mismatched profile, wrong GPU, missing replica, failed representative call, or failed concurrent call still produces a diagnostic JSON report but exits nonzero so automation cannot promote the model.
- Deterministic regressions cover valid replicated, valid split, valid GTX 1070 development, repeated-slot, wrong-profile, wrong-GPU, and failed-call cases. Physical dual-GTX-1080 execution remains required to close HL-032.
- A fresh live v2 control run on the attached GTX 1070 passed all seven gate checks: 20/20 representative and 2/2 serialized concurrent calls, 20/20 schema and citation validation, no timeout or OOM, 984 ms median latency, 62.38 tokens/second median throughput, and a 3,078-token maximum tested prompt. Its explicit `dev-1070` requirement prevents the report from being used as dual-GPU promotion evidence.
- Profile-selection regressions prove that a manually requested replica cannot bypass per-card headroom, split mode cannot exceed combined VRAM, and undersized one- or two-GPU hosts fall back to the CPU diagnostic profile. The chosen deployment now retains the exact model, reserved-headroom, required-layout, and visible-VRAM byte counts for target-host diagnosis.
- Replicated-state regressions prove that a partially warmed two-runner deployment stays `starting`, becomes `ready` only at two available slots, and remains `degraded` when a late-ready runner coexists with a failed replica. This closes the race that could previously overwrite degraded state with a false aggregate `ready`.

## Public single-server boundary — 3 September 2026

- The dedicated production Compose plan renders with PostgreSQL, Redis, one migration job, API, CPU/AI workers, scheduler, web, private model manager, and Caddy. Inspection of the rendered port bindings shows only TCP 80, TCP 443, and UDP 443; data, API, and inference services have no host binding.
- PostgreSQL, Redis, Python, Node, llama.cpp, and Caddy inputs are pinned by registry digest. The three project images build successfully from the production file, including the complete five-locale Next.js production build.
- The Caddy 2.10.2 image validates the checked-in TLS/reverse-proxy configuration. Startup ordering requires the migration to complete, PostgreSQL and Redis readiness to pass, the web service to become healthy, and only then the public proxy to start.
- The production environment validator passes a complete non-secret fixture and deliberately rejects the tracked placeholder template, HTTP origins, anonymous access, insecure cookies, private-network fetching, inline jobs, cloud-first AI, oversized uploads, and relative backup paths. Matching application validation prevents bypassing the critical controls.
- All 295 API tests pass, including the new database-and-Redis readiness endpoint and production configuration boundary. Ruff and TypeScript pass. The repository's pre-existing full-tree Prettier check still reports 26 application files; this deployment increment does not modify those files.
- Backup/restore, cleanup retention, cross-service correlation/metrics, and an actual target-host install/upgrade/rollback rehearsal remain open HL-048 gates.

## Backup and restore rehearsal — 3 September 2026

- The production backup service writes a PostgreSQL custom dump, complete document/evidence archive, environment, Caddy configuration, metadata, and SHA-256 checksums into a hidden partial directory, then publishes the timestamped directory and status marker atomically. The application receives only the marker volume; backup contents and credentials stay on the separately mounted destination.
- The scheduled service runs immediately and daily by default, retains completed timestamped backups for a bounded configurable period, restarts after failure, and supports an explicit one-shot pre-upgrade backup.
- Restore requires a strict timestamp identifier plus an identical confirmation value, rejects unexpected filesystem roots, verifies all component checksums first, restores PostgreSQL with ownership removed, and replaces the document volume. Host environment/configuration replacement remains a deliberate operator comparison.
- A live isolated rehearsal created a PostgreSQL `recovery_probe=before-backup` row and `/artifacts/recovery-probe.txt`, backed both up, changed both to `after-backup`, then restored. PostgreSQL and the artifact both returned `before-backup`; the isolated containers, networks, volumes, and host backup directory were removed afterward.
- The scheduled service and a manual pre-upgrade run now take the same exclusive destination lock. A pinned-image concurrency rehearsal starts two overlapping backups, proves they serialize into two complete checksum-valid timestamp directories, and verifies that `LATEST_SUCCESS` names the later complete backup.

## Operational retention and Redis recovery — 3 September 2026

- A daily maintenance task removes only expired redacted integration diagnostics, terminal non-connector job envelopes/steps/outbox messages, development mailbox files, dedicated temporary files, and unreferenced artifact files after configured grace periods. It atomically records its latest outcome for the platform status view.
- The regression creates expired and current diagnostics, an expired terminal job with children, an old active job, an old referenced evidence artifact, an old orphan, a recent orphan, and an expired mailbox file. Cleanup removes only the expected expired operational data and orphan while retaining the active job, current diagnostic, recent orphan, and referenced immutable evidence.
- The administration API reports exact configured retention bounds and the last cleanup time; the five-language UI renders the diagnostic-log duration rather than the earlier manual-cleanup placeholder.
- Existing durable-job regressions prove that a failed broker send leaves the PostgreSQL outbox pending, a stale worker lease is reconciled, duplicate submission reuses one idempotent job, and only one worker can claim it. Together with Redis AOF `everysec`/`noeviction`, this is the recovery contract to rehearse under target-host load in HL-049.

## Structured correlation and operational metrics — 3 September 2026

- Every API response carries a server-generated UUID request ID. Jobs persist that ID plus an allowlisted correlation envelope, and the worker restores it before connector, parsing, or AI work. Connector runs, scanned documents, regulatory events, comparisons, Impact analyses, and Ask records enrich the same envelope when their identifiers become known.
- Integration diagnostics expose the request ID and correlation envelope beside the already redacted request/response evidence. A regression follows one request context through durable job persistence and a simulated worker-side provider log, and proves an attempted token field is discarded.
- The platform status reports a bounded 2,000-request rolling window with in-flight count, p50/p95/max latency, 5xx count/rate, status classes, and normalized top routes. UUID and numeric path segments are collapsed to `{id}` and query values are never labels.
- Database query latency and Redis ping latency join the existing queue depth/age, connector freshness, model slots/GPU/RAM, disk, backup age, AI latency/citation/cache, and failure signals in the five-language administration console.
- All 299 API tests pass, including legacy-to-head migration, organization isolation, bounded-label metrics, and request-to-job-to-provider correlation. Ruff, the localization audit, TypeScript, and the Next.js production build pass.

## Reproducible capacity and recovery gate — 3 September 2026

- A confirmation-gated seed command creates 100 accounts in 10 isolated organizations without writing the supplied password to its manifest. Each organization receives a private synthetic law, two artifact-backed versions, a verified identity result, a deterministic material comparison, and a watch; rerunning the seed is idempotent.
- A matching confirmation-gated cleanup removes only the exact synthetic prefix, related metadata, and newly unreferenced artifacts. The development rehearsal removed all four temporary capacity datasets after testing.
- The real HTTP runner authenticates 30 representative accounts, drives 10–20 concurrent registry/evidence/comparison readers and filters, submits scans, one official connector job, and 20 AI jobs, then follows durable job state to completion. Reports include request IDs, phase p50/p95/max, queue wait/drain, execution duration, retries, connector timing, bounded platform status, and Docker/host/GPU/disk samples.
- Recovery mode restarts the scheduler, API, Redis, CPU worker, AI worker, and model manager individually. It verifies that each named service is running and that the API recovers before proceeding, then requests reactivation of the model that was active before the exercise.
- The model gateway now prefers a waiting organization that does not already occupy a slot while staying work-conserving when no other organization waits. Its inventory exposes aggregate slot, queue, priority, oldest-wait, and served counts without exposing organization names. PostgreSQL uses bounded per-process pools and production validation rejects an aggregate above the documented single-host limit.
- The development smoke created the required 100 accounts and 10 organizations. Sixty concurrent reads completed at 252 ms p50 and 388 ms p95; 22 command enqueues completed at 294 ms p50 and 333 ms p95; all responses carried request IDs and the unexpected HTTP error rate was zero. A separate recovery smoke restored all six named services. These runs do not count as the public-beta capacity result because they used the GTX 1070 development host, skipped some external work, and did not include the required target-host backup/restore and dual-GPU inference reports.
- The full API suite passes with 300 tests; model-manager admission regressions, capacity seed/cleanup tests, Ruff, the five-locale catalogue audit, TypeScript, and both development and production Compose rendering pass. Target-host execution on the intended i7/32 GB/two-GTX-1080 server remains the open HL-049 gate.

## Personal web and email digests — 3 September 2026

- Every user can keep separate daily or weekly preferences in each organization, opt into email, filter by severity and source, trigger a rate-limited delivery, and unsubscribe from a signed direct link.
- The preview and email are generated from the persisted impact inbox without a new AI call. They retain internal evidence/comparison links, exclude dismissed or muted items, and never alter personal read state or regulatory history.
- Celery Beat discovers due preferences hourly and creates one durable idempotent job per user, organization, and period. Empty periods and disabled mail are stored as skipped; SMTP/development delivery failures remain retryable.
- Delivery summaries are capped at 50 events and five impacts per event, the UI returns only the latest 20 attempts, and terminal delivery records have a separate 180-day default retention. Cleanup leaves user preferences, legal evidence, and impact history intact.
- Focused API regressions cover saved-data delivery, explicit SMTP failure/read-state preservation, scheduler idempotency, direct unsubscribe, viewer opt-in, and bounded cleanup. The full 304-test API suite, Ruff, the five-locale catalogue audit, TypeScript, and the Next.js production build pass. The rebuilt development stack is healthy and PostgreSQL reports `a1d8f63c2b74 (head)`.

## Hard-coded localization gate — 3 September 2026

- The catalogue check now parses TypeScript/TSX rather than relying on same-line regular expressions. It inspects JSX text, accessible string attributes, confirmation/prompt/error text, and rendered conditional string branches while explicitly allowing stable units and product names.
- The stronger gate found the English `read` badge and 27 user-facing settings strings that were previously hidden by multiline markup or conditional expressions. Local-AI, credential-boundary, generation, connection-test, and reset guidance now has distinct DE/FR/IT/RM/EN catalogue entries; the local model-manager hint also names its actual port 12436.
- The 1,195-key catalogue, inheritance/placeholder audit, TypeScript, and Next.js production build pass. HL-057 remains open only for the documented fluent German/French/Italian and native Romansh review; this automated work is not presented as human language approval.

## Evidence-first relation review — 3 September 2026

- Organization administrators can confirm, reject, or annotate a proposed relation only with a written reason. The API and database keep every decision, author, timestamp, and note as an append-only history.
- A non-decisive annotation leaves the latest confirmed/rejected state unchanged. A later decision supersedes the current organization status without deleting any earlier entry, model result, or source proposal.
- The Impact Inbox exposes the review form and complete newest-first history in DE/FR/IT/RM/EN. Viewers can inspect the history but the existing API authorization boundary rejects their mutations.
- Confirmed official metadata remains authoritative and cannot be replaced through organization review. The graph remains gated on measured improvement over the complete inbox/list workflow.
- The review panel now records a capped time-to-decision, fixed `inbox_list_v1` variant, and evidence-open flag with the saved review. The platform control room aggregates decision-time p95 and evidence-open rate without notes, source passages, organization names, user names, or candidate identifiers; unmeasured historical rows do not become artificial zero samples.
- All 305 API tests pass after the migrations, including append-only review history, annotation semantics, bounded workflow metrics, viewer authorization, legacy upgrades, connectors, and AI regressions. Ruff, the five-locale catalogue audit, TypeScript, the production web build, and the rebuilt development Compose stack pass; PostgreSQL reports `d4a8c1f6b205 (head)`.

## Reliable application shell and scrolling — 4 September 2026

- The desktop shell now owns the dynamic viewport. The route list and main content are independently bounded scroll regions, while the brand, organization switcher, account controls, support links, and sticky route header remain available.
- At 1280×600, the document stayed at 600 px with no document scroll while the 698 px navigation and 1,699 px main content scrolled independently. Keyboard traversal reached the final route without moving the document or main region.
- At 1024×768 with German labels, navigation stayed within its 168 px horizontal viewport (`scrollWidth === clientWidth`); keyboard focus reached `Einstellungen` with visible focus spacing at the bottom of the independently scrolling menu.
- At 390×844, the sidebar is removed from layout and the page uses one normal document scroller. The main region and AI history do not create nested vertical scroll traps, and the 375 px layout has no horizontal overflow.
- Safe-area padding is applied to viewport chrome and content. The Node shell contract, five-language catalogue audit, TypeScript/Next.js production build, and rebuilt Compose web image all pass.

## Task navigation and canonical Company profile — 4 September 2026

- Everyday navigation is grouped as **Today**, **Monitoring**, **Impact inbox**, **Discover**, and **Sources**. Workspace destinations stay separate, while settings, prompts, diagnostics, connector control, model control, and platform administration live in one collapsible role-gated group.
- Desktop and mobile render the same authorized workspace and administration fragments. Mobile exposes **Today** and **Monitoring** directly, names the current overflow destination, keeps workspace switching available, and places the remaining destinations in an explicit scrollable **More** menu; responsive browser checks found no horizontal overflow.
- `/discover` is the canonical saved-event view, while `/registry` is the canonical monitored-document view; legacy contradictory `?view=` URLs replace themselves without losing filters. Both controls expose selected tab semantics. `/organization#company-profile` is the only Company profile editor; Settings links to it, and the workspace selector only calls the organization-switch endpoint.
- The profile editor loads and saves the shared profile inline, shows viewers readable fields without edit controls, supports discard, and intercepts route links, browser back, reload/close, sign-out, and organization changes while edits are unsaved. It preserves a dirty draft across background refreshes and only arms navigation after an asynchronous switch or sign-out succeeds. Browser QA proved that initial hydration adds no history entry, normalized saves become clean, Save/Discard removes the guard entry, and the localized confirmation blocks navigation.
- Unknown or failed session state now grants no apparent administrative capability. The API regression proves that a platform administrator with a viewer organization membership can mutate platform defaults but receives `403 viewer_read_only` for organization profile writes.
- Eleven shell/navigation regression checks, the five-language catalogue/value audit, TypeScript, Ruff, focused authorization/relation API tests, the full 335-test API suite, the Next.js production build, and the rebuilt Compose stack pass. All running containers report healthy where a health check is defined. The daily digest fixture now uses a relative event time so its daily-window test does not expire on a future calendar day.

## Keyed resource updates — 4 September 2026

- A typed resource catalogue now assigns every UI read to a session, organization, or platform scope and an explicit feature owner. Cache identity includes locale only for localized resources; shared profile, provider, and prompt configuration cannot diverge when a user changes among the five UI languages.
- One external store owns stale-while-revalidate state, exact and family invalidation, in-flight deduplication, mutation race protection, scope epochs, centralized polling, hidden-page suspension for background work, focus/reconnect coalescing, and bounded least-recently-used eviction of inactive search and pagination entries.
- Ask job polling and Impact job observation use the same keyed store as mounted components. An enqueued Impact job is immediately persisted in its comparison snapshot, so leaving and returning resumes progress. Completion patches the report and history while preserving the immutable diff, local input, selected view, expanded evidence, and scroll ownership.
- Source, law, scan, job, relation-review, organization, settings, prompt, model, connector, integration-log, registry, inbox, matrix, and digest mutations now update or invalidate their smallest authoritative resource families. Organization changes reset session and organization epochs; sign-out clears all scopes so late responses cannot leak across identity boundaries.
- Sixteen resource regressions cover deduplication, stale-data continuity, exact/family request counts, hidden polling, reconnect coalescing, mutation and invalidation races, scope isolation, bounded eviction, Ask, Impact, relation review, watch creation, organization switch, sign-out, and source-level removal of the former global refresh broadcast. Eleven shell regressions, the five-language catalogue/value audit, TypeScript, and the Next.js production build pass.
- The rebuilt development Compose web image runs the resource contracts inside its production build. PostgreSQL, Redis, API, and model manager report healthy; browser smoke checks loaded persisted Local Docker Apertus settings and a saved comparison/Ask/history workspace after restart with no console errors.

## Responsive comparison decision workspace — 4 September 2026

- A wide comparison keeps exact saved evidence in the primary pane and a sticky 420–480 px companion in the viewport. Summary, Actions, Ask, and History are separate tabs whose content scrolls inside the companion instead of extending the document.
- The compact Summary keeps the saved headline, material relevance, applicability, and at most three action titles visible. Complete action decisions, material-change evidence, uncertainties, coverage, model execution metadata, provenance, and prior reports remain available through task tabs and disclosures.
- Citation controls in the current report, saved questions, and history resolve matching passage IDs to the deterministic diff, switch from semantic clusters to exact rows, focus the saved unit, and retain the companion selection. Unmatched citations still open their immutable evidence URL.
- At tablet widths the companion becomes a dismissible overlay drawer. At 390 px it becomes a full-screen task surface opened from the task tabs before the long diff; the deterministic filters, originals, pagination, and complete history remain available.
- Sixteen shell/comparison checks, sixteen keyed-resource checks, the 1,247-key five-language catalogue audit, TypeScript, and the Next.js production build pass. Live browser checks confirmed a 432 px sticky companion at 1440×900, a full-screen task surface, exact-row focus, retained Summary selection, no horizontal overflow, and no console errors.

## Durable asynchronous Ask — 4 September 2026

- Submitting Ask now returns a persisted job immediately. The comparison observes the organization-scoped job list, restores active work after navigation, and refreshes only AI history when the answer is saved; the deterministic comparison, filters, expanded evidence, and scroll position are not reloaded.
- Worker progress records evidence selection, generation, and validation after queue/model startup. The interface maps those durable states to localized queue, startup, evidence, generation, validation, completed, limited, failed, and cancelled cards with `aria-live` announcements and reduced-motion behavior.
- Questions remain visible through failure and cancellation. Recovery actions distinguish unavailable models, delayed queues, context limits, invalid output or citations, and unsupported evidence; users can revise, cancel, open Local models, or retry the same saved job.
- Duplicate and reconnect submissions reuse one idempotent job. A completed reuse increments the existing history record and is labelled as a saved cached answer; a failed retry updates its existing Ask record instead of creating a second shared-history entry.
- Twelve durable-job regressions and 57 focused analysis, identity, and organization-isolation tests pass. The complete frontend production build passes 19 shell/Ask checks, 16 resource contracts, the 1,309-key five-locale audit, TypeScript, and static generation. An additional 340 API tests pass when the Linux-only release-manager module is excluded from collection on Windows. The rebuilt development Compose stack serves a healthy PostgreSQL, Redis, API, and model manager; the login page responds successfully.

## Working mobile information architecture — 4 September 2026

- Mobile exposes **Today**, **Monitoring**, **Impact**, and **Discover** as four fixed bottom destinations. A labelled **More** control opens a bounded, independently scrollable menu containing Sources plus every remaining workspace and role-authorized administration route; Escape, outside pointer input, route changes, and the trigger all close it with focus restoration.
- Comparison presents Summary, Diff, Actions, Ask, and History as five compact task tabs before the deterministic evidence. The full labels remain available to assistive technology, while the selected companion task becomes a bounded full-screen phone surface with a visible close control.
- Phone layouts reserve safe-area space for the bottom navigation, keep the top context from forcing overflow, wrap long source URLs and diagnostics, bound dialogs to the dynamic viewport, and enforce at least 44 CSS px for buttons, inputs, selects, tabs, and close controls.
- A dependency-free Chrome DevTools journey checks the production Compose application at 360×844, 390×844, 430×844, and 768×1024. It covers Today, Registry, Impact, Discover, personal digests, Company profile, focused onboarding, and a saved comparison; detects document/body horizontal overflow and undersized controls; verifies exact primary destinations and all five comparison tasks in every supported locale.
- The same journey intercepts bounded local responses to render loading, empty, error, and viewer-shaped states. It proves that platform/configuration routes are absent from the viewer More menu, then switches Chrome itself offline and online and confirms the cached workspace remains present through recovery.
- The five-language catalogue/value audit passes with 1,320 production keys. Nineteen shell/comparison/Ask checks, sixteen resource-flow checks, TypeScript, the Next.js 22-route production build, and the rebuilt web image pass. PostgreSQL, Redis, API, and model manager report healthy after the run.

## Versioned source capability catalogue — 4 September 2026

- One versioned manifest covers all 23 scheduled official-source streams across Fedlex, Parliament, the Federal Supreme Court, the Federal Criminal Court, News Service Bund, and FINMA. Every entry records authority, publisher, jurisdiction, kinds, languages, cadence, incremental cursor, historical boundary, immutable artifact and provenance behavior, attribution, known gaps, and the last verified bounded live check.
- Catalogue promotion fails closed. Six exact streams with fixture, overlap, deduplication, provenance, drift, artifact-reopening, and dated live-smoke evidence are `available`; the other seventeen remain `partial` instead of inheriting evidence from another language or stream.
- The public capability API and all 23 operator schedule cards use the same serialized manifests. Runtime state separately reports available, syncing, healthy, degraded, unavailable, or partial, and the five-language Sources view groups the manifests into six official-source families with explicit boundaries.
- Nineteen focused connector/capability tests pass. The full Windows-compatible API suite passes with 344 tests; the known Linux-only release-manager module remains excluded on Windows. Ruff, the 1,334-key five-locale catalogue audit, TypeScript, nineteen shell/comparison checks, sixteen resource-flow checks, and the Next.js 22-route production build pass.
- A dependency-free Chrome DevTools journey verifies all six user-facing source families and all 23 operator contracts on the rebuilt production Compose stack at 360, 390, 430, and 768 px, in all five locales, for admin and viewer permissions, including loading, empty, error, and offline/reconnect states. The live API reported schema `helvetic-lens.source-capability/v1`, revision `2026-09-04.1`, 23 streams, six available, and seventeen partial.

## Organization source packs — 4 September 2026

- A versioned global Swiss Federal Starter exposes five separately controllable organization packs spanning all 23 capability streams: Fedlex legislation, federal consultations, Parliament, supported federal courts, and official policy/regulator notices.
- Pack definitions stay global while subscriptions and viewer change requests are organization-scoped. Organization-admin mutations are idempotent and audited; viewers can inspect every state and submit one reusable pending request without gaining a mutation path.
- Each activation creates one bounded ingest job per affected subpack. Backfill selects at most 500 unseen shared events and creates only organization event-state rows. Continuation skips prior inclusions; partial and failed states retain progress and errors. Active subscriptions receive future matching connector events, while deactivation stops future inclusion and retains all prior event state and immutable evidence.
- Six focused source-pack tests plus seven synchronization/capability regressions pass, including exact connector-and-stream pair filtering during backfill. The full Windows-compatible API suite passes with 349 tests; the Linux-only release-manager module remains excluded on Windows. Ruff, the 1,359-key five-locale audit, TypeScript, nineteen shell checks, sixteen resource-flow checks, and the Next.js 22-route production build pass.
- The rebuilt Compose stack applied Alembic head `b27f5a91c304`. A live activation completed through the real Redis/Celery ingest worker, reused 100 saved events, created zero shared documents or events, and then deactivated cleanly. The Chrome journey rendered five pack cards alongside six capability families at 360, 390, 430, and 768 px across all five locales, admin/viewer roles, and offline recovery.

## Durable monitoring topics — 4 September 2026

- Organization topics persist their complete plan and every edit or lifecycle transition as an immutable revision. Idempotency keys reuse one create result, optimistic revision checks reject stale updates, and archival retains the full record.
- The optional local-first model path writes an inactive draft only. Strict structured output is validated against the available source-pack and plan contracts, repaired at most once, exposed for editing, deterministically previewed, and explicitly confirmed before monitoring becomes active. Manual creation remains independent of model availability.
- Preview examines at most 500 recent saved events and displays at most ten representative candidates with exact term and source-pack signals. Persistence and UI both identify these as topic candidates and never as confirmed legal relations.
- Focused API regressions cover draft repair and confirmation provenance, manual idempotency, revision immutability, bounded preview, lifecycle, tenant isolation, and viewer permissions. The full Windows-compatible API suite passes with 355 tests; Alembic reports the single head `c3a81e92f7b0`. Ruff and the 1,428-key five-language Next.js production build pass. The rebuilt Compose stack is healthy, and its live Topics screen exposes the loaded five source packs, manual builder, optional AI draft, preview, and empty state.

## Bounded topic matching — 4 September 2026

- Matching starts from organization-visible normalized events and current active topic revisions. It reuses source-pack membership and the established title/citation normalizers, applies exact official references and PostgreSQL full-text retrieval before controlled metadata filters, and never upgrades relevance into a legal relation.
- Each persisted candidate keeps exact topic/event/document identity, source and official identifier evidence, reason signals, deterministic evidence/rule fingerprints, confidence, review state, and retention timestamps. Same fingerprints reuse one row; changed evidence is reprocessed and a topic edit creates a separate revision-bound identity.
- Connector work is capped at 100 organizations per event, 50 topics per organization/event, and 20 matches. Topic creation/edit/resume creates one idempotent ingest job over at most 500 already-visible saved events. The regression constructs 101 organizations and proves only 100 are considered.
- The focused matching, topic, relation-candidate, and maintenance suite passes with 26 tests, including five relevant national-language cases, explicit exclusion/noise controls, durable backfill, revision/evidence reprocessing, and expiry behavior. The complete Windows-compatible API suite passes with 365 tests; only the unchanged Linux-only `fcntl` release-manager test is excluded on Windows. Ruff, the 1,428-key five-language audit, TypeScript, the 23-route production build, and the rebuilt Compose stack pass. PostgreSQL reports `d4b92f03a8c1 (head)`. pgvector and ambiguous model expansion remain off until reviewed metrics justify them.


## HL-097 immediate contrast correction — 4 September 2026

- Replaced every `color: var(--muted)` surface-token misuse in global CSS with the semantic text token, including AI tabs/close, triage, job status, applicability/dates, action metadata, prompts and history. Darkened `--muted-foreground` to `#566176` without changing muted surfaces.
- `npm run check:ai:contrast` compiles the real stylesheet through the installed Tailwind/PostCSS pipeline and renders focused component fixtures in disposable local Chromium. No API, model, live data or paid request is required. The gate fails if Chromium or expected fixtures are absent.
- Reproduced the original defect using the baseline stylesheet: Actions failed at **1.12:1** at 390px. With the correction, **345 rendered samples pass, minimum 6.00:1**, across 390×844, 768×1024, 1024×768 and 1440×900. Tests cover enabled default/selected/hover text, real Tab-key focus and its ≥3:1 outline, metadata and error text. Disabled controls are excluded from text-contrast thresholds and cannot receive focus.
- `npm run typecheck` passes: 1,526 production localization keys, 27 shell checks, 22 resource/interaction checks and TypeScript. Shared CDP transport was extracted without changing the existing mobile journey behavior.
- Scope limit: this focused Chromium fixture gate is **not** complete populated-route, screen-reader, physical-mobile, zoom, Firefox/Safari or five-locale usability acceptance. Those explicit HL-097 criteria remain open; no WCAG certification is claimed.


## HL-100 evidence floor and legacy-result safety — 5 September 2026

- Relation candidate rule v2 filters instrument/authority boilerplate in DE/FR/IT/RM/EN and standalone numeric tokens. The reported climate/direct-payments pair no longer qualifies solely through `uber, verordnung`; exact norm-reference retrieval is preserved. No held-out recall calibration is claimed.
- Relation-impact v3 checks each positive conclusion and each action against a saved source/target passage pair with work/version/passage identity, or the matching confirmed official-relation row. Metadata/rationale-only citations, missing document sides, unrelated work IDs and out-of-range references cannot supply that evidence floor. Fixed action-loop variable shadowing.
- Known generic/contradictory prose and missing evidence become explicit unassessed results, with no AI High rating or actions; the localized explanation says this does not establish absence of impact. The inbox presents unknown AI importance/Needs re-analysis while preserving independently recorded event urgency and official relations.
- Earlier result schemas remain byte-for-byte history with a stale badge; they no longer provide the current conclusion. A repeat analysis uses the same event/delivery, appends history, and produces no extra inbox item. This does not yet implement automatic bounded historical candidate repair or complete read-time profile/runtime freshness.
- **422 Windows-compatible API tests passed**; only the unchanged Linux-only release-manager module was excluded. **56 targeted tests passed**, including **24 new adversarial regressions** plus relation analysis/candidate/inbox and topic-matching tests. Ruff and TypeScript/localization/resource checks pass. These are deterministic test doubles, with no provider calls, human participants or target-hardware benchmark.
- Remaining HL-100 gates: independently labelled precision/recall, factual entailment and calibrated severity, bounded candidate repair, complete current-input freshness. A cited passage pair is a prerequisite for analysis, not proof that the model interpreted it correctly.


## HL-093 honest metric compatibility — 5 September 2026

- Canonical deterministic-overview latency remains separate from observed human comprehension. The obsolete `time_to_first_useful_insight` API alias retains its values for existing clients but is explicitly deprecated, typed as optional in the frontend, and points to `latency.deterministic_overview`. The dashboard already reads/names the canonical measure in five locales. No stored timing or user review was rewritten.
- Release evaluator v2 uses the `deterministic_overview_latency_measured` check; both current and explicitly typed older capacity exports are accepted. Empty/null canonical measurements cannot be replaced by optimistic legacy values. Bound human-review evidence is still required independently.
- Missing/invalid/nonfinite latency observations are excluded rather than manufactured as 0 ms; measured zero is retained. The human-review validator also rejects nonfinite observations.
- **45 targeted tests passed** across metrics, release/usability validation, administrative API and relation-graph experiment compatibility, plus Ruff and the TypeScript/localization/shell/resource checks. Cases demonstrate that fast machine timing and citation clicks do not pass failed comprehension. These test participants are synthetic fixtures only.
- This is the metric-compatibility portion of HL-093. No independent gold-set labels, reviewer judgments, semantic pass rate, user-research results or hardware performance claims were produced.

## HL-091 truthful selected-evidence mode — 5 September 2026

- Impact report v3 and persisted Ask output identify selected evidence, generated explanation and deterministic replies separately from evidence coverage. Cache/history reuse preserves the original mode even when reuse costs zero provider calls. Earlier reports are retained, labelled legacy and made stale through the versioned cache boundary.
- The existing local selector cannot establish High/Medium/Low impact, organization applicability, actions or absence of dates. Its final report now explicitly leaves those unassessed; empty actions do not mean no action is needed. Quotations retain their saved version side and cannot imply deletion/addition or align unrelated articles solely by citation order.
- **460 Windows-compatible API tests passed** before the final unsupported-reply origin correction; the unchanged Linux-only `fcntl` release-manager module was excluded. All **16 focused selected-evidence tests passed** after that correction. Tests use deterministic doubles, not live provider calls or independent judgments of model quality.
- The Next.js production build, **1,532-key** five-locale catalogue/value audit, **27 shell** checks, **22 resource** checks, TypeScript and Ruff passed. The real notice component/catalogue is included in the Chromium contrast fixture: **645 rendered samples passed, minimum 4.79:1**, at 390, 768, 1024 and 1440 px, including all five locales and legacy mode.
- This fixture check is not complete populated-route, native-language, screen-reader or physical-mobile acceptance. No model was downloaded, swapped, promoted or benchmarked, and no production deployment occurred. Provider-based adapter selection still requires the approved capability/profile routing work in HL-091; generated mode describes response origin, not verified legal correctness.

## HL-092 date mentions with source provenance — 5 September 2026

- Report v4 removes automatically absent effective dates/deadlines. A deterministic scanner locates literal calendar/period mentions in the full saved text of the selected material dossier. It preserves exact quote, version/passage/change identity and side, scans past preview/display limits, and records scanned/detected/displayed counts. It does not interpret dates, calculate deadlines or assess applicability; missing matches stay legally unreviewed.
- The comparison and history share a localized date renderer with compact entries, collapsed source context and the existing citation controls. Legacy reports remain readable with original values/timestamps and a recorded-status warning. The new schema/cache boundary requires explicit reassessment instead of rewriting history. Missing action due bases now say unreviewed.
- **93 focused API tests passed**, including **22 new date regressions**: five source languages, absent matches, numeric ambiguity, invalid calendar strings, relative periods, proposal/exception/repeal context, source quotes beyond preview, old/new sides, repeat occurrences, display limits, both adapters and cache/history immutability. These are synthetic regression fixtures, not independent legal assessments.
- The complete Windows-compatible API suite passed with **483 tests**. Only the unchanged Linux-only `fcntl` release-manager module was excluded; its Ubuntu deployment gate remains separate.
- **20 actual-component render tests passed** across DE/FR/IT/RM/EN and populated/empty/limited/legacy states. They use the production catalogue/formatter and run in `npm run build` and `npm run typecheck`. The production build, **1,545-key** locale audit, **27 shell** checks, **22 resource** checks, TypeScript and Ruff passed.
- The focused Chromium gate passes **3,260 rendered contrast samples, minimum 4.79:1**, including expanded date quotes at 390/768/1024/1440 px. Date sections fit their pane; a source disclosure accepts focus, opens with Space and reaches its evidence link with Tab. These are isolated fixtures with no live user data, not full route/human/screen-reader acceptance.
- No extra LLM requests, dependency, migration, model swap or production deployment. Determining legal date type, reviewed absence/not-applicability, relative-period anchors, native-language/domain validation and the broader HL-092 decision/action contract remain open.

## HL-094 resumable saved-topic history — 5 September 2026

- History checks now use a stable organization-admission-time/ID cursor and first-start cutoff, process at most 500 events per execution, and persist matches, counters, checkpoint and continuation outbox in one transaction. Successful batches yield the worker without exhausting retry attempts. A failed batch rolls back; cancelled/failed checks resume their saved progress. Superseded revisions retain their actual processed count.
- Topic replay queries only its owning organization, so the global 100-organization shortlist cannot silently omit that owner. PostgreSQL job/plan locks protect each batch; lease reconciliation skips actively locked transactions. A composite history index is supplied by migration `f8b394a26d10`.
- The topic API/card exposes pending/completed/failed/cancelled/legacy-limited coverage and checked/remaining/matched/excluded counters in DE/FR/IT/RM/EN. Administrators can idempotently recover legacy truncated work or resume failed/cancelled work. Viewers and other organizations cannot enqueue it. Unknown legacy totals are not shown as zero and an empty saved history is not reported as absence of developments.
- Automated cases cover 501 events, identical timestamps, duplicate broker delivery, rollback after writes, first-attempt failure/capture consistency, more successful batches than the retry budget, cancellation/resume, stale revisions, owner-after-100 scope, checkpoint rejection, legacy recovery, fair yielding between topics, exclusions and removed unchecked admissions. No model calls or notifications were made.
- The final Windows-compatible API run passed **493 tests**; the unchanged Linux-only `fcntl` release-manager module was excluded. After integrating HappySnowman’s build fix, all **7 production-deployment tests** passed as well. The combined Linux Docker build reran the frontend gates. The only API warning is the existing Starlette/httpx deprecation.
- The isolated **PostgreSQL 16.14** gate passed the real 501-event/outbox path, populated index downgrade/upgrade, active-lock skip/recovery and plan-lock/completed-checkpoint replay. Its disposable container/database was removed. The ordinary API tests use isolated SQLite fixtures, not production data.
- TypeScript, Ruff, **1,565-key** five-language catalogue/value audit, **27 shell** checks, **22 resource** checks and **40 actual-component render** checks passed. The focused Chromium fixture passed **5,500 contrast samples, minimum 4.79:1**, at 390/768/1024/1440 px, including topic history widths. These are regression fixtures, not native-language, screen-reader or full populated-route usability acceptance.
- Integrated HappySnowman's `943c843` by fast-forward before publication; its new Docker context gate passed. The actual Next.js production build and **Linux Docker web build** passed with both changes. Windows temporary pytest directories are now excluded from Docker context; no permissions or local test files were removed. The temporary web-image tag was removed after testing.
- This completes the saved-history slice only. Live fan-out over 100 organizations / 50 topics / 20 matches, preview/scorer parity, feed-wide monitoring-from and connector degradation remain open in HL-094. Capture times describe local saved-event visibility, not legal publication/effective dates or complete website coverage.
- No model installation, model swap, live provider request, production restart or production migration was performed. The new index is exercised only in disposable test databases; production rollout remains a separate release operation.
